# 开发记录

## 2026-06-01 16:10:00 +08:00 - Stage 5 CI / Fixture Agent

署名: Codex

### 本轮改动

- 阅读并评审最新文档 `docs/full-project-audit-2026-06-01.md`、`docs/security-audit-2026-05-29.md`、`docs/remediation-plan-v2.1.md`。其中 v2.1 Phase 5 明确记录 `cargo deny`、formal CycloneDX/SPDX SBOM、小型 RAW/E01 fixture 策略仍未完成；本轮补齐其中 CI 与 tiny RAW/logical fixture 部分。
- 新增 `deny.toml`，启用 advisory、license、ban、source 检查；现有 Tauri/Tantivy transitive unmaintained advisories 均带 owner、reason、expiry date。
- 后端 CI 新增 `cargo deny check`，保留 `cargo audit`，并将 backend SBOM 从 `cargo metadata` 替换为 CycloneDX JSON artifact `backend-sbom.json`。
- 前端 CI 将 `pnpm list` lightweight SBOM 替换为 `@cyclonedx/cyclonedx-npm` CycloneDX JSON artifact `frontend/frontend-sbom.json`，并增加 JSON 格式验证。
- 新增 `testdata/fixtures/tiny/`：包含 tiny logical directory 和 1024-byte tiny RAW fixture；新增 `scripts/generate-tiny-fixtures.ps1` 可重建 RAW fixture。
- `crates/testing` 暴露 fixture path helper：`tiny_logical_dir()`、`tiny_raw_image()`、`local_e01_fixture()`，并用单元测试保证默认 fixture 可用。默认测试仍不依赖私有本地 E01。
- `runtime-cache` 补齐 workspace authors/license 元数据，使 license policy 可以覆盖所有 workspace crate。
- 本地 `cargo audit` 额外暴露 `glib` 与 `lru` warning-class advisories，均为 Tauri/Tantivy transitive dependency；`cargo deny` 当前目标图未命中这两项，因此不写入 deny 例外，后续应通过升级 Tauri/wry/Tantivy 消除。

### 最新文档评审

- `docs/full-project-audit-2026-06-01.md` 对 Stage 5 风险判断仍准确：原 CI 的 SBOM 是 lightweight metadata/list，不是 formal SBOM；默认 E01 慢测风险需要继续隔离。
- `docs/remediation-plan-v2.1.md` 的 Phase 5 状态在本轮后应更新为：5.2/5.4 completed for CI baseline，5.5 partial，因为 tiny E01 尚未提交，仅有 opt-in `FORENSICS_E01_FIXTURE` 策略。该文档仍保留为历史 backlog 状态，本轮不直接重写，以免与并行阶段文档编辑冲突。

### 验证记录

- `cargo fmt --all -- --check`: 通过。
- `cargo test -p testing`: 通过，3 个 fixture helper 单测通过。
- `cargo deny check advisories bans licenses sources --hide-inclusion-graph`: 通过；仍有 duplicate dependency warnings，属于现有依赖图治理项。
- backend CycloneDX 本地验证：`cargo cyclonedx --manifest-path apps/desktop/src-tauri/Cargo.toml --format json --spec-version 1.5 --override-filename backend-sbom` 生成 `backend-sbom.json`，验证 `bomFormat=CycloneDX`、`specVersion=1.5`、components=401。
- frontend CycloneDX 本地验证：`pnpm dlx @cyclonedx/cyclonedx-npm --output-file frontend/frontend-sbom.json --spec-version 1.6 --output-format JSON --validate frontend/package.json` 生成 `frontend/frontend-sbom.json`，验证 `bomFormat=CycloneDX`、`specVersion=1.6`、components=455。
- `cargo audit`: 通过，退出码 0；报告 19 个 warning-class advisories，主要来自 Tauri Linux GTK/WebKit transitive stack、Tantivy transitive `lru` 和 `urlpattern`/rust-unic。CI 仍保留该检查，短期风险用 `deny.toml` 到期例外跟踪。

---

## 2026-06-02 01:13:05 +08:00 - v2.1 Backlog 主线程统一复核

署名: Codex

### 本轮改动

- 复核四个 subagents 已进入当前 HEAD 的实现，重点检查 Analysis provenance、Reports provenance、Job partial/warning 语义、Viewer scoped range、CI/SBOM/fixture 和文档状态。
- 将 `crates/image-e01/tests/e01_regression_test.rs` 与 `crates/image-e01/tests/e01_dump.rs` 中残留的私有 E01 路径改为 `FORENSICS_E01_FIXTURE`，并统一标记为 ignored opt-in slow tests。
- 更新 `docs/remediation-plan-v2.1.md`、`docs/architecture-model.md`、`docs/algorithm-complexity-analysis.md`，修正旧状态：Analysis provenance、Reports provenance、Job partial、dependency gate、CycloneDX SBOM 和 tiny logical/RAW fixtures 已落地；真实 Registry key/value parser、真实 EVTX parser、连续媒体 streaming、tiny E01 fixture、FS enum 去重和 command SQL 下沉仍是剩余项。
- 新增 `development-reports/sessions/2026-06-02.md`，记录 subagent 分工复核、验证结果和剩余风险。

### 验证记录

- `cargo fmt --all -- --check`: 通过。
- `cargo clippy --workspace --all-targets -- -D warnings`: 通过。
- `cargo test --workspace`: 通过；真实 E01 tests 默认 ignored。
- `cargo build -p forensics-desktop`: 通过。
- `powershell -ExecutionPolicy Bypass -File scripts\check-release-guard.ps1`: 通过。
- `cargo doc --workspace --no-deps`: 通过。
- `cargo deny check advisories bans licenses sources`: 通过；仍有 duplicate dependency warnings。
- `cargo audit`: 通过，退出码 0；仍报告 19 个 allowed warning-class transitive advisories。
- `pnpm --dir frontend typecheck`: 通过。
- `pnpm --dir frontend lint`: 通过，0 error，7 个既有 warning。
- `pnpm --dir frontend test`: 通过，13 个测试文件 / 51 个测试。
- `pnpm --dir frontend build`: 通过；Vite 主 chunk 约 515KB warning。
- `pnpm --dir frontend audit --audit-level high`: 通过。
- `git diff --check`: 通过。

---

## 2026-06-02 02:35:01 +08:00 - 可信 Beta 收口执行

署名: Codex

### 本轮改动

- 开发前读取并评审 `docs/remediation-plan-v2.1.md`、`docs/architecture-model.md`、`docs/algorithm-complexity-analysis.md`、`docs/开发记录.md` 与 `development-reports/sessions/2026-06-02.md`。上一轮文档中的剩余项判断准确：大媒体协议、Registry 定向解析和 EVTX adapter 是本轮主线。
- 新增 `evidence-media://handle/<encoded>` Tauri protocol，保留 `read_media_range` fallback；protocol Range parser 做 overflow-safe 解析，单次读取 clamp 到 1MB，并通过 evidence reader helper 读取，不暴露宿主证据绝对路径。
- Registry 从 header/provenance stub 推进为 Analysis 定向字段 parser：bounded 读取 SYSTEM/SOFTWARE hive，解析 `ComputerName`、timezone、Windows product/build、install date、owner/org/product id，并给每个字段输出 `fieldProvenance`。
- EVTX 从 unavailable/notParsed stub 推进为 boot/shutdown candidate adapter：bounded 解析 System.evtx 中 6005/6006/6008/1074 事件，输出 event id、record id、note 和 provenance。报告和 UI 明确标注为候选事件，不宣称绝对开关机事实。
- DataAnalysis、frontend models、mock data、Reports HTML/CSV/JSON 同步新增 field provenance 和 EVTX candidate 展示；保留 mock mode、HTML escaping 和 CSV formula sanitization。
- `deny.toml` 新增 `RUSTSEC-2021-0153` 临时例外，用于跟踪 `evtx -> encoding` unmaintained transitive advisory，包含 owner、reason、expiry。
- 更新 `docs/remediation-plan-v2.1.md`、`docs/architecture-model.md`、`docs/algorithm-complexity-analysis.md` 和 `development-reports/sessions/2026-06-02.md`。未启动新的 subagents，因此没有闲置 agent 需要关闭。

### 验证记录

- `cargo fmt --all -- --check`: 通过。
- `cargo clippy --workspace --all-targets -- -D warnings`: 通过。
- `cargo test --workspace`: 通过；真实 E01 tests 默认 ignored。
- `cargo test -p app-services --lib analysis_service`: 通过。
- `cargo test -p app-services --lib report_service`: 通过。
- `cargo test -p artifacts-windows registry`: 通过。
- `cargo test -p artifacts-windows evtx`: 通过。
- `cargo test -p transport analysis`: 通过。
- `pnpm --dir frontend typecheck`: 通过。
- `pnpm --dir frontend lint`: 通过，0 error，7 个既有 warning。
- `pnpm --dir frontend test -- analysis files`: 通过，4 个文件 / 20 个测试。
- `pnpm --dir frontend test`: 通过，13 个文件 / 53 个测试；ErrorBoundary 测试打印预期 jsdom stack。
- `pnpm --dir frontend build`: 通过；Vite chunk size warning 存在。
- `cargo build -p forensics-desktop`: 通过。
- `powershell -ExecutionPolicy Bypass -File scripts\check-release-guard.ps1`: 通过。
- `cargo doc --workspace --no-deps`: 通过。
- `cargo deny check advisories bans licenses sources`: 通过；仍有 duplicate dependency warnings。
- `cargo audit`: 通过，退出码 0；报告 20 个 warning-class advisories，其中 `RUSTSEC-2021-0153` 来自本轮 `evtx -> encoding`，已由 `deny.toml` 到期例外跟踪。
- `pnpm --dir frontend audit --audit-level high`: 通过，无已知 high/critical 漏洞。
- Slow E01 manual gate 未运行：当前环境未设置 `FORENSICS_E01_FIXTURE`；提供合法真实 E01 样本后再执行两条 ignored slow test。

### 剩余风险

- EVTX adapter 缺合法 tiny real `.evtx` fixture；当前测试覆盖 JSON 提取和 malformed/oversized path。
- Registry parser 是定向字段 parser，不是完整 hive browser。
- `evidence-media` protocol 尚未做 Windows WebView2 桌面 seek smoke。
- `evtx -> encoding` advisory 例外需在 2026-09-01 前替换或复审。
- tiny E01 fixture 尚未入库；真实 E01 慢测仍依赖 `FORENSICS_E01_FIXTURE`。

---

## 2026-06-02 02:47:09 +08:00 - EVTX fixture/dependency 风险收束

署名: Codex

### 本轮改动

- 未启动新的 subagents，因此没有闲置 agent 需要关闭。
- 复核当前分支 `codex/beta-forensics-backlog`、HEAD `7d12299`，工作树仅有既有未跟踪 `.context/plans.md`；本轮继续不 stage、不删除该文件。
- 本地复核 `evtx 0.11.2` 发布包：`Cargo.toml.orig` 声明 `exclude = ["**/*.evtx", "**/*.dat"]`，因此 crates.io package 中没有可直接复用的合法 tiny real EVTX fixture；license 为 `MIT/Apache-2.0`。
- 本地复核依赖链：`cargo tree -p artifacts-windows -i encoding` 显示 `encoding v0.2.33 -> evtx v0.11.2 -> artifacts-windows`；`encoding = "0.2.33"` 是 `evtx` 的直接非可选依赖，不能通过 feature trimming 消除 `RUSTSEC-2021-0153`。
- 为 EVTX adapter 补充 truncated `ElfFile` magic 输入回归，覆盖更接近真实 EVTX header 的损坏输入，要求返回 warning 且不 panic。
- 更新 `docs/remediation-plan-v2.1.md` 和本记录，明确 EVTX fixture 缺口与 dependency exception 不是配置遗漏。

### 验证记录

- `cargo test -p artifacts-windows evtx`: 通过，6 个 EVTX targeted tests。
- `cargo fmt --all -- --check`: 通过。
- `cargo clippy --workspace --all-targets -- -D warnings`: 通过。
- `git diff --check`: 通过。

### 剩余风险

- 仍缺合法 tiny real `.evtx` fixture；下一步需要自行生成、引入可再分发 fixture，或替换 parser 路径。
- `evtx -> encoding` 临时例外仍需在 2026-09-01 前替换、vendor/patch 或重新评审。

---

## 2026-06-02 02:52:17 +08:00 - Dependency exception guard

署名: Codex

### 本轮改动

- 未启动新的 subagents，因此没有闲置 agent 需要关闭。
- 新增 `scripts/check-deny-exceptions.ps1`，扫描 `deny.toml` 中 `RUSTSEC-*` advisory ignore 项，强制每条例外包含 `owner:`、`expires: YYYY-MM-DD` 和说明文本，并在 expiry 早于当前日期时失败。
- 将 dependency exception guard 接入 `.github/workflows/ci-backend.yml`，位置在 release guard 之后、`cargo deny` 之前；这样到期例外会先以清晰错误失败，而不是沉积在文档中。
- 更新 `docs/remediation-plan-v2.1.md`，把该守卫纳入 Phase 5.2 dependency gate 的已完成证据。

### 验证记录

- `powershell -ExecutionPolicy Bypass -File scripts\check-deny-exceptions.ps1`: 通过，18 条 advisory exceptions checked。
- `powershell -ExecutionPolicy Bypass -File scripts\check-release-guard.ps1`: 通过。
- `cargo fmt --all -- --check`: 通过。
- `cargo clippy --workspace --all-targets -- -D warnings`: 通过。
- `git diff --check`: 通过。

### 剩余风险

- 该脚本只约束 advisory ignore 元数据和到期时间，不替代 `cargo deny` / `cargo audit`。
- 已存在的 upstream/transitive advisory 仍需按 expiry 前升级、替换或重新评审。

---

## 2026-06-02 02:58:33 +08:00 - Settings config closure

署名: Codex

### 本轮改动

- 未启动新的 subagents，因此没有闲置 agent 需要关闭。
- 复核 Settings 实现：路径类设置已通过 `get_app_settings` / `save_app_settings` 后端 config 持久化并校验；theme 与 dev event trace 也写入后端 DTO，同时镜像到 localStorage 以支持 mock/dev 即时生效。
- 给 Settings 页面案件目录和镜像搜索路径输入补充显式 `label` / `htmlFor`，提升可访问性并让 render tests 能按标签查询。
- 新增 `frontend/src/app/pages/Settings.test.tsx`，覆盖后端配置加载、保存 API payload、localStorage mirror、主题应用、mock/local fallback 和非法镜像路径拒绝。
- 更新 `docs/remediation-plan-v2.1.md`，将 Phase 3.8 Settings editable config 从 Partial 收口为 Completed，并记录验证命令。

### 验证记录

- `pnpm --dir frontend test -- Settings`: 通过，3 files / 11 tests。
- `pnpm --dir frontend typecheck`: 通过。
- `pnpm --dir frontend lint`: 通过，0 error / 7 existing warnings。
- `git diff --check`: 通过。

### 剩余风险

- Settings 的 theme/dev event trace 仍保留 localStorage mirror，这是 mock/dev 即时生效的有意设计，不影响后端 config 作为路径类设置契约源。

---

## 2026-06-02 03:05:16 +08:00 - Event bridge closure

署名: Codex

### 本轮改动

- 未启动新的 subagents，因此没有闲置 agent 需要关闭。
- 新增 transport typed topic：`data-source-imported` 与 `job-cancelled`，并同步 `EventTopic::as_str()` serde 测试。
- `event_bridge` 新增 `emit_data_source_imported()` 与 `emit_job_cancelled()` helper。`data-source-imported` payload 只包含 `dataSourceId`、`name`、`kind`、`jobId`，不包含宿主证据 `sourcePath`。
- `cancel_import` 成功请求取消时发 `job-cancelled`；import finalizing 阶段查询 data source summary 并发 `data-source-imported`。
- 前端 `EventTopic` union 和 Tauri bridge 监听列表同步新增 topic；新增 EventBus 测试覆盖两个 topic。
- 更新 `docs/remediation-plan-v2.1.md`，将 Phase 4.2 Event bridge 扩展从 Partial 收口为 Completed。

### 验证记录

- `cargo test -p transport events`: 通过。
- `cargo clippy -p forensics-desktop --all-targets -- -D warnings`: 通过。
- `pnpm --dir frontend test -- events`: 通过，1 file / 1 test。
- `pnpm --dir frontend typecheck`: 通过。
- `cargo fmt --all -- --check`: 通过。

### 剩余风险

- `job-cancelled` 作为事件发出；现有 job snapshot 仍用既有 status/detail 表达最终状态，未在本轮引入新的 cancelled job status 迁移。

---

## 2026-06-02 03:10:13 +08:00 - Command SQL boundary guard

署名: Codex

### 本轮改动

- 未启动新的 subagents，因此没有闲置 agent 需要关闭。
- 复核 `apps/desktop/src-tauri/src/commands`，当前无原始 SQL 语句或 `rusqlite::params!/prepare/execute` 低层调用；command layer 仍允许打开 case DB 连接并调用 repository/service/helper。
- 新增 `scripts/check-command-sql-boundary.ps1`，扫描 Tauri command 层并拦截显式 SQL 关键字和底层 rusqlite SQL API，防止后续把业务 SQL 写回 command。
- 将 command SQL boundary guard 接入 `.github/workflows/ci-backend.yml`。
- 更新 `docs/remediation-plan-v2.1.md`，将 Phase 6.2 SQL 下沉 repo 从 Partial 收口为 Completed / guarded。

### 验证记录

- `powershell -ExecutionPolicy Bypass -File scripts\check-command-sql-boundary.ps1`: 通过。
- `powershell -ExecutionPolicy Bypass -File scripts\check-release-guard.ps1`: 通过。
- `powershell -ExecutionPolicy Bypass -File scripts\check-deny-exceptions.ps1`: 通过。
- `cargo fmt --all -- --check`: 通过。
- `git diff --check`: 通过。

### 剩余风险

- Guard 只约束 Tauri command 层不写业务 SQL；repository/service 层仍需依赖现有单元/集成测试保证 SQL 行为正确。

---

## 2026-06-02 03:19:24 +08:00 - Beta final gate and review closeout

署名: Codex

### 本轮改动

- 未启动新的 subagents，因此没有闲置 agent 需要关闭。
- 复核当前分支 `codex/beta-forensics-backlog`，HEAD 为 `af2833c ci: guard command sql boundary`；工作树仅保留用户侧未跟踪 `.context/plans.md`。
- 按 AGENTS 要求重新读取 `docs/remediation-plan-v2.1.md`、`docs/architecture-model.md`、`docs/algorithm-complexity-analysis.md`、本开发记录和 `development-reports/sessions/2026-06-02.md`。
- 补跑可信 beta final gate，并更新 `docs/remediation-plan-v2.1.md` 中的最终测试计数和剩余风险表述。
- 统一 grep 审阅伪取证事实、direct Tauri invoke、host path 泄露和 evidence 直读回退：命中均为审计文档、负向测试、时间戳单测注释或既有导入内部路径处理；页面/feature 的 Tauri invoke 只存在于统一 `apiClient`。

### 验证记录

- `cargo fmt --all -- --check`: 通过。
- `cargo clippy --workspace --all-targets -- -D warnings`: 通过。
- `cargo test --workspace`: 通过；真实 E01 tests 默认 ignored，需要 `FORENSICS_E01_FIXTURE` 手动运行。
- `cargo build -p forensics-desktop`: 通过。
- `cargo doc --workspace --no-deps`: 通过。
- `powershell -ExecutionPolicy Bypass -File scripts\check-release-guard.ps1`: 通过。
- `powershell -ExecutionPolicy Bypass -File scripts\check-command-sql-boundary.ps1`: 通过。
- `powershell -ExecutionPolicy Bypass -File scripts\check-deny-exceptions.ps1`: 通过，18 条 advisory exceptions checked。
- `cargo deny check advisories bans licenses sources`: 通过；仍有 duplicate dependency warnings。
- `cargo audit`: 通过，退出码 0；报告 20 个 allowed warning-class advisories。
- `pnpm --dir frontend typecheck`: 通过。
- `pnpm --dir frontend lint`: 通过，0 error / 7 existing warnings。
- `pnpm --dir frontend test`: 通过，15 files / 58 tests；ErrorBoundary 用例打印预期 jsdom stack。
- `pnpm --dir frontend build`: 通过；Vite 主 chunk 约 520KB，仍提示 chunk size warning。
- `pnpm --dir frontend audit --audit-level high`: 通过，无已知漏洞。
- `git diff --check`: 通过。

### 剩余风险

- EVTX adapter 仍缺合法 tiny real `.evtx` fixture；当前覆盖 JSON extraction helper、truncated magic、malformed 和 oversized path。
- Registry parser 是 Analysis 定向字段 parser，不是完整 hive browser。
- `evidence-media://` protocol 尚未做真实 Windows WebView2 桌面 seek smoke。
- `evtx -> encoding` advisory 例外需在 2026-09-01 前升级、替换 parser、vendor/patch 或重新评审。
- tiny E01 fixture 尚未入库；真实样本慢测仍依赖 `FORENSICS_E01_FIXTURE`。

---

## 2026-06-02 03:26:01 +08:00 - FS reader helper dedupe

署名: Codex

### 本轮改动

- 未启动新的 subagents，因此没有闲置 agent 需要关闭。
- 复核当前分支 `codex/beta-forensics-backlog`，HEAD 为 `ad4ccd2 docs: record beta final gate`；工作树仅保留用户侧未跟踪 `.context/plans.md`。
- 从剩余 backlog 中选择本地可验证的 Phase 6.1 子项，避免依赖真实 EVTX fixture、私有 E01 或桌面人工 smoke。
- 在 `evidence-core::filesystem` 抽出 `root_node`、`join_child_path` / `join_child_path_with_separator`、`path_not_found`、`file_not_found`、`path_is_directory`、`path_is_not_directory` 等公共 helper。
- `fs-fat`、`fs-ntfs`、`fs-exfat` 复用公共 helper，去除重复 root node 构造和路径拼接/常见错误构造。exFAT 保持反斜杠 path convention，FAT/NTFS 保持 slash convention，避免改变既有行为。
- 更新 `docs/remediation-plan-v2.1.md`，将 Phase 6.1 从 Remaining 标为 Partial：公共 root/path/error helper 已落地，更深的枚举流程和 FS 特定错误转换去重仍留后续。

### 验证记录

- `cargo fmt --all -- --check`: 通过。
- `cargo test -p evidence-core filesystem`: 通过。
- `cargo test -p fs-fat`: 通过。
- `cargo test -p fs-exfat`: 通过。
- `cargo test -p fs-ntfs`: 通过。
- `cargo test -p app-services file_service`: 通过。
- `cargo test -p app-services --test file_service_real_test`: 通过。
- `cargo clippy --workspace --all-targets -- -D warnings`: 通过。

### 剩余风险

- 本轮只抽取 FS reader 的公共 root/path/error helper，不重构 FAT/NTFS/exFAT 深层枚举流程或错误类型体系。
- 进一步去重需谨慎保持 public API、排序、root semantics、path separator convention 和 reader behavior 不变。

---

## 2026-06-02 10:23:41 +08:00 - Frontend lint warning cleanup

署名: Codex

### 本轮改动

- 未启动新的 subagents，因此没有闲置 agent 需要关闭。
- 保留用户侧未跟踪 `.context/plans.md`，未 stage、未删除。
- 清理前端既有 7 个 lint warning：
  - 移除 `McpServerItem.tsx` 未使用的 `Settings` icon import。
  - 将 `McpToolList.tsx` tool call result `data` 从 `any` 收紧为 `unknown`，并在渲染前显式 stringify。
  - 将 `DenseDataTable.tsx` memo row props 改为泛型类型，移除 `any`。
  - 移除 `SyntaxHighlighter.tsx` 未使用的 `lineRenderer` prop。
  - 移除 `tauri-bridge.ts` 未使用的 `JobSnapshot` type import。
- 更新 `docs/remediation-plan-v2.1.md`，将当前 frontend lint 状态更新为 `0 error / 0 warnings`。

### 验证记录

- `pnpm --dir frontend lint`: 通过，0 error / 0 warnings。
- `pnpm --dir frontend typecheck`: 通过。
- `pnpm --dir frontend test`: 通过，15 个测试文件 / 58 个测试；ErrorBoundary 用例打印预期 jsdom stack。
- `pnpm --dir frontend build`: 通过；仍保留既有 Vite chunk size warning，主 chunk 约 520KB。

### 剩余风险

- 本轮只清理 lint warning，不处理 Vite chunk size warning；后续可通过 route-level dynamic import 或 manualChunks 单独收口。

---

## 2026-06-02 10:27:03 +08:00 - Frontend route-level code splitting

署名: Codex

### 本轮改动

- 未启动新的 subagents，因此没有闲置 agent 需要关闭。
- 保留用户侧未跟踪 `.context/plans.md`，未 stage、未删除。
- 将 `frontend/src/app/routes.tsx` 的页面级静态导入改为 React Router `lazy` 动态导入。
- 保留 `Layout` 静态导入，确保 app shell 首屏结构不变；各页面按 route 拆包加载。
- 更新 `docs/remediation-plan-v2.1.md`，把 frontend build 状态更新为无 chunk size warning，主 chunk 约 315KB。

### 验证记录

- `pnpm --dir frontend typecheck`: 通过。
- `pnpm --dir frontend lint`: 通过，0 error / 0 warnings。
- `pnpm --dir frontend test`: 通过，15 个测试文件 / 58 个测试；ErrorBoundary 用例打印预期 jsdom stack。
- `pnpm --dir frontend build`: 通过；无 Vite chunk size warning，主 chunk 约 315KB，页面 chunk 独立输出。

### 剩余风险

- 本轮只做 route-level code splitting；未引入 manualChunks。若后续依赖体积继续增长，可再对 vendor/common chunk 做专项拆分。

---

## 2026-06-02 10:30:21 +08:00 - Route splitting regression guard

署名: Codex

### 本轮改动

- 未启动新的 subagents，因此没有闲置 agent 需要关闭。
- 保留用户侧未跟踪 `.context/plans.md`，未 stage、未删除。
- 为 `frontend/src/app/routes.tsx` 导出 `appRoutes`，并新增 `frontend/src/app/routes.test.ts`。
- 测试断言 root `Layout` 保持静态加载，所有页面 child routes 使用 `lazy`，防止后续把页面静态 import 回来导致主 chunk 膨胀。
- `ui-store` 的 `localStorage` 读写改为安全 helper，避免测试、SSR 或受限浏览器环境缺少 storage API 时模块初始化崩溃；正常浏览器持久化行为不变。
- 更新 `docs/remediation-plan-v2.1.md`，将当前 frontend test 计数更新为 16 files / 59 tests。

### 验证记录

- `pnpm --dir frontend test -- routes`: 通过，1 个测试文件 / 1 个测试。
- `pnpm --dir frontend test`: 通过，16 个测试文件 / 59 个测试；ErrorBoundary 用例打印预期 jsdom stack。
- `pnpm --dir frontend typecheck`: 通过。
- `pnpm --dir frontend lint`: 通过，0 error / 0 warnings。
- `pnpm --dir frontend build`: 通过；无 Vite chunk size warning，主 chunk 约 315KB。

### 剩余风险

- Vitest 单文件 route 测试仍会打印 Node 的 `--localstorage-file` warning，但退出码为 0，且 `ui-store` 已对 storage API 不可用场景容错。

---

## 2026-06-02 10:39:22 +08:00 - Tiny E01 reader fixture

署名: Codex

### 本轮改动

- 未启动新的 subagents，因此没有闲置 agent 需要关闭。
- 保留用户侧未跟踪 `.context/plans.md`，未 stage、未删除。
- 在 `scripts/generate-tiny-fixtures.ps1` 中增加 deterministic synthetic single-segment E01 fixture 生成逻辑，生成 `testdata/fixtures/tiny/e01/tiny.E01`，大小 4405 bytes。
- `crates/testing::fixtures` 新增 `tiny_e01_image()` helper，并补 fixture 存在性/header/marker 单测。
- `crates/image-e01` 增加 `testing` dev-dependency，默认 regression tests 打开 committed tiny E01，验证 reader kind/size、开头 marker、MBR signature 位置、seek-to-end 和 EOF 行为。
- 更新 `testdata/fixtures/tiny/README.md`、`docs/remediation-plan-v2.1.md`、`docs/architecture-model.md` 和 `docs/algorithm-complexity-analysis.md`，明确 tiny E01 只覆盖 reader section/table/read/seek，不是完整文件系统镜像；真实 E01 慢测仍需 `FORENSICS_E01_FIXTURE`。

### 验证记录

- `powershell -ExecutionPolicy Bypass -File scripts\generate-tiny-fixtures.ps1`: 通过，生成 RAW 1024 bytes 和 E01 4405 bytes。
- `cargo test -p image-e01`: 通过；6 个 lib tests，4 个默认 regression tests，7 个 ignored real-E01 dump/slow tests。
- `cargo test -p testing`: 通过，4 个 fixture helper tests。
- `cargo fmt --all -- --check`: 通过。
- `cargo clippy -p image-e01 --all-targets -- -D warnings`: 通过。

### 剩余风险

- Tiny E01 不包含真实 partition/filesystem；`app-services` 的真实 E01 probe/import/full pipeline 慢测仍依赖 `FORENSICS_E01_FIXTURE`。
- EVTX 仍缺合法 tiny real `.evtx` fixture。

---

## 2026-06-02 10:56:14 +08:00 - Coverage reporting gate

署名: Codex

### 本轮改动

- 未启动新的 subagents，因此没有闲置 agent 需要关闭。
- 保留用户侧未跟踪 `.context/plans.md`，未 stage、未删除。
- 继续从 `e3692f2 test: add tiny e01 fixture` 基线推进剩余 backlog，选择不依赖私有样本或人工 WebView2 smoke 的 Phase 5.6 coverage 子项。
- 前端新增 `@vitest/coverage-v8` devDependency 和 `pnpm --dir frontend test:coverage` 脚本，Vitest coverage 输出 `text`、`json-summary`、`lcov` 到 `frontend/coverage`。
- 新增 `scripts/run-coverage.ps1`，统一执行前端 coverage；Rust coverage 在安装 `cargo-llvm-cov` 时生成 `coverage/rust-lcov.info`，未安装时默认 warning 跳过，避免本地默认门禁被工具缺失阻塞。
- 后端 CI 新增 `llvm-tools-preview`、`cargo-llvm-cov`、`backend-coverage` artifact；前端 CI 新增 `frontend-coverage` artifact。当前只上传报告，不设置百分比阈值。
- 更新 `.gitignore`、`scripts/README.md`、`docs/remediation-plan-v2.1.md`、`docs/architecture-model.md`、`docs/algorithm-complexity-analysis.md`，明确 coverage 已有执行入口但阈值仍需稳定 baseline 后再定。

### 验证记录

- `pnpm --dir frontend test:coverage`: 通过；16 个测试文件 / 59 个测试，生成 `frontend/coverage/coverage-summary.json`、`lcov.info`，当前总行覆盖率约 47.97%。
- `powershell -ExecutionPolicy Bypass -File scripts\run-coverage.ps1 -Frontend`: 通过；统一 runner 的前端 coverage 路径可用。
- `pnpm --dir frontend typecheck`: 通过。
- `pnpm --dir frontend lint`: 通过，0 error / 0 warnings。
- `pnpm --dir frontend test`: 通过；16 个测试文件 / 59 个测试，ErrorBoundary 用例打印预期 jsdom stack，退出码为 0。
- `pnpm --dir frontend build`: 通过；无 Vite chunk size warning，主 chunk 约 315KB。
- `pnpm --dir frontend audit --audit-level high`: 通过，无已知漏洞。
- `pnpm --dir frontend install --frozen-lockfile`: 通过。
- `powershell -ExecutionPolicy Bypass -File scripts\run-coverage.ps1 -Rust`: 通过；本机未安装 `cargo-llvm-cov`，按设计 warning 后跳过 Rust coverage。
- `cargo fmt --all -- --check`: 通过。
- `git diff --check`: 通过。

### 剩余风险

- Coverage 当前是报告型 artifact，不是硬阈值 gate；后续需要在报告稳定后决定 backend/frontend 阈值。
- Rust coverage CI 会安装 `cargo-llvm-cov` 并运行全 workspace coverage，CI 时长会增加；本地脚本默认不强制要求该工具。
- EVTX 合法 tiny real `.evtx` fixture、Windows WebView2 media smoke、`evtx -> encoding` advisory cleanup 仍是后续项。

---

## 2026-06-02 11:06:05 +08:00 - Frontend coverage baseline threshold

署名: Codex

### 本轮改动

- 未启动新的 subagents，因此没有闲置 agent 需要关闭。
- 保留用户侧未跟踪 `.context/plans.md`，未 stage、未删除。
- 基于上一轮 `frontend/coverage/coverage-summary.json` 的当前前端 baseline：lines 47.97%、statements 47.69%、functions 49.10%、branches 38.32%。
- 在 `frontend/vitest.config.ts` 增加初始全局 coverage thresholds：lines/statements/functions 45%，branches 35%。该阈值低于当前 baseline，用于防止明显回退；不是最终质量目标。
- 前端 CI 已调用 `pnpm --dir frontend test:coverage`，因此该 threshold 自动成为 frontend coverage gate；backend coverage 仍只生成 LCOV artifact。
- 更新 `scripts/README.md`、`docs/remediation-plan-v2.1.md`、`docs/architecture-model.md`、`docs/algorithm-complexity-analysis.md`，说明 frontend baseline gate 与 backend artifact-only 的差异。

### 验证记录

- `pnpm --dir frontend test:coverage`: 通过；16 个测试文件 / 59 个测试，coverage 为 lines 47.97%、statements 47.69%、functions 49.10%、branches 38.32%，高于新阈值。
- `pnpm --dir frontend typecheck`: 通过。
- `pnpm --dir frontend lint`: 通过，0 error / 0 warnings。
- `pnpm --dir frontend test`: 通过；16 个测试文件 / 59 个测试，ErrorBoundary 用例打印预期 jsdom stack，退出码为 0。
- `pnpm --dir frontend build`: 通过；无 Vite chunk size warning，主 chunk约 315KB。
- `git diff --check`: 通过。

### 剩余风险

- 前端 coverage 阈值是初始防回退阈值，后续应随测试补强逐步提高。
- 后端 coverage 仍没有百分比阈值，只上传 LCOV artifact。
- EVTX 合法 tiny real `.evtx` fixture、Windows WebView2 media smoke、`evtx -> encoding` advisory cleanup 仍未完成。

---

## 2026-06-02 11:15:09 +08:00 - FS path component helper dedupe

署名: Codex

### 本轮改动

- 未启动新的 subagents，因此没有闲置 agent 需要关闭。
- 保留用户侧未跟踪 `.context/plans.md`，未 stage、未删除。
- 继续推进 Phase 6.1 FS enum 去重，选择行为中立的重复逻辑：FAT、exFAT、NTFS reader 中的 path component normalization。
- 在 `evidence-core::filesystem` 新增 `path_components(path: &str) -> Vec<&str>`，统一处理 slash/backslash、首尾分隔符和空 component。
- `fs-fat`、`fs-exfat`、`fs-ntfs` 改为复用该 helper；不改变 public API、排序、root semantics、path separator convention 或 reader behavior。
- 更新 `docs/remediation-plan-v2.1.md`、`docs/architecture-model.md`、`docs/algorithm-complexity-analysis.md`，记录当前 FS helper 去重状态仍为 Partial，深层枚举流程和 FS 特定错误转换后续再做。

### 验证记录

- `cargo test -p evidence-core filesystem`: 通过。
- `cargo test -p fs-fat`: 通过。
- `cargo test -p fs-exfat`: 通过。
- `cargo test -p fs-ntfs`: 通过。
- `cargo test -p app-services file_service`: 通过。
- `cargo test -p app-services --test file_service_real_test`: 通过。
- `cargo clippy -p evidence-core -p fs-fat -p fs-exfat -p fs-ntfs --all-targets -- -D warnings`: 通过。
- `cargo fmt --all -- --check`: 通过。

### 剩余风险

- 本轮只抽出 path component normalization helper，不重构 FAT/NTFS/exFAT 深层枚举流程或错误类型体系。
- 进一步去重仍需保持 public API、排序、root semantics、path separator convention 和 reader behavior 不变。

---

## 2026-06-02 11:28:11 +08:00 - EVTX real fixture regression

署名: Codex

### 本轮改动

- 未启动新的 subagents，因此没有闲置 agent 需要关闭。
- 保留用户侧未跟踪 `.context/plans.md`，未 stage、未删除。
- 从剩余 backlog 中选择 EVTX fixture gap：上游 `evtx 0.11.2` crates.io package 排除了 `*.evtx` fixture，但上游 GitHub repository 仍有 MIT/Apache-2.0 licensed samples。
- 新增 `testdata/fixtures/tiny/evtx/system.evtx`，来源为 `https://github.com/omerbenamram/EVTX` commit `38a2d50b21629edb3dd77953a2c02a4b944badf1` 的 `samples/system.evtx`，SHA-256 为 `60e5fd8f10a3deecd58bd7bee2e03b1308d190fd13b52908be9d576c7f5d6623`，大小 1,118,208 bytes。
- 新增 `testdata/fixtures/tiny/evtx/README.md` 记录 fixture provenance、license、hash 和大小；更新 tiny fixture 总 README。
- `crates/testing::fixtures` 新增 `tiny_system_evtx()` helper 和 header/size 单测。
- `artifacts-windows` dev-dependency 引入 workspace `testing`，EVTX parser 新增 real `System.evtx` parser-path regression，断言能解析出 6005/6006/6008/1074 boot/shutdown candidate，并验证 timestamp/source/note 非空。
- 更新 `docs/remediation-plan-v2.1.md`、`docs/architecture-model.md`、`docs/algorithm-complexity-analysis.md`，把 EVTX fixture gap 改为已覆盖真实 parser path；`evtx -> encoding` advisory 仍保留为到期例外风险，不声明已解决。

### 验证记录

- `cargo test -p artifacts-windows evtx`: 通过，7 个 EVTX targeted tests。
- `cargo test -p testing tiny_system_evtx`: 通过。
- `cargo test -p artifacts-windows`: 通过，24 个 lib tests、9 个 parser tests，4 个 real fixture tests ignored。
- `cargo test -p testing`: 通过，5 个 fixture helper tests。
- `cargo fmt --all -- --check`: 通过。
- `cargo clippy -p artifacts-windows -p testing --all-targets -- -D warnings`: 通过。
- `powershell -ExecutionPolicy Bypass -File scripts\check-deny-exceptions.ps1`: 通过，18 条 advisory exceptions checked。
- `cargo deny check advisories bans licenses sources`: 通过；仍有既有 duplicate dependency warnings。
- `cargo audit`: 通过，退出码 0；仍报告 20 个 warning-class advisories，包括 `evtx -> encoding`，继续由到期例外跟踪。

### 剩余风险

- `evtx -> encoding` unmaintained advisory 仍是 `deny.toml` 到期例外，需在 2026-09-01 前升级、替换 parser、vendor/patch 或重新评审。
- 新增 fixture 覆盖 System.evtx parser path，不代表完整 EVTX 平台或所有 Windows event log 变体。
- Windows WebView2 `evidence-media://` 播放/seek smoke 仍未执行。

---

## 2026-06-02 11:38:10 +08:00 - Media protocol CI guard

署名: Codex

### 本轮改动

- 未启动新的 subagents，因此没有闲置 agent 需要关闭。
- 保留用户侧未跟踪 `.context/plans.md`，未 stage、未删除。
- 复核 `evidence-media` 剩余项后确认：真实 Windows WebView2 `<video>/<audio>` 播放/seek 需要桌面壳、案件和媒体样本，不适合替代为无头 CI。
- 新增 `scripts/check-media-protocol-guard.ps1`，固定 CI 可验证的 media 安全边界：
  - `tauri.conf.json` 的 `media-src` 必须允许 `evidence-media:`。
  - Tauri shell 必须注册 `media_protocol`。
  - `media_protocol.rs` 必须保留 asynchronous URI scheme、bounded range parser、206/416 行为和 `MAX_MEDIA_PROTOCOL_READ_BYTES`。
  - `file_commands.rs` 必须保留 protocol mode、`media_protocol_url()` 和 `read_media_range` fallback。
  - frontend API/hook 必须继续走 `get_media_url`、`read_media_range`、protocol mode 和 range fallback。
  - 扫描 desktop/frontend viewer 相关代码，拒绝 `asset://localhost` 与 `convertFileSrc` 回退。
- 后端 CI 在 release guard 后新增 Media protocol guard step。
- 更新 `scripts/README.md`、`docs/remediation-plan-v2.1.md`、`docs/architecture-model.md`、`docs/algorithm-complexity-analysis.md`，明确该 guard 不替代真实 WebView2 seek smoke。

### 验证记录

- `powershell -ExecutionPolicy Bypass -File scripts\check-media-protocol-guard.ps1`: 通过。
- `cargo test -p forensics-desktop media_protocol`: 通过，10 个 protocol/range tests。
- `cargo test -p forensics-desktop commands::file_commands::tests::`: 通过，7 个 file command tests，覆盖 protocol media URL、range clamp、invalid handle、EOF、host path 不泄露和 extract file。
- `powershell -ExecutionPolicy Bypass -File scripts\check-release-guard.ps1`: 通过。
- `git diff --check`: 通过。

### 剩余风险

- 仍需人工或未来专用 harness 验证 Windows WebView2 中 `<video>/<audio>` 对 `evidence-media://` 的连续播放和 seek 行为。

---

## 2026-06-02 11:44:15 +08:00 - EVTX dependency decision guard

署名: Codex

### 本轮改动

- 未启动新的 subagents，因此没有闲置 agent 需要关闭。
- 保留用户侧未跟踪 `.context/plans.md`，未 stage、未删除。
- 复核 `evtx -> encoding` advisory cleanup 可行性：
  - `cargo search evtx` / `cargo info evtx` 显示 crates.io 最新仍是 `evtx 0.11.2`。
  - 上游 `EVTX` commit `38a2d50b21629edb3dd77953a2c02a4b944badf1` 仍为 `version = "0.11.2"` 且直接依赖 `encoding = "0.2.33"`。
  - 本地 `evtx` 源码中 `encoding::EncodingRef`、`WINDOWS_1252`、`DecoderTrap` 分布于 parser/binxml/render 路径，移除不是 feature trimming 或一行 patch。
  - `evtx-msg 1.0.1` 为 GPL-3.0，`exhume_artefacts 0.2.6` 为 GPL-2.0-or-later，不适合当前 MIT workspace 作为直接依赖；`rsigma 0.13.0` 是更大的 Sigma/runtime 工具链，不是窄 EVTX parser 替代。
- 新增 `docs/evtx-dependency-decision.md`，记录当前保留 `evtx 0.11.2` 的范围、风险边界、证据和 2026-09-01 前必须执行的替换/patch/复审选项。
- 新增 `scripts/check-evtx-dependency-decision.ps1`，检查 `deny.toml` 的 `RUSTSEC-2021-0153` 例外仍与决策文档同步，避免 dependency exception 和技术决策分叉。
- 后端 CI 在 dependency exception guard 后新增 EVTX dependency decision guard。
- 更新 `scripts/README.md`、`docs/remediation-plan-v2.1.md`、`docs/architecture-model.md`、`docs/algorithm-complexity-analysis.md`。

### 验证记录

- `powershell -ExecutionPolicy Bypass -File scripts\check-evtx-dependency-decision.ps1`: 通过。
- `powershell -ExecutionPolicy Bypass -File scripts\check-deny-exceptions.ps1`: 通过。
- `cargo deny check advisories bans licenses sources`: 通过；仍报告既有 duplicate dependency warnings。
- `cargo audit`: 通过，退出码 0；仍报告 20 个 warning-class transitive advisories，其中包含 `evtx -> encoding` 链路。
- `git diff --check`: 通过。

### 剩余风险

- `evtx -> encoding` advisory 本身仍未消除；本轮只是把不可安全替换的原因和到期处理要求变成可审计、可 CI 检查的决策记录。

---

## 2026-06-02 11:52:50 +08:00 - Beta backlog final gate refresh

署名: Codex

### 本轮改动

- 未启动新的 subagents，因此没有闲置 agent 需要关闭。
- 保留用户侧未跟踪 `.context/plans.md`，未 stage、未删除。
- 在 `1d25ba2 test: guard evtx dependency decision` 后补跑可信 beta 默认门禁，确认最近 EVTX dependency decision guard、media guard 与 fixture/coverage 相关改动没有破坏基线。
- 执行最终 grep 审阅：
  - 伪取证事实命中只存在于负向测试、旧审计文档、时间戳注释或 Cargo checksum；业务代码/UI 未输出 `FORENSICS-PC`、默认 `Windows 10`、默认 `19045` 或固定 boot timestamp。
  - `frontend/src/app/pages` 和 `frontend/src/features` 无直接 Tauri `invoke`。
  - host path 字符串命中集中在 Settings 默认路径/测试、artifact parser 示例路径和防泄露断言；media/viewer 相关代码未回退到 `asset://localhost` 或 `convertFileSrc`。

### 验证记录

- `cargo fmt --all -- --check`: 通过。
- `cargo clippy --workspace --all-targets -- -D warnings`: 通过。
- `cargo test --workspace`: 通过；真实 E01 慢测按设计 ignored，需要 `FORENSICS_E01_FIXTURE` opt-in。
- `cargo build -p forensics-desktop`: 通过。
- `cargo doc --workspace --no-deps`: 通过。
- `powershell -ExecutionPolicy Bypass -File scripts\check-release-guard.ps1`: 通过。
- `powershell -ExecutionPolicy Bypass -File scripts\check-media-protocol-guard.ps1`: 通过。
- `powershell -ExecutionPolicy Bypass -File scripts\check-evtx-dependency-decision.ps1`: 通过。
- `cargo deny check advisories bans licenses sources`: 通过；仍报告既有 duplicate dependency warnings。
- `cargo audit`: 通过，退出码 0；仍报告 20 个 warning-class transitive advisories，包含 `evtx -> encoding` 链路。
- `pnpm --dir frontend typecheck`: 通过。
- `pnpm --dir frontend lint`: 通过。
- `pnpm --dir frontend test`: 通过，16 个 test files / 59 个 tests；ErrorBoundary 用例打印预期 jsdom stack，退出码为 0。
- `pnpm --dir frontend build`: 通过，主 chunk 约 315KB，无 Vite chunk size warning。
- `pnpm --dir frontend test:coverage`: 通过，16 个 test files / 59 个 tests；coverage 为 statements 47.69%、branches 38.32%、functions 49.10%、lines 47.97%，高于当前阈值。
- `pnpm --dir frontend audit --audit-level high`: 通过，No known vulnerabilities found。

### 剩余风险

- Windows WebView2 中 `<video>/<audio>` 对 `evidence-media://` 的真实播放和 seek 仍需桌面 manual smoke 或专用 harness。
- `evtx -> encoding` advisory 仍是到期例外，需在 2026-09-01 前升级、替换 parser、vendor/patch 或重新评审。
- Registry parser 仍是 Analysis 定向字段 parser，不是完整 hive browser；FS enum 更深层流程/错误转换去重仍是后续质量项。

---

## 2026-06-02 11:59:26 +08:00 - Registry value-list cell hardening

署名: Codex

### 本轮改动

- 未启动新的 subagents，因此没有闲置 agent 需要关闭。
- 保留用户侧未跟踪 `.context/plans.md`，未 stage、未删除。
- 修复 `artifacts-windows::registry::lookup` 的真实 hive value-list 解析：NK 的 `values_list_offset` 现在按 registry cell 读取，先校验负 cell size 和 cell bounds，再从 cell payload 读取 value offsets；不再把 value-list 当作裸 u32 offset 数组。
- 强化 value data 边界：
  - inline data 长度超过 4 bytes 时返回错误，不再静默截断。
  - `REG_DWORD` / `REG_QWORD` 外部数据长度不足时返回错误，不再静默补 0。
- 同步 app-services Analysis 测试 fixture builder，使 synthetic SYSTEM/SOFTWARE hive 使用带 cell header 的 value-list 布局，继续覆盖 Analysis registry provenance flow。
- 补充 `lh` / `li` / `ri` 子键列表导航回归测试，确保文档声称支持的 list 变体都能从 root 导航到目标 key 并读取 value。
- 本轮仍是 Registry targeted parser 增量，不是完整 hive browser。

### 验证记录

- `cargo test -p artifacts-windows registry::lookup`: 通过，16 个 targeted tests，新增 value-list cell header、truncated value-list、inline overlong、short DWORD、`lh/li/ri` subkey list 回归。
- `cargo test -p app-services --lib analysis_service`: 通过，9 个 Analysis targeted tests。
- `cargo fmt --all`: 已执行。

### 剩余风险

- Registry parser 仍只承诺 Analysis 所需字段；完整 hive traversal、真实 registry fixture 覆盖和更复杂 cell graph 仍需继续扩展。
- `evtx -> encoding` advisory 和 WebView2 media manual smoke 仍是独立剩余项。

---

## 2026-06-02 15:38:12 +08:00 - Registry typed value coverage

署名: Codex

### 本轮改动

- 未启动新的 subagents，因此没有闲置 agent 需要关闭。
- 保留用户侧未跟踪 `.context/plans.md`，未 stage、未删除。
- 继续深化 `artifacts-windows::registry::lookup` targeted parser 覆盖：
  - 补充非 compressed UTF-16 NK 名称测试。
  - 补充 `REG_EXPAND_SZ`、`REG_MULTI_SZ`、`REG_QWORD` value parsing 回归。
  - 修正 `REG_MULTI_SZ` 解码路径：multi-string 现在保留完整 UTF-16 字符串并按 NUL 分割，不再在第一个 NUL 截断。
  - 奇数长度 UTF-16 registry string data 现在返回 parse error，不再静默丢弃尾字节。
- 本轮仍只承诺 Analysis 所需 Registry targeted parser，不是完整 hive browser。

### 验证记录

- `cargo test -p artifacts-windows registry::lookup`: 通过，21 个 targeted tests。
- `cargo test -p app-services --lib analysis_service`: 通过，9 个 Analysis targeted tests。

### 剩余风险

- Registry parser 仍缺真实 registry hive fixture 和更复杂 cell graph 覆盖；完整 hive traversal 仍需后续单独设计。
- `evtx -> encoding` advisory 和 WebView2 media manual smoke 仍是独立剩余项。

---

## 2026-06-02 15:52:16 +08:00 - FS node parent path helper dedupe

署名: Codex

### 本轮改动

- 未启动新的 subagents，因此没有闲置 agent 需要关闭。
- 保留用户侧未跟踪 `.context/plans.md`，未 stage、未删除。
- 继续推进 Phase 6.1 FS enum 去重中的低风险子项：
  - 在 `evidence-core::filesystem` 新增 `node_with_parent_path` 和 `node_with_parent_path_with_separator`。
  - FAT、NTFS 使用 slash-separated parent path helper。
  - exFAT 使用 backslash-preserving parent path helper。
  - 新增 evidence-core 单测确认 helper 会补齐 child path，同时保留 `FsNode` metadata。
- 本轮不重构 FAT/NTFS/exFAT 深层枚举流程、排序、root semantics 或 FS 特定错误体系。

### 验证记录

- `cargo test -p evidence-core filesystem`: 通过，7 个 targeted tests。
- `cargo test -p fs-fat`: 通过。
- `cargo test -p fs-exfat`: 通过。
- `cargo test -p fs-ntfs`: 通过。
- `cargo clippy -p evidence-core -p fs-fat -p fs-exfat -p fs-ntfs --all-targets -- -D warnings`: 通过。
- `cargo test -p app-services file_service`: 通过，10 个 filtered unit tests。
- `cargo test -p app-services --test file_service_real_test`: 通过，7 个 real file service tests。

### 剩余风险

- FS enum 去重仍是 Partial：更深层的目录枚举流程和 FS 特定错误转换还需要后续分轮处理，避免改变现有 reader 行为。
- WebView2 media manual smoke 与 `evtx -> encoding` advisory cleanup 仍是独立剩余项。

---

## 2026-06-02 16:21:41 +08:00 - EVTX local patched dependency cleanup

**署名**: Codex
**基线**: `5acdf8d docs: order beta backlog session entries`

### 本轮改动摘要

- 未启动新的 subagents，因此没有闲置 agent 需要关闭。
- 工作树仍保留用户侧未跟踪 `.context/plans.md`；本轮未 stage、未删除该文件。
- 新增 workspace member `crates/evtx-patched`，以本地 `evtx 0.11.2` fork 替代 crates.io `evtx` 依赖路径。
- local fork 保留库源码和 license/readme，移除 CLI/bench/sample 依赖路径；将 legacy `encoding = "0.2.33"` 替换为 `encoding_rs`，并用 `EncodingRef = &'static encoding_rs::Encoding` 保持 parser/binxml/render 接口稳定。
- 由于 crates.io package 排除了上游 sample EVTX 文件，local fork 禁用上游 sample-based internal tests 和 doctests；运行时行为由本仓 `artifacts-windows` EVTX targeted tests 和 tiny real `System.evtx` fixture 覆盖。
- 从 `deny.toml` 移除 `RUSTSEC-2021-0153` 例外，`scripts/check-evtx-dependency-decision.ps1` 改为 anti-regression guard：检查 workspace 使用 `crates/evtx-patched`、local fork 使用 `encoding_rs`、`Cargo.lock` 不含 `encoding`、决策文档记录 Resolved Decision。
- 更新 `docs/evtx-dependency-decision.md`、`docs/remediation-plan-v2.1.md`、`docs/architecture-model.md`、`docs/algorithm-complexity-analysis.md` 和 `scripts/README.md`，把 `evtx -> encoding` 从剩余 advisory cleanup 改为已通过 local patched fork 解决；仍保留 WebView2 media manual smoke、完整 Registry browser 和 FS 深层去重为剩余项。

### 验证记录

- `cargo check -p artifacts-windows`: 通过。
- `cargo test -p artifacts-windows evtx`: 通过，7 个 EVTX targeted tests。
- `cargo clippy -p evtx -p artifacts-windows --all-targets -- -D warnings`: 通过。
- `cargo test -p evtx`: 通过，10 个 local fork unit tests；doctests 因上游 sample 缺失在 manifest 中禁用。
- `cargo tree -p artifacts-windows -i encoding`: 未匹配 `encoding` package；仅提示相近 package `encoding_rs`。
- `cargo tree -p artifacts-windows -i evtx`: 指向 `evtx v0.11.2 (D:\forensics\crates\evtx-patched)`。
- `cargo tree -p artifacts-windows | Select-String -Pattern "encoding|evtx|encoding_rs"`: 显示 `evtx` local path 与 `encoding_rs v0.8.35`。
- `cargo fmt --all -- --check`: 通过。
- `cargo test --workspace`: 通过；真实 E01 slow tests ignored。
- `cargo clippy --workspace --all-targets -- -D warnings`: 通过。
- `git diff --check`: 通过。
- `powershell -ExecutionPolicy Bypass -File scripts\check-deny-exceptions.ps1`: 通过，17 条 advisory exceptions checked。
- `powershell -ExecutionPolicy Bypass -File scripts\check-evtx-dependency-decision.ps1`: 通过。
- `cargo audit`: 通过，退出码 0；仍报告 19 个 warning-class transitive advisories，`RUSTSEC-2021-0153` 已不在列表中。
- `cargo deny check advisories bans licenses sources`: 通过；仍报告 duplicate dependency warnings。

### 剩余风险

- local `evtx` fork 需要后续维护；若 upstream 发布 maintained permissive 版本，应优先替换回正常 dependency。
- EVTX adapter 仍只承诺 6005/6006/6008/1074 boot/shutdown candidate extraction，不是完整 EVTX parser 平台。
- WebView2 media manual smoke、完整 Registry browser 和 FS 深层枚举流程/错误转换去重仍是后续项。

---

## 2026-06-02 16:48:28 +08:00 - WebView2 media smoke harness

**署名**: Codex
**基线**: `197b0c3 fix: vendor evtx parser without encoding`

### 本轮改动摘要

- 未启动新的 subagents，因此没有闲置 agent 需要关闭。
- 新增 `scripts/run-webview2-media-smoke.ps1`，用于准备真实 Windows WebView2 播放/seek 手动 smoke：
  - 先运行 `scripts/check-media-protocol-guard.ps1`，确认 CSP/protocol/fallback/host-path guard 仍成立。
  - 在系统临时目录生成 logical evidence fixture：`small-inline.wav` 小于 inline threshold，`large-protocol.wav` 大于 20MB inline media threshold。
  - 输出 `WEBVIEW2_MEDIA_SMOKE.md` 清单，要求创建/打开 case、导入 generated logical evidence、验证小音频 inline preview、大音频 `evidence-media://handle/` protocol preview、WebView2 播放和 seek、以及不泄露宿主证据路径。
- 更新 `scripts/README.md`、`docs/remediation-plan-v2.1.md`、`docs/architecture-model.md` 和 `docs/algorithm-complexity-analysis.md`，把 media 剩余项从“manual smoke pending”细化为“smoke harness ready / manual result pending”。

### 验证记录

- `powershell -ExecutionPolicy Bypass -File scripts\run-webview2-media-smoke.ps1`: 通过。
  - `Media protocol guard passed`。
  - 生成 evidence directory: `C:\Users\QAQ\AppData\Local\Temp\forensics-webview2-media-smoke\logical-media-evidence`。
  - 生成 `small-inline.wav`，大小 88,244 bytes。
  - 生成 `large-protocol.wav`，大小 22,020,140 bytes，超过 20MB inline media threshold。
  - 生成 checklist: `C:\Users\QAQ\AppData\Local\Temp\forensics-webview2-media-smoke\WEBVIEW2_MEDIA_SMOKE.md`。

### 剩余风险

- 本轮只提供可复现 smoke harness 和 fixture，不宣称已经完成真实 WebView2 播放/seek 人工验证。
- 真实结果仍需在 Windows Tauri 桌面壳中按 checklist 操作后记录。

---

## 2026-06-02 17:08:21 +08:00 - Registry synthetic tiny hive fixture

**署名**: Codex
**基线**: `5e60435 test: add webview2 media smoke harness`

### 本轮改动摘要

- 未启动新的 subagents，因此没有闲置 agent 需要关闭。
- 从剩余 backlog 中选择 Registry fixture gap：当前 parser 已覆盖 Analysis targeted fields，但仓库内缺少可复现 SYSTEM/SOFTWARE hive fixture。
- 新增 `crates/testing::builders::registry`，集中生成 deterministic synthetic SYSTEM/SOFTWARE hives，覆盖 `ComputerName`、`TimeZoneKeyName`、`ProductName`、`CurrentBuild`、`DisplayVersion`、`RegisteredOwner`、`ProductId`、`InstallDate`。
- 新增 `testdata/fixtures/tiny/logical/Windows/System32/config/SYSTEM` 与 `SOFTWARE`，删除旧 `SYSTEM.placeholder`。
- `scripts/generate-tiny-fixtures.ps1` 现在可重建 RAW/E01/Registry fixtures。
- `crates/testing::fixtures` 新增 `tiny_registry_system_hive()` / `tiny_registry_software_hive()`，并用单测保证 committed fixture 与 builder 输出逐字节一致。
- `artifacts-windows::registry::lookup` 新增 committed fixture parser-path 回归；`app-services::analysis_service` 的 registry provenance 测试改为读取同一组 committed fixture，并删除重复私有 builder。
- 更新 tiny fixture README、scripts README、`docs/remediation-plan-v2.1.md`、`docs/architecture-model.md`、`docs/algorithm-complexity-analysis.md`，明确这些 hive 是 synthetic targeted fixtures，不是完整 Windows registry corpus。

### 验证记录

- `powershell -ExecutionPolicy Bypass -File scripts\generate-tiny-fixtures.ps1`: 通过，生成 RAW、E01、SYSTEM、SOFTWARE tiny fixtures。
- `cargo test -p testing registry`: 通过，4 个 registry builder/fixture tests。
- `cargo test -p artifacts-windows registry::lookup`: 通过，23 个 targeted tests。
- `cargo test -p app-services --lib analysis_service`: 通过，9 个 analysis_service tests。

### 剩余风险

- Registry parser 仍只承诺 Analysis targeted fields，不是完整 hive browser。
- 新增 SYSTEM/SOFTWARE 为 synthetic fixture，不替代真实 Windows hive corpus。后续若要提升可信度，需要引入可再分发真实 registry hive fixture 或生成更复杂 cell graph corpus。
- WebView2 media manual smoke 和 FS 深层枚举/错误转换去重仍是后续项。

---

## 2026-06-02 17:25:35 +08:00 - FS child list helper expansion

**署名**: Codex
**基线**: `050ffa8 test: add tiny registry hive fixtures`

### 本轮改动摘要

- 未启动新的 subagents，因此没有闲置 agent 需要关闭。
- 从剩余 Phase 6.1 中选择可自动验证的去重子项：FAT/exFAT/NTFS 目录枚举后都需要给 child node 赋 parent path，并在 FAT/exFAT 中跳过 `.` / `..`。
- `evidence-core::filesystem` 新增 `child_nodes_with_parent_path()`、`child_nodes_with_parent_path_with_separator()` 和 `is_special_directory_name()`，并补单元测试。
- `fs-fat`、`fs-exfat`、`fs-ntfs` 改用共享 child list finalization helper，保留各自原有 separator convention、排序和 root/list/open 行为。

### 验证记录

- `cargo test -p evidence-core filesystem`: 通过，9 个 filesystem/helper tests。
- `cargo test -p fs-fat -p fs-exfat -p fs-ntfs`: 通过，FAT/exFAT/NTFS 全量 crate tests。
- `cargo clippy -p evidence-core -p fs-fat -p fs-exfat -p fs-ntfs --all-targets -- -D warnings`: 通过。
- `cargo test -p app-services file_service`: 通过，10 个 filtered file_service tests。

### 剩余风险

- 本轮不改变低层目录解析流程，也不合并 FS 特定错误转换；Phase 6.1 仍为 Partial，但公共 path/list finalization helper 已进一步收口。
- 后续若继续处理 FS enum 去重，需要更细的 behavior snapshot，尤其是排序、root semantics、NTFS parent verification 和 FAT/exFAT cluster traversal。

### 后置 workspace gate

- `cargo test --workspace`: 通过；真实 E01 slow/manual tests 保持 ignored / opt-in。
- `cargo clippy --workspace --all-targets -- -D warnings`: 通过。

**后置验证时间**: 2026-06-02 17:31:10 +08:00

---

## 2026-06-02 17:43:07 +08:00 - Dependency direct dirs alignment

**署名**: Codex
**基线**: `2cd945d docs: record registry fs post-check gates`

### 本轮改动摘要

- 未启动新的 subagents，因此没有闲置 agent 需要关闭。
- 从剩余依赖治理项中选择可自动验证的直接依赖去重子项：desktop crate 仍直接依赖 `dirs = "5"`，而 Tauri 相关传递依赖已使用 `dirs = "6"`。
- 将 `apps/desktop/src-tauri/Cargo.toml` 的 `dirs` 依赖从 v5 对齐到 v6，并用 `cargo update -p dirs@5.0.1 --precise 6.0.0` 更新 lockfile。
- `Cargo.lock` 不再包含 `dirs 5.0.1`、`dirs-sys 0.4.1`、`redox_users 0.4.6`、`windows-sys 0.48.0`、`windows-targets 0.48.5` 及其 `windows_* 0.48.5` target 包。
- 现有调用点仅使用 `dirs::config_dir()`，v6 保持兼容；本轮不尝试强行 patch Tauri/Tantivy/GTK 等传递依赖。

### 验证记录

- `cargo check -p forensics-desktop`: 通过。
- `cargo test -p forensics-desktop`: 通过，25 个 desktop lib tests；包含 media protocol/range、file command、analysis command、settings command 和 import pipeline regression。
- `cargo clippy -p forensics-desktop --all-targets -- -D warnings`: 通过。
- `cargo deny check bans`: 通过；仍报告其他 Tauri/Tantivy/GTK 传递依赖 duplicate warnings，但 `dirs` / `dirs-sys` 和 `windows-targets 0.48` 重复组已移除。
- `cargo fmt --all -- --check`: 通过。
- `git diff --check`: 通过。

### 剩余风险

- 本轮只清理 workspace 直接依赖造成的 `dirs` v5/v6 分叉；其他 duplicate warnings 多为上游传递依赖组合，需要后续按具体 crate 评估升级，不能用不安全 `[patch]` 强行压平。
- WebView2 media manual smoke 与真实 E01 slow/manual gate 仍依赖人工/外部样本。

---

## 2026-06-02 17:59:08 +08:00 - Tantivy advisory cleanup

**署名**: Codex
**基线**: `b75aa52 chore: align desktop dirs dependency`

### 本轮改动摘要

- 未启动新的 subagents，因此没有闲置 agent 需要关闭。
- 继续依赖治理：`cargo audit` 当前显示 `tantivy 0.22.1 -> measure_time/instant` 和 `tantivy -> lru 0.12.5` 是可由直接依赖升级处理的剩余风险。
- 将 `crates/search/Cargo.toml` 中 `tantivy = "0.22"` 升级到 `tantivy = "0.26"`，lockfile 解析为 `tantivy 0.26.1`。
- 适配 Tantivy 0.26 collector API：`TopDocs::with_limit(limit)` 改为 `TopDocs::with_limit(limit).order_by_score()`。
- `Cargo.lock` 不再包含 `instant`；`lru` 从 `0.12.5` 升到 `0.16.4`；`RUSTSEC-2024-0384` deny 例外已删除，因为该 advisory 不再命中。
- `cargo audit` warning count 从 19 降到 17。剩余 warning 主要仍来自 Tauri Linux GTK/WebKit stack、glib unsound advisory 和 `tauri-utils -> urlpattern -> rust-unic` 传递链。

### 验证记录

- `cargo test -p search`: 通过，14 个 search crate tests，覆盖 index create/search/reopen/highlight。
- `cargo check -p app-services`: 通过。
- `cargo clippy -p search -p app-services --all-targets -- -D warnings`: 通过。
- `cargo test -p app-services --test search_service_test`: 通过，3 个 search service integration tests。
- `cargo test -p forensics-desktop`: 通过，25 个 desktop tests。
- `cargo check -p forensics-desktop`: 通过。
- `cargo audit`: 通过，17 个 allowed warning-class advisories；`instant` 和 `lru` advisories 已消除。
- `cargo deny check advisories bans licenses sources`: 通过；不再报告 `RUSTSEC-2024-0384` ignore entry 未命中。
- `cargo fmt --all -- --check`: 通过。
- `git diff --check`: 通过。

### 剩余风险

- Tantivy 0.26 升级消除了 `instant` 和旧 `lru` advisories，但新增/更新了一批 Tantivy 传递依赖；后续全量门禁仍需要定期复跑。
- 其余 17 个 `cargo audit` warnings 主要受 Tauri Linux GTK/WebKit/urlpattern 上游依赖限制，不能通过本仓直接小改完全消除。
- WebView2 media manual smoke 与真实 E01 slow/manual gate 仍依赖人工/外部样本。

---

## 2026-06-02 18:12:41 +08:00 - FS declared-size truncation helper

**署名**: Codex
**基线**: `dc29c41 chore: upgrade tantivy search dependency`

### 本轮改动摘要

- 未启动新的 subagents，因此没有闲置 agent 需要关闭。
- 工作树仍保留用户侧未跟踪 `.context/plans.md`；本轮未 stage、未删除该文件。
- 继续 Phase 6.1 FS enum / reader helper 去重，选择不改变路径解析、排序、root semantics 或 NTFS parent verification 的数据读取收口项。
- `evidence-core::filesystem` 新增 `truncate_data_to_declared_size()`，集中处理 FAT/exFAT/NTFS 读取完整 cluster/data-run 后按 filesystem declared logical size 去除 padding 的逻辑。
- `fs-fat`、`fs-exfat`、`fs-ntfs` 复用该 helper；exFAT 不再为截断场景额外 copy slice，FAT/NTFS 避免各自手写 `size as usize` 截断分支。

### 验证记录

- `cargo test -p evidence-core filesystem`: 通过，12 个 filesystem/helper tests。
- `cargo test -p fs-fat -p fs-exfat -p fs-ntfs`: 通过。
- `cargo test -p app-services file_service`: 通过，10 个 filtered file_service tests。
- `cargo test -p app-services --test file_service_real_test`: 通过，7 个 real file service tests。
- `cargo clippy -p evidence-core -p fs-fat -p fs-exfat -p fs-ntfs --all-targets -- -D warnings`: 通过。
- `cargo fmt --all -- --check`: 通过。
- `git diff --check`: 通过。

### 剩余风险

- 本轮只收口 filesystem declared-size data truncation，不触碰 FAT/exFAT directory traversal、NTFS path resolution 或 FS 特定错误转换；Phase 6.1 仍是 Partial。
- WebView2 media manual smoke 与真实 E01 slow/manual gate 仍依赖人工/外部样本。

### 后置 workspace gate

- `cargo test --workspace`: 通过；真实 E01 tests 保持 ignored / opt-in。
- `cargo clippy --workspace --all-targets -- -D warnings`: 通过。
- `cargo fmt --all -- --check`: 通过。
- `powershell -ExecutionPolicy Bypass -File scripts\run-webview2-media-smoke.ps1`: 通过；重新生成 `C:\Users\QAQ\AppData\Local\Temp\forensics-webview2-media-smoke\WEBVIEW2_MEDIA_SMOKE.md`，仍需真实 Windows Tauri WebView2 手动播放/seek 验证。
- `FORENSICS_E01_FIXTURE`: 当前未设置，因此真实 E01 slow/manual gate 未执行。
- `cargo audit`: 通过，仍报告 17 个 allowed warning-class advisories，来源集中在 Tauri Linux GTK/WebKit/urlpattern/glib 传递链。

**后置验证时间**: 2026-06-02 18:19:06 +08:00
