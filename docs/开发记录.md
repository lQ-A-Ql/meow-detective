# 开发记录

## 2026-06-01 16:10:00 +08:00 - Stage 5 CI / Fixture Agent

署名: Codex

### 本轮改动

- 阅读并评审最新文档 `docs/full-project-audit-2026-06-01.md`、`docs/security-audit-2026-05-29.md`、`docs/remediation-plan-v2.1.md`。其中 v2.1 Phase 5 明确记录 `cargo deny`、formal CycloneDX/SPDX SBOM、小型 RAW/E01 fixture 策略仍未完成；本轮补齐其中 CI 与 tiny RAW/logical fixture 部分。
- 新增 `deny.toml`，启用 advisory、license、ban、source 检查；现有 Tauri/Tantivy transitive unmaintained advisories 均带 owner、reason、expiry date。
- 后端 CI 新增 `cargo deny check`，保留 `cargo audit`，并将 backend SBOM 从 `cargo metadata` 替换为 CycloneDX JSON artifact `backend-sbom.json`。
- 前端 CI 将 `pnpm list` lightweight SBOM 替换为 `@cyclonedx/cyclonedx-npm` CycloneDX JSON artifact `frontend/frontend-sbom.json`，并增加 JSON 格式验证。
- 新增 `testdata/fixtures/tiny/`：包含 tiny logical directory 和 1024-byte tiny RAW fixture；新增 `scripts/generate-tiny-fixtures.ps1` 可重建 RAW fixture。
- `crates/testing` 暴露 fixture path helper：`tiny_logical_dir()`、`tiny_raw_image()`、`local_e01_fixture()`，并用单元测试保证默认 fixture 可用。默认测试仍不依赖私有本地 E01。
- `runtime-cache` 补齐 workspace authors/license 元数据，使 license policy 可以覆盖所有 workspace crate。
- 本地 `cargo audit` 额外暴露 `glib` 与 `lru` warning-class advisories，均为 Tauri/Tantivy transitive dependency；`cargo deny` 当前目标图未命中这两项，因此不写入 deny 例外，后续应通过升级 Tauri/wry/Tantivy 消除。

### 最新文档评审

- `docs/full-project-audit-2026-06-01.md` 对 Stage 5 风险判断仍准确：原 CI 的 SBOM 是 lightweight metadata/list，不是 formal SBOM；默认 E01 慢测风险需要继续隔离。
- `docs/remediation-plan-v2.1.md` 的 Phase 5 状态在本轮后应更新为：5.2/5.4 completed for CI baseline，5.5 partial，因为 tiny E01 尚未提交，仅有 opt-in `FORENSICS_E01_FIXTURE` 策略。该文档仍保留为历史 backlog 状态，本轮不直接重写，以免与并行阶段文档编辑冲突。

### 验证记录

- `cargo fmt --all -- --check`: 通过。
- `cargo test -p testing`: 通过，3 个 fixture helper 单测通过。
- `cargo deny check advisories bans licenses sources --hide-inclusion-graph`: 通过；仍有 duplicate dependency warnings，属于现有依赖图治理项。
- backend CycloneDX 本地验证：`cargo cyclonedx --manifest-path apps/desktop/src-tauri/Cargo.toml --format json --spec-version 1.5 --override-filename backend-sbom` 生成 `backend-sbom.json`，验证 `bomFormat=CycloneDX`、`specVersion=1.5`、components=401。
- frontend CycloneDX 本地验证：`pnpm dlx @cyclonedx/cyclonedx-npm --output-file frontend/frontend-sbom.json --spec-version 1.6 --output-format JSON --validate frontend/package.json` 生成 `frontend/frontend-sbom.json`，验证 `bomFormat=CycloneDX`、`specVersion=1.6`、components=455。
- `cargo audit`: 通过，退出码 0；报告 19 个 warning-class advisories，主要来自 Tauri Linux GTK/WebKit transitive stack、Tantivy transitive `lru` 和 `urlpattern`/rust-unic。CI 仍保留该检查，短期风险用 `deny.toml` 到期例外跟踪。

---

## 2026-06-02 01:13:05 +08:00 - v2.1 Backlog 主线程统一复核

署名: Codex

### 本轮改动

- 复核四个 subagents 已进入当前 HEAD 的实现，重点检查 Analysis provenance、Reports provenance、Job partial/warning 语义、Viewer scoped range、CI/SBOM/fixture 和文档状态。
- 将 `crates/image-e01/tests/e01_regression_test.rs` 与 `crates/image-e01/tests/e01_dump.rs` 中残留的私有 E01 路径改为 `FORENSICS_E01_FIXTURE`，并统一标记为 ignored opt-in slow tests。
- 更新 `docs/remediation-plan-v2.1.md`、`docs/architecture-model.md`、`docs/algorithm-complexity-analysis.md`，修正旧状态：Analysis provenance、Reports provenance、Job partial、dependency gate、CycloneDX SBOM 和 tiny logical/RAW fixtures 已落地；真实 Registry key/value parser、真实 EVTX parser、连续媒体 streaming、tiny E01 fixture、FS enum 去重和 command SQL 下沉仍是剩余项。
- 新增 `development-reports/sessions/2026-06-02.md`，记录 subagent 分工复核、验证结果和剩余风险。

### 验证记录

- `cargo fmt --all -- --check`: 通过。
- `cargo clippy --workspace --all-targets -- -D warnings`: 通过。
- `cargo test --workspace`: 通过；真实 E01 tests 默认 ignored。
- `cargo build -p forensics-desktop`: 通过。
- `powershell -ExecutionPolicy Bypass -File scripts\check-release-guard.ps1`: 通过。
- `cargo doc --workspace --no-deps`: 通过。
- `cargo deny check advisories bans licenses sources`: 通过；仍有 duplicate dependency warnings。
- `cargo audit`: 通过，退出码 0；仍报告 19 个 allowed warning-class transitive advisories。
- `pnpm --dir frontend typecheck`: 通过。
- `pnpm --dir frontend lint`: 通过，0 error，7 个既有 warning。
- `pnpm --dir frontend test`: 通过，13 个测试文件 / 51 个测试。
- `pnpm --dir frontend build`: 通过；Vite 主 chunk 约 515KB warning。
- `pnpm --dir frontend audit --audit-level high`: 通过。
- `git diff --check`: 通过。

---

## 2026-06-02 02:35:01 +08:00 - 可信 Beta 收口执行

署名: Codex

### 本轮改动

- 开发前读取并评审 `docs/remediation-plan-v2.1.md`、`docs/architecture-model.md`、`docs/algorithm-complexity-analysis.md`、`docs/开发记录.md` 与 `development-reports/sessions/2026-06-02.md`。上一轮文档中的剩余项判断准确：大媒体协议、Registry 定向解析和 EVTX adapter 是本轮主线。
- 新增 `evidence-media://handle/<encoded>` Tauri protocol，保留 `read_media_range` fallback；protocol Range parser 做 overflow-safe 解析，单次读取 clamp 到 1MB，并通过 evidence reader helper 读取，不暴露宿主证据绝对路径。
- Registry 从 header/provenance stub 推进为 Analysis 定向字段 parser：bounded 读取 SYSTEM/SOFTWARE hive，解析 `ComputerName`、timezone、Windows product/build、install date、owner/org/product id，并给每个字段输出 `fieldProvenance`。
- EVTX 从 unavailable/notParsed stub 推进为 boot/shutdown candidate adapter：bounded 解析 System.evtx 中 6005/6006/6008/1074 事件，输出 event id、record id、note 和 provenance。报告和 UI 明确标注为候选事件，不宣称绝对开关机事实。
- DataAnalysis、frontend models、mock data、Reports HTML/CSV/JSON 同步新增 field provenance 和 EVTX candidate 展示；保留 mock mode、HTML escaping 和 CSV formula sanitization。
- `deny.toml` 新增 `RUSTSEC-2021-0153` 临时例外，用于跟踪 `evtx -> encoding` unmaintained transitive advisory，包含 owner、reason、expiry。
- 更新 `docs/remediation-plan-v2.1.md`、`docs/architecture-model.md`、`docs/algorithm-complexity-analysis.md` 和 `development-reports/sessions/2026-06-02.md`。未启动新的 subagents，因此没有闲置 agent 需要关闭。

### 验证记录

- `cargo fmt --all -- --check`: 通过。
- `cargo clippy --workspace --all-targets -- -D warnings`: 通过。
- `cargo test --workspace`: 通过；真实 E01 tests 默认 ignored。
- `cargo test -p app-services --lib analysis_service`: 通过。
- `cargo test -p app-services --lib report_service`: 通过。
- `cargo test -p artifacts-windows registry`: 通过。
- `cargo test -p artifacts-windows evtx`: 通过。
- `cargo test -p transport analysis`: 通过。
- `pnpm --dir frontend typecheck`: 通过。
- `pnpm --dir frontend lint`: 通过，0 error，7 个既有 warning。
- `pnpm --dir frontend test -- analysis files`: 通过，4 个文件 / 20 个测试。
- `pnpm --dir frontend test`: 通过，13 个文件 / 53 个测试；ErrorBoundary 测试打印预期 jsdom stack。
- `pnpm --dir frontend build`: 通过；Vite chunk size warning 存在。
- `cargo build -p forensics-desktop`: 通过。
- `powershell -ExecutionPolicy Bypass -File scripts\check-release-guard.ps1`: 通过。
- `cargo doc --workspace --no-deps`: 通过。
- `cargo deny check advisories bans licenses sources`: 通过；仍有 duplicate dependency warnings。
- `cargo audit`: 通过，退出码 0；报告 20 个 warning-class advisories，其中 `RUSTSEC-2021-0153` 来自本轮 `evtx -> encoding`，已由 `deny.toml` 到期例外跟踪。
- `pnpm --dir frontend audit --audit-level high`: 通过，无已知 high/critical 漏洞。
- Slow E01 manual gate 未运行：当前环境未设置 `FORENSICS_E01_FIXTURE`；提供合法真实 E01 样本后再执行两条 ignored slow test。

### 剩余风险

- EVTX adapter 缺合法 tiny real `.evtx` fixture；当前测试覆盖 JSON 提取和 malformed/oversized path。
- Registry parser 是定向字段 parser，不是完整 hive browser。
- `evidence-media` protocol 尚未做 Windows WebView2 桌面 seek smoke。
- `evtx -> encoding` advisory 例外需在 2026-09-01 前替换或复审。
- tiny E01 fixture 尚未入库；真实 E01 慢测仍依赖 `FORENSICS_E01_FIXTURE`。

---

## 2026-06-02 02:47:09 +08:00 - EVTX fixture/dependency 风险收束

署名: Codex

### 本轮改动

- 未启动新的 subagents，因此没有闲置 agent 需要关闭。
- 复核当前分支 `codex/beta-forensics-backlog`、HEAD `7d12299`，工作树仅有既有未跟踪 `.context/plans.md`；本轮继续不 stage、不删除该文件。
- 本地复核 `evtx 0.11.2` 发布包：`Cargo.toml.orig` 声明 `exclude = ["**/*.evtx", "**/*.dat"]`，因此 crates.io package 中没有可直接复用的合法 tiny real EVTX fixture；license 为 `MIT/Apache-2.0`。
- 本地复核依赖链：`cargo tree -p artifacts-windows -i encoding` 显示 `encoding v0.2.33 -> evtx v0.11.2 -> artifacts-windows`；`encoding = "0.2.33"` 是 `evtx` 的直接非可选依赖，不能通过 feature trimming 消除 `RUSTSEC-2021-0153`。
- 为 EVTX adapter 补充 truncated `ElfFile` magic 输入回归，覆盖更接近真实 EVTX header 的损坏输入，要求返回 warning 且不 panic。
- 更新 `docs/remediation-plan-v2.1.md` 和本记录，明确 EVTX fixture 缺口与 dependency exception 不是配置遗漏。

### 验证记录

- `cargo test -p artifacts-windows evtx`: 通过，6 个 EVTX targeted tests。
- `cargo fmt --all -- --check`: 通过。
- `cargo clippy --workspace --all-targets -- -D warnings`: 通过。
- `git diff --check`: 通过。

### 剩余风险

- 仍缺合法 tiny real `.evtx` fixture；下一步需要自行生成、引入可再分发 fixture，或替换 parser 路径。
- `evtx -> encoding` 临时例外仍需在 2026-09-01 前替换、vendor/patch 或重新评审。

---

## 2026-06-02 02:52:17 +08:00 - Dependency exception guard

署名: Codex

### 本轮改动

- 未启动新的 subagents，因此没有闲置 agent 需要关闭。
- 新增 `scripts/check-deny-exceptions.ps1`，扫描 `deny.toml` 中 `RUSTSEC-*` advisory ignore 项，强制每条例外包含 `owner:`、`expires: YYYY-MM-DD` 和说明文本，并在 expiry 早于当前日期时失败。
- 将 dependency exception guard 接入 `.github/workflows/ci-backend.yml`，位置在 release guard 之后、`cargo deny` 之前；这样到期例外会先以清晰错误失败，而不是沉积在文档中。
- 更新 `docs/remediation-plan-v2.1.md`，把该守卫纳入 Phase 5.2 dependency gate 的已完成证据。

### 验证记录

- `powershell -ExecutionPolicy Bypass -File scripts\check-deny-exceptions.ps1`: 通过，18 条 advisory exceptions checked。
- `powershell -ExecutionPolicy Bypass -File scripts\check-release-guard.ps1`: 通过。
- `cargo fmt --all -- --check`: 通过。
- `cargo clippy --workspace --all-targets -- -D warnings`: 通过。
- `git diff --check`: 通过。

### 剩余风险

- 该脚本只约束 advisory ignore 元数据和到期时间，不替代 `cargo deny` / `cargo audit`。
- 已存在的 upstream/transitive advisory 仍需按 expiry 前升级、替换或重新评审。

---

## 2026-06-02 02:58:33 +08:00 - Settings config closure

署名: Codex

### 本轮改动

- 未启动新的 subagents，因此没有闲置 agent 需要关闭。
- 复核 Settings 实现：路径类设置已通过 `get_app_settings` / `save_app_settings` 后端 config 持久化并校验；theme 与 dev event trace 也写入后端 DTO，同时镜像到 localStorage 以支持 mock/dev 即时生效。
- 给 Settings 页面案件目录和镜像搜索路径输入补充显式 `label` / `htmlFor`，提升可访问性并让 render tests 能按标签查询。
- 新增 `frontend/src/app/pages/Settings.test.tsx`，覆盖后端配置加载、保存 API payload、localStorage mirror、主题应用、mock/local fallback 和非法镜像路径拒绝。
- 更新 `docs/remediation-plan-v2.1.md`，将 Phase 3.8 Settings editable config 从 Partial 收口为 Completed，并记录验证命令。

### 验证记录

- `pnpm --dir frontend test -- Settings`: 通过，3 files / 11 tests。
- `pnpm --dir frontend typecheck`: 通过。
- `pnpm --dir frontend lint`: 通过，0 error / 7 existing warnings。
- `git diff --check`: 通过。

### 剩余风险

- Settings 的 theme/dev event trace 仍保留 localStorage mirror，这是 mock/dev 即时生效的有意设计，不影响后端 config 作为路径类设置契约源。

---

## 2026-06-02 03:05:16 +08:00 - Event bridge closure

署名: Codex

### 本轮改动

- 未启动新的 subagents，因此没有闲置 agent 需要关闭。
- 新增 transport typed topic：`data-source-imported` 与 `job-cancelled`，并同步 `EventTopic::as_str()` serde 测试。
- `event_bridge` 新增 `emit_data_source_imported()` 与 `emit_job_cancelled()` helper。`data-source-imported` payload 只包含 `dataSourceId`、`name`、`kind`、`jobId`，不包含宿主证据 `sourcePath`。
- `cancel_import` 成功请求取消时发 `job-cancelled`；import finalizing 阶段查询 data source summary 并发 `data-source-imported`。
- 前端 `EventTopic` union 和 Tauri bridge 监听列表同步新增 topic；新增 EventBus 测试覆盖两个 topic。
- 更新 `docs/remediation-plan-v2.1.md`，将 Phase 4.2 Event bridge 扩展从 Partial 收口为 Completed。

### 验证记录

- `cargo test -p transport events`: 通过。
- `cargo clippy -p forensics-desktop --all-targets -- -D warnings`: 通过。
- `pnpm --dir frontend test -- events`: 通过，1 file / 1 test。
- `pnpm --dir frontend typecheck`: 通过。
- `cargo fmt --all -- --check`: 通过。

### 剩余风险

- `job-cancelled` 作为事件发出；现有 job snapshot 仍用既有 status/detail 表达最终状态，未在本轮引入新的 cancelled job status 迁移。

---

## 2026-06-02 03:10:13 +08:00 - Command SQL boundary guard

署名: Codex

### 本轮改动

- 未启动新的 subagents，因此没有闲置 agent 需要关闭。
- 复核 `apps/desktop/src-tauri/src/commands`，当前无原始 SQL 语句或 `rusqlite::params!/prepare/execute` 低层调用；command layer 仍允许打开 case DB 连接并调用 repository/service/helper。
- 新增 `scripts/check-command-sql-boundary.ps1`，扫描 Tauri command 层并拦截显式 SQL 关键字和底层 rusqlite SQL API，防止后续把业务 SQL 写回 command。
- 将 command SQL boundary guard 接入 `.github/workflows/ci-backend.yml`。
- 更新 `docs/remediation-plan-v2.1.md`，将 Phase 6.2 SQL 下沉 repo 从 Partial 收口为 Completed / guarded。

### 验证记录

- `powershell -ExecutionPolicy Bypass -File scripts\check-command-sql-boundary.ps1`: 通过。
- `powershell -ExecutionPolicy Bypass -File scripts\check-release-guard.ps1`: 通过。
- `powershell -ExecutionPolicy Bypass -File scripts\check-deny-exceptions.ps1`: 通过。
- `cargo fmt --all -- --check`: 通过。
- `git diff --check`: 通过。

### 剩余风险

- Guard 只约束 Tauri command 层不写业务 SQL；repository/service 层仍需依赖现有单元/集成测试保证 SQL 行为正确。

---

## 2026-06-02 03:19:24 +08:00 - Beta final gate and review closeout

署名: Codex

### 本轮改动

- 未启动新的 subagents，因此没有闲置 agent 需要关闭。
- 复核当前分支 `codex/beta-forensics-backlog`，HEAD 为 `af2833c ci: guard command sql boundary`；工作树仅保留用户侧未跟踪 `.context/plans.md`。
- 按 AGENTS 要求重新读取 `docs/remediation-plan-v2.1.md`、`docs/architecture-model.md`、`docs/algorithm-complexity-analysis.md`、本开发记录和 `development-reports/sessions/2026-06-02.md`。
- 补跑可信 beta final gate，并更新 `docs/remediation-plan-v2.1.md` 中的最终测试计数和剩余风险表述。
- 统一 grep 审阅伪取证事实、direct Tauri invoke、host path 泄露和 evidence 直读回退：命中均为审计文档、负向测试、时间戳单测注释或既有导入内部路径处理；页面/feature 的 Tauri invoke 只存在于统一 `apiClient`。

### 验证记录

- `cargo fmt --all -- --check`: 通过。
- `cargo clippy --workspace --all-targets -- -D warnings`: 通过。
- `cargo test --workspace`: 通过；真实 E01 tests 默认 ignored，需要 `FORENSICS_E01_FIXTURE` 手动运行。
- `cargo build -p forensics-desktop`: 通过。
- `cargo doc --workspace --no-deps`: 通过。
- `powershell -ExecutionPolicy Bypass -File scripts\check-release-guard.ps1`: 通过。
- `powershell -ExecutionPolicy Bypass -File scripts\check-command-sql-boundary.ps1`: 通过。
- `powershell -ExecutionPolicy Bypass -File scripts\check-deny-exceptions.ps1`: 通过，18 条 advisory exceptions checked。
- `cargo deny check advisories bans licenses sources`: 通过；仍有 duplicate dependency warnings。
- `cargo audit`: 通过，退出码 0；报告 20 个 allowed warning-class advisories。
- `pnpm --dir frontend typecheck`: 通过。
- `pnpm --dir frontend lint`: 通过，0 error / 7 existing warnings。
- `pnpm --dir frontend test`: 通过，15 files / 58 tests；ErrorBoundary 用例打印预期 jsdom stack。
- `pnpm --dir frontend build`: 通过；Vite 主 chunk 约 520KB，仍提示 chunk size warning。
- `pnpm --dir frontend audit --audit-level high`: 通过，无已知漏洞。
- `git diff --check`: 通过。

### 剩余风险

- EVTX adapter 仍缺合法 tiny real `.evtx` fixture；当前覆盖 JSON extraction helper、truncated magic、malformed 和 oversized path。
- Registry parser 是 Analysis 定向字段 parser，不是完整 hive browser。
- `evidence-media://` protocol 尚未做真实 Windows WebView2 桌面 seek smoke。
- `evtx -> encoding` advisory 例外需在 2026-09-01 前升级、替换 parser、vendor/patch 或重新评审。
- tiny E01 fixture 尚未入库；真实样本慢测仍依赖 `FORENSICS_E01_FIXTURE`。

---

## 2026-06-02 03:26:01 +08:00 - FS reader helper dedupe

署名: Codex

### 本轮改动

- 未启动新的 subagents，因此没有闲置 agent 需要关闭。
- 复核当前分支 `codex/beta-forensics-backlog`，HEAD 为 `ad4ccd2 docs: record beta final gate`；工作树仅保留用户侧未跟踪 `.context/plans.md`。
- 从剩余 backlog 中选择本地可验证的 Phase 6.1 子项，避免依赖真实 EVTX fixture、私有 E01 或桌面人工 smoke。
- 在 `evidence-core::filesystem` 抽出 `root_node`、`join_child_path` / `join_child_path_with_separator`、`path_not_found`、`file_not_found`、`path_is_directory`、`path_is_not_directory` 等公共 helper。
- `fs-fat`、`fs-ntfs`、`fs-exfat` 复用公共 helper，去除重复 root node 构造和路径拼接/常见错误构造。exFAT 保持反斜杠 path convention，FAT/NTFS 保持 slash convention，避免改变既有行为。
- 更新 `docs/remediation-plan-v2.1.md`，将 Phase 6.1 从 Remaining 标为 Partial：公共 root/path/error helper 已落地，更深的枚举流程和 FS 特定错误转换去重仍留后续。

### 验证记录

- `cargo fmt --all -- --check`: 通过。
- `cargo test -p evidence-core filesystem`: 通过。
- `cargo test -p fs-fat`: 通过。
- `cargo test -p fs-exfat`: 通过。
- `cargo test -p fs-ntfs`: 通过。
- `cargo test -p app-services file_service`: 通过。
- `cargo test -p app-services --test file_service_real_test`: 通过。
- `cargo clippy --workspace --all-targets -- -D warnings`: 通过。

### 剩余风险

- 本轮只抽取 FS reader 的公共 root/path/error helper，不重构 FAT/NTFS/exFAT 深层枚举流程或错误类型体系。
- 进一步去重需谨慎保持 public API、排序、root semantics、path separator convention 和 reader behavior 不变。

---

## 2026-06-02 10:23:41 +08:00 - Frontend lint warning cleanup

署名: Codex

### 本轮改动

- 未启动新的 subagents，因此没有闲置 agent 需要关闭。
- 保留用户侧未跟踪 `.context/plans.md`，未 stage、未删除。
- 清理前端既有 7 个 lint warning：
  - 移除 `McpServerItem.tsx` 未使用的 `Settings` icon import。
  - 将 `McpToolList.tsx` tool call result `data` 从 `any` 收紧为 `unknown`，并在渲染前显式 stringify。
  - 将 `DenseDataTable.tsx` memo row props 改为泛型类型，移除 `any`。
  - 移除 `SyntaxHighlighter.tsx` 未使用的 `lineRenderer` prop。
  - 移除 `tauri-bridge.ts` 未使用的 `JobSnapshot` type import。
- 更新 `docs/remediation-plan-v2.1.md`，将当前 frontend lint 状态更新为 `0 error / 0 warnings`。

### 验证记录

- `pnpm --dir frontend lint`: 通过，0 error / 0 warnings。
- `pnpm --dir frontend typecheck`: 通过。
- `pnpm --dir frontend test`: 通过，15 个测试文件 / 58 个测试；ErrorBoundary 用例打印预期 jsdom stack。
- `pnpm --dir frontend build`: 通过；仍保留既有 Vite chunk size warning，主 chunk 约 520KB。

### 剩余风险

- 本轮只清理 lint warning，不处理 Vite chunk size warning；后续可通过 route-level dynamic import 或 manualChunks 单独收口。

---

## 2026-06-02 10:27:03 +08:00 - Frontend route-level code splitting

署名: Codex

### 本轮改动

- 未启动新的 subagents，因此没有闲置 agent 需要关闭。
- 保留用户侧未跟踪 `.context/plans.md`，未 stage、未删除。
- 将 `frontend/src/app/routes.tsx` 的页面级静态导入改为 React Router `lazy` 动态导入。
- 保留 `Layout` 静态导入，确保 app shell 首屏结构不变；各页面按 route 拆包加载。
- 更新 `docs/remediation-plan-v2.1.md`，把 frontend build 状态更新为无 chunk size warning，主 chunk 约 315KB。

### 验证记录

- `pnpm --dir frontend typecheck`: 通过。
- `pnpm --dir frontend lint`: 通过，0 error / 0 warnings。
- `pnpm --dir frontend test`: 通过，15 个测试文件 / 58 个测试；ErrorBoundary 用例打印预期 jsdom stack。
- `pnpm --dir frontend build`: 通过；无 Vite chunk size warning，主 chunk 约 315KB，页面 chunk 独立输出。

### 剩余风险

- 本轮只做 route-level code splitting；未引入 manualChunks。若后续依赖体积继续增长，可再对 vendor/common chunk 做专项拆分。

---

## 2026-06-02 10:30:21 +08:00 - Route splitting regression guard

署名: Codex

### 本轮改动

- 未启动新的 subagents，因此没有闲置 agent 需要关闭。
- 保留用户侧未跟踪 `.context/plans.md`，未 stage、未删除。
- 为 `frontend/src/app/routes.tsx` 导出 `appRoutes`，并新增 `frontend/src/app/routes.test.ts`。
- 测试断言 root `Layout` 保持静态加载，所有页面 child routes 使用 `lazy`，防止后续把页面静态 import 回来导致主 chunk 膨胀。
- `ui-store` 的 `localStorage` 读写改为安全 helper，避免测试、SSR 或受限浏览器环境缺少 storage API 时模块初始化崩溃；正常浏览器持久化行为不变。
- 更新 `docs/remediation-plan-v2.1.md`，将当前 frontend test 计数更新为 16 files / 59 tests。

### 验证记录

- `pnpm --dir frontend test -- routes`: 通过，1 个测试文件 / 1 个测试。
- `pnpm --dir frontend test`: 通过，16 个测试文件 / 59 个测试；ErrorBoundary 用例打印预期 jsdom stack。
- `pnpm --dir frontend typecheck`: 通过。
- `pnpm --dir frontend lint`: 通过，0 error / 0 warnings。
- `pnpm --dir frontend build`: 通过；无 Vite chunk size warning，主 chunk 约 315KB。

### 剩余风险

- Vitest 单文件 route 测试仍会打印 Node 的 `--localstorage-file` warning，但退出码为 0，且 `ui-store` 已对 storage API 不可用场景容错。

---

## 2026-06-02 10:39:22 +08:00 - Tiny E01 reader fixture

署名: Codex

### 本轮改动

- 未启动新的 subagents，因此没有闲置 agent 需要关闭。
- 保留用户侧未跟踪 `.context/plans.md`，未 stage、未删除。
- 在 `scripts/generate-tiny-fixtures.ps1` 中增加 deterministic synthetic single-segment E01 fixture 生成逻辑，生成 `testdata/fixtures/tiny/e01/tiny.E01`，大小 4405 bytes。
- `crates/testing::fixtures` 新增 `tiny_e01_image()` helper，并补 fixture 存在性/header/marker 单测。
- `crates/image-e01` 增加 `testing` dev-dependency，默认 regression tests 打开 committed tiny E01，验证 reader kind/size、开头 marker、MBR signature 位置、seek-to-end 和 EOF 行为。
- 更新 `testdata/fixtures/tiny/README.md`、`docs/remediation-plan-v2.1.md`、`docs/architecture-model.md` 和 `docs/algorithm-complexity-analysis.md`，明确 tiny E01 只覆盖 reader section/table/read/seek，不是完整文件系统镜像；真实 E01 慢测仍需 `FORENSICS_E01_FIXTURE`。

### 验证记录

- `powershell -ExecutionPolicy Bypass -File scripts\generate-tiny-fixtures.ps1`: 通过，生成 RAW 1024 bytes 和 E01 4405 bytes。
- `cargo test -p image-e01`: 通过；6 个 lib tests，4 个默认 regression tests，7 个 ignored real-E01 dump/slow tests。
- `cargo test -p testing`: 通过，4 个 fixture helper tests。
- `cargo fmt --all -- --check`: 通过。
- `cargo clippy -p image-e01 --all-targets -- -D warnings`: 通过。

### 剩余风险

- Tiny E01 不包含真实 partition/filesystem；`app-services` 的真实 E01 probe/import/full pipeline 慢测仍依赖 `FORENSICS_E01_FIXTURE`。
- EVTX 仍缺合法 tiny real `.evtx` fixture。

---

## 2026-06-02 10:56:14 +08:00 - Coverage reporting gate

署名: Codex

### 本轮改动

- 未启动新的 subagents，因此没有闲置 agent 需要关闭。
- 保留用户侧未跟踪 `.context/plans.md`，未 stage、未删除。
- 继续从 `e3692f2 test: add tiny e01 fixture` 基线推进剩余 backlog，选择不依赖私有样本或人工 WebView2 smoke 的 Phase 5.6 coverage 子项。
- 前端新增 `@vitest/coverage-v8` devDependency 和 `pnpm --dir frontend test:coverage` 脚本，Vitest coverage 输出 `text`、`json-summary`、`lcov` 到 `frontend/coverage`。
- 新增 `scripts/run-coverage.ps1`，统一执行前端 coverage；Rust coverage 在安装 `cargo-llvm-cov` 时生成 `coverage/rust-lcov.info`，未安装时默认 warning 跳过，避免本地默认门禁被工具缺失阻塞。
- 后端 CI 新增 `llvm-tools-preview`、`cargo-llvm-cov`、`backend-coverage` artifact；前端 CI 新增 `frontend-coverage` artifact。当前只上传报告，不设置百分比阈值。
- 更新 `.gitignore`、`scripts/README.md`、`docs/remediation-plan-v2.1.md`、`docs/architecture-model.md`、`docs/algorithm-complexity-analysis.md`，明确 coverage 已有执行入口但阈值仍需稳定 baseline 后再定。

### 验证记录

- `pnpm --dir frontend test:coverage`: 通过；16 个测试文件 / 59 个测试，生成 `frontend/coverage/coverage-summary.json`、`lcov.info`，当前总行覆盖率约 47.97%。
- `powershell -ExecutionPolicy Bypass -File scripts\run-coverage.ps1 -Frontend`: 通过；统一 runner 的前端 coverage 路径可用。
- `pnpm --dir frontend typecheck`: 通过。
- `pnpm --dir frontend lint`: 通过，0 error / 0 warnings。
- `pnpm --dir frontend test`: 通过；16 个测试文件 / 59 个测试，ErrorBoundary 用例打印预期 jsdom stack，退出码为 0。
- `pnpm --dir frontend build`: 通过；无 Vite chunk size warning，主 chunk 约 315KB。
- `pnpm --dir frontend audit --audit-level high`: 通过，无已知漏洞。
- `pnpm --dir frontend install --frozen-lockfile`: 通过。
- `powershell -ExecutionPolicy Bypass -File scripts\run-coverage.ps1 -Rust`: 通过；本机未安装 `cargo-llvm-cov`，按设计 warning 后跳过 Rust coverage。
- `cargo fmt --all -- --check`: 通过。
- `git diff --check`: 通过。

### 剩余风险

- Coverage 当前是报告型 artifact，不是硬阈值 gate；后续需要在报告稳定后决定 backend/frontend 阈值。
- Rust coverage CI 会安装 `cargo-llvm-cov` 并运行全 workspace coverage，CI 时长会增加；本地脚本默认不强制要求该工具。
- EVTX 合法 tiny real `.evtx` fixture、Windows WebView2 media smoke、`evtx -> encoding` advisory cleanup 仍是后续项。
