# Registry 真实样本回归 — liuyang_pc.E01

**日期**: 2026-06-13
**样本**: `E:\pangushi\刘洋\liuyang_pc.E01`

## 提取的 Hive 文件

| Hive | 路径 | 大小 | 状态 |
|------|------|------|------|
| SYSTEM | Windows/System32/config/SYSTEM | 12.5 MB | ✅ regf header |
| SOFTWARE | Windows/System32/config/SOFTWARE | 76.2 MB | ✅ regf header |

## SYSTEM Hive 字段

| 字段 | 提取结果 | 保证级别 |
|------|---------|---------|
| computer_name | 已提取 | Guaranteed |
| timezone | 已提取 | Guaranteed |
| services | 未验证 | BestEffort |
| network_adapters | 未验证 | BestEffort |

## SOFTWARE Hive 字段

| 字段 | 提取结果 | 保证级别 |
|------|---------|---------|
| product_name | 已提取 | Guaranteed |
| current_build | 已提取 | Guaranteed |
| display_version | 已提取 | BestEffort |
| registered_owner | 已提取 | Guaranteed |
| registered_organization | 已提取 | BestEffort |
| product_id | 已提取 | BestEffort |
| install_date | 已提取 | Guaranteed |

## Not-guaranteed 字段

| 字段 | 说明 |
|------|------|
| 事务日志回放 | 未实现，可能丢失最近的注册表更改 |
| 已删除 cell 恢复 | 未实现 |
| 安全描述符解析 | 未实现 |
| 完整 key path 枚举 | 当前仅提取已知高价值字段 |

## NTUSER Hive 提取结果

**Hive 路径**: `Users/<username>/NTUSER.DAT`（每个用户 profile 一个 hive）

| 字段 | 提取结果 | 保证级别 | 来源 Key |
|------|---------|---------|---------|
| user_profile_path | 已提取 | Guaranteed | ProfileImagePath |
| shell_folders | 已提取 (Desktop, Documents, Downloads 等) | Guaranteed | Shell Folders |
| recent_files | 已提取 | Guaranteed | RecentDocs |
| typed_paths | 已提取 | Guaranteed | TypedPaths |
| run_mru | 已提取 | Guaranteed | RunMRU |
| mount_points | 已提取 | BestEffort | MountPoints2 |
| network_shares | 已提取 | BestEffort | Map Network Drive MRU |
| user_assist | 未实现 | — | UserAssist (ROT13 编码，规划中) |
| comdlg32 | 未实现 | — | ComDlg32 (打开/保存对话框历史) |
| word_wheel_query | 未实现 | — | WordWheelQuery (Explorer 搜索历史) |
| bag_mru | 未实现 | — | BagMRU (文件夹视图设置) |

### 提取说明

- NTUSER.DAT 按用户 profile 层级发现：遍历 `Users/` 下子目录，对每个包含 `NTUSER.DAT` 的用户执行提取
- 字段提取采用定向 key path 读取，不走完整 hive 枚举
- shell_folders 解析为 `{GUID} → 路径` 映射，与 Known Folder GUID 表关联以生成友好名称
- recent_files 提取 RecentDocs 下按扩展名分组的 MRU 列表，含文件路径与最后写入时间
- run_mru 提取 `RunMRU` 下的启动命令历史，按 MRU 顺序排列
- typed_paths 提取 Explorer 地址栏手动输入路径历史

## SAM Hive 提取结果

**Hive 路径**: `Windows/System32/config/SAM`

| 字段 | 提取结果 | 保证级别 | 来源 Key |
|------|---------|---------|---------|
| local_users | 已提取 (用户名、RID、最后登录时间、密码重置信息) | Guaranteed | SAM\Domains\Account\Users |
| local_groups | 已提取 (组名、RID) | Guaranteed | SAM\Domains\Builtin\Aliases |
| group_membership | 已提取 (用户→组映射) | Guaranteed | SAM\Domains\Builtin\Aliases\Members |
| login_count | 已提取 (每位用户) | Guaranteed | F 结构体 login_count 字段 |
| password_policy | 已提取 | BestEffort | SAM\Domains\Account |
| user_account_control_flags | 已提取 (禁用/锁定/密码过期等) | Guaranteed | F 结构体 control flags |
| NT/LM hash | 未提取 (安全边界) | — | 不提取密码哈希，保持取证最小必要原则 |

### 提取说明

- SAM hive 以 SYSTEM hive 的同名 key 解密，或通过已知 syskey 派生解密
- 用户信息解析自 C:\ 格式的注册表 value 结构 (F 结构体)，含 login_count、最后登录时间、账户控制标志
- 组成员关系从 `SAM\Domains\Builtin\Aliases\<RID>` 的成员列表解析
- 本地组映射：`Administrators` (RID 544)、`Users` (RID 545)、`Guests` (RID 546) 等

## Transaction Log (txlog) 集成状态

### 当前能力

| 能力 | 状态 | 说明 |
|------|------|------|
| txlog 文件发现 | ✅ 已实现 | 自动发现 .LOG1、.LOG2 文件 |
| txlog 脏页标记读取 | ✅ 已实现 | 解析 dirty page vector，识别哪些 hive 页有未提交变更 |
| txlog → hive 合并 | ❌ 未实现 | 当前不将 txlog 中的脏页合并回 base hive 解析结果 |
| txlog-only 键值恢复 | ❌ 未实现 | txlog 中已提交但 base hive 中不可见的键值不提取 |

### 影响评估

- **影响范围**: 注册表最后修改（关机前未刷盘的事务）可能丢失。对于在线采集或非正常关机的镜像影响较大
- **缓解措施**: 对于已关机/正常关机的镜像（如 liuyang_pc.E01），hive base 文件通常已包含所有已提交修改，txlog 缺失影响有限
- **规划**: V3 期内实现 txlog dirty page 合并读取，补齐非正常关机场景覆盖

### 技术路线 (规划)

1. 解析 .LOG1/.LOG2 的 header → dirty page bitmap
2. 为每个 dirty page 在 txlog 中定位对应 sector
3. 当 hive 页在 txlog 中有更新版本时，以 txlog 版本覆盖 base hive 页
4. 合并后的 hive 视图送入已有的定向 key path 提取链路
