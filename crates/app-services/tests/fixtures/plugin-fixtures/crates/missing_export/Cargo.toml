[package]
name = "meow-fixture-missing-export"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
plugin-api = { path = "../../../../../../plugin-api" }
