[package]
name = "meow-fixture-undeclared-family"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
plugin-api = { path = "../../../../../../plugin-api" }
