# PRD: Rust + React 磁盘介质取证工具

## 1. 产品概述

本产品是一款面向个人研究者的桌面优先磁盘介质取证工具，使用 Rust 构建核心解析与任务编排能力，使用 React 构建交互界面。产品目标是借鉴 Autopsy 在案件管理、证据导入、检索、时间线、痕迹提取和报告导出上的成熟能力，同时避免其历史桌面架构耦合重、模块边界不清和平台演进成本高的问题。

第一版优先运行于 Windows 宿主机，聚焦单用户、本地离线分析场景，重点支持磁盘镜像导入、文件系统浏览、关键词检索、时间线分析、Windows 常见取证痕迹提取和调查结果导出。

## 2. 背景与机会

Autopsy 证明了通用数字取证工作台的长期价值，尤其是在以下几个方面：
- 以案件为单位组织证据、分析过程和导出结果
- 通过模块化解析流程逐步提取高价值痕迹
- 将文件浏览、全文检索、时间线和报告能力串成完整调查闭环

但对新的 Rust + React 产品而言，也存在明确机会：
- 用更清晰的后端领域边界替代历史单体式桌面协调器
- 用现代响应式 UI 替代重桌面控件耦合
- 用更强的类型系统和并发模型重构取证任务编排
- 从一开始为可扩展的解析器、索引器和导出器设计稳定接口

## 3. 目标用户

### 主要目标用户
- 安全研究员
- 独立取证分析者
- 红蓝对抗/事件响应中的本地证据分析人员
- 教学与实验环境中的高级用户

### 用户特征
- 能理解镜像、分区、文件系统、时间线、哈希、证据导出等概念
- 对准确性、可追溯性、处理性能有明确要求
- 能接受本地安装式桌面产品，不要求第一版支持多人协作

## 4. 产品目标

### 4.1 第一版核心目标
1. 让用户能在 Windows 上稳定导入并分析常见磁盘镜像
2. 让用户能高效浏览文件系统与关键元数据
3. 让用户能对文件内容和提取文本执行关键词检索
4. 让用户能对文件系统事件和痕迹事件进行时间线分析
5. 让用户能提取首批高价值 Windows 痕迹
6. 让用户能导出调查结果、命中列表和证据摘要报告

### 4.2 成功标准
- 用户可以在单个案件内完成从导入、分析、筛选到导出的完整闭环
- 核心流程不依赖命令行
- 长耗时任务具备可见进度、可取消、可恢复查看结果的体验
- 主要能力可以通过模块扩展，而不是修改核心主流程

## 5. 非目标

第一版不做以下内容：
- 多用户协作办案
- 云端集中式案件管理
- 分布式分析集群
- 移动端取证
- 内存取证
- 网络流量取证
- 手机镜像专用解析
- 全量替代所有成熟外部取证生态
- 完整司法交付链所需的全部法规适配

## 6. 使用场景

### 场景 1：导入磁盘镜像并定位可疑文件
用户创建案件，导入 E01 或 RAW 镜像，浏览分区和目录树，通过文件类型、时间、路径、大小、哈希等条件缩小范围，并导出关键文件。

### 场景 2：通过关键词检索命中敏感内容
用户对提取文本后的文件集合执行关键词、短语或正则检索，查看命中文件、命中片段与上下文，并将结果加入报告。

### 场景 3：通过时间线复原行为过程
用户查看文件 MACB 时间、解析出的 Windows 痕迹时间和关键系统事件，按时间轴聚合过滤，以辅助复原用户操作过程。

### 场景 4：快速提取 Windows 常见痕迹
用户一键运行 Windows 痕迹模块，获得 Prefetch、LNK、Jump List、Recycle Bin、Registry、SRU 等结果，用于判断程序执行、文档访问和用户行为轨迹。

### 场景 5：输出调查结果
用户从文件命中、痕迹事件、标签与备注中挑选内容，导出 HTML/JSON/CSV/PDF 等形式的报告或证据摘要。

## 7. 核心能力需求

## 7.1 案件管理
### 必须支持
- 创建案件
- 打开历史案件
- 配置案件名称、编号、分析员、备注
- 记录案件创建时间、最后打开时间
- 将分析结果、标签、备注、任务状态持久化

### 关键要求
- 案件是最顶层工作单元
- 所有数据源、检索索引、痕迹结果、报告记录都归属于案件
- 关闭案件后可重新打开并继续分析

## 7.2 数据源导入
### 必须支持
- RAW/dd 镜像
- E01 镜像
- 本地目录作为逻辑数据源
- 只读方式打开原始证据

### 应支持
- 多分区识别
- 基本文件系统识别
- 时区配置
- 导入前校验与错误提示
- 对缺失片段、损坏段给出结构化错误

### 暂不要求
- 直接物理磁盘写阻设备工作流
- 远程证据源挂载

## 7.3 文件系统浏览
### 必须支持
- 树形目录浏览
- 文件列表视图
- 文件元数据查看
- 十六进制查看
- 文本查看
- 基本媒体预览
- 文件导出
- 哈希展示

### 关键元数据
- 路径
- 大小
- 文件类型
- 创建/修改/访问/元数据变更时间
- 哈希
- 所属分区/卷
- 已删除标记
- 索引状态
- 标签状态

## 7.4 关键词检索
借鉴 Autopsy 的关键词检索与索引设计，但第一版采用更清晰的后端服务边界。

### 必须支持
- 字面量关键词搜索
- 短语搜索
- 正则搜索
- 按路径、扩展名、文件类型、时间范围过滤
- 命中片段与上下文查看
- 命中结果导出

### 关键要求
- 索引过程可见进度
- 索引与搜索结果属于案件
- 搜索语法可被保存和复用
- 搜索结果可添加标签或加入报告

## 7.5 时间线
借鉴 Autopsy 的案件级时间线思路，但前后端分离实现。

### 必须支持
- 展示文件 MACB 时间事件
- 展示解析器产出的痕迹时间事件
- 按时间范围筛选
- 按事件类型筛选
- 按数据源/路径筛选
- 事件详情查看

### 应支持
- 多粒度缩放
- 事件聚合
- 时间线跳转到原始文件或痕迹记录

## 7.6 Windows 痕迹提取
这是第一版差异化重点之一。

### 第一批必须支持
- Windows Registry 基础解析
- Prefetch
- LNK
- Jump List
- Recycle Bin
- SRU

### 第二优先级
- Thumbcache
- Shellbags
- Zone.Identifier
- 浏览器历史（优先 Chromium 系）

### 输出要求
- 每类痕迹有独立结果视图
- 关键字段可筛选、排序、导出
- 结果可回链到原始文件或原始数据位置
- 结果可进入时间线和报告

## 7.7 标签、书签与备注
### 必须支持
- 给文件、命中结果、痕迹记录打标签
- 添加调查备注
- 标记重要/待复核项
- 在报告导出时选用标签集

## 7.8 报告与导出
借鉴 Autopsy 的可插拔报告模块思想。

### 第一版必须支持
- HTML 报告
- JSON 导出
- CSV 导出
- 证据文件批量导出

### 报告内容至少包括
- 案件元信息
- 数据源摘要
- 关键词命中摘要
- 时间线摘要
- Windows 痕迹摘要
- 标签与备注
- 导出文件清单

## 8. 用户体验要求

### 桌面应用体验
- 优先本地桌面交互
- 支持长任务后台执行
- 支持任务取消
- 支持失败后查看错误详情
- 支持再次打开案件继续查看历史结果

### 可观测性
- 所有长任务需要阶段进度
- 用户能知道当前正在分析哪个数据源、哪个模块、哪一类工件
- 所有解析失败应尽可能结构化呈现，而不是只有日志

### 信息架构建议
- 左侧：案件/数据源/结果导航
- 中间：列表、时间线、检索结果或痕迹视图
- 右侧：详情面板
- 顶部：全局搜索、任务状态、导出入口

## 9. 产品原则

1. **只读优先**：默认绝不修改原始证据
2. **案件优先**：所有分析行为都属于案件上下文
3. **任务可见**：所有长任务必须有状态、进度和结果
4. **模块化**：导入、解析、索引、报告都应可替换或扩展
5. **结果可回链**：所有高层结果都能回到原始文件或原始工件
6. **先单机后协作**：第一版不做多用户，但架构不阻断未来演进

## 10. 竞争/借鉴分析

### 借鉴 Autopsy 的部分
- 案件作为核心工作单元
- 数据源导入 + 文件浏览 + 检索 + 时间线 + 报告的完整闭环
- 工件提取器按能力域拆分
- 报告模块化
- 时间线和检索作为核心分析入口

### 不照搬 Autopsy 的部分
- 不把 UI 生命周期和案件生命周期揉在一个核心对象中
- 不采用强桌面框架耦合的模块体系
- 不把检索、提取、通知、状态更新都塞进单例管理器
- 不在第一版追求过多外部工具拼装

## 11. 关键发布范围

### MVP 范围
- 单用户案件管理
- RAW/E01/逻辑目录导入
- 文件系统浏览与导出
- 文本/十六进制基础查看
- 关键词索引与搜索
- 时间线基础能力
- 第一批 Windows 痕迹解析
- HTML/JSON/CSV 报告导出
- 标签与备注

### MVP 之后
- 更多文件系统与镜像格式
- 更多浏览器与应用痕迹
- 更强关联分析
- 更完整报告模板
- 插件市场或第三方模块 SDK
- 多用户协作

## 12. 成功指标

### 体验指标
- 用户能在 30 分钟内完成首次案件导入与基本分析
- 主要功能无需命令行辅助
- 关键长任务状态清晰且可取消

### 功能指标
- 支持至少一种常见镜像格式与一种逻辑数据源
- 支持至少 6 类 Windows 痕迹输出
- 支持关键词命中回链和结果导出
- 支持时间线从事件跳转至原始对象

### 工程指标
- 核心分析流程具备稳定的任务编排与错误恢复能力
- 模块新增不需要修改核心 UI 主流程
- 案件数据结构可迁移、可版本化

## 13. 风险与约束

### 主要风险
- 纯 Rust 路线会显著提升镜像解析、文件系统支持和工件解析的初期研发成本
- Windows 痕迹解析覆盖面较大，容易在第一版范围失控
- 全文索引与提取文本性能可能成为桌面环境瓶颈
- 案件数据库、索引、工件结果的持久化模型若前期设计不稳，后续迁移成本高

### 应对策略
- 第一版优先做高价值路径而非全覆盖
- 工件解析按依赖层次分波次落地
- 统一结果模型，避免每个解析器单独造数据结构
- 提前定义稳定的任务、事件和持久化边界

## 14. 开放问题

以下问题会影响后续详细设计：
- 纯 Rust 是否允许有限度引入成熟解析库作为过渡依赖
- 第一版是否纳入 NTFS 之外的文件系统深度支持优先级
- 浏览器痕迹是否进入 MVP 第一批还是延后
- 报告是否需要 PDF 原生导出，还是先通过 HTML 转换