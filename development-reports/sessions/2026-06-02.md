# 2026-06-02 开发记录

## v2.1 Backlog 多 Agent 集成复核

**时间戳**: 2026-06-02 01:13:05 +08:00  
**署名**: Codex  
**基线**: `30d67bf [pi] Work in progress`

### 开发前文档复核

- 已读取并复核 `docs/remediation-plan-v2.1.md`、`docs/architecture-model.md`、`docs/algorithm-complexity-analysis.md`、`development-reports/sessions/2026-06-01.md` 和 `docs/开发记录.md`。
- 复核结论：四个 subagents 的实现已进入当前 HEAD；但 `docs/remediation-plan-v2.1.md` 仍把 Analysis provenance、Reports provenance、Job partial、Dependency gate、SBOM 和 tiny fixture 标成旧的 Partial/Remaining 状态。本轮已同步文档，保留真实 Registry/EVTX parser 与连续 media streaming 为剩余风险。

### 主线程统一审阅结论

| Stage | 审阅结果 | 说明 |
|---|---|---|
| Stage 1 Analysis / Artifact Parser | Accepted with known parser limits | `AnalysisProvenanceDto` 已覆盖 system info、boot records、classification 与 classified file；Registry 只做 hive discovery + `regf` header verification，不解析 key/value；EVTX 为正式 `notParsed/unavailable` adapter，不生成 boot/shutdown 假时间 |
| Stage 2 Frontend Analysis / Reports | Accepted | DataAnalysis 展示 status/warnings/provenance；前端类型、mock、hooks 与 API wrapper 同步；Reports HTML/CSV/JSON 输出 analysis summary/provenance，保留 HTML escaping 与 CSV formula sanitization |
| Stage 3 Viewer / Streaming | Accepted with fallback limit | 大媒体不暴露 host absolute path，使用 opaque handle + bounded `read_media_range`；当前仍是首 chunk/range fallback，不是完整 continuous streaming protocol |
| Stage 4 Jobs / Events | Accepted | `JobSnapshotDto`、SQLite migration、JobRepo/JobService、import/search/artifact stats 与 BottomDrawer UI 已同步 `warning/skipped/failed/partial` |
| Stage 5 CI / Fixture | Accepted with tiny E01 gap | CI 增加 `cargo deny`、`cargo audit`、`pnpm audit` 与 CycloneDX backend/frontend SBOM；仓库已有 tiny logical/RAW fixture 与 helper；真实 E01 改为 `FORENSICS_E01_FIXTURE` opt-in ignored slow tests，tiny E01 未入库 |
| Stage 6 Docs / Rustdoc | Accepted | `cargo doc --workspace --no-deps` 已通过；本轮修复 rustdoc bare URL / invalid link warnings，并更新 remediation/architecture/complexity 状态 |

### 本轮收尾改动

- 将 `frontend/package.json` 中 `vite` 升级到 `6.4.2`、`vitest` 升级到 `^4.1.0`，并同步 `pnpm.overrides.vite = 6.4.2` 和 lockfile，修复 `pnpm audit --audit-level high` 报告的 Vite/Vitest high/critical advisory。
- 清理本地 CycloneDX 验证生成的 `backend-sbom.json` 散落文件；这些文件是 CI artifact，不应提交到仓库源码树。
- 修复 rustdoc 文案中的裸 URL / 误识别 intra-doc link，确保 `cargo doc --workspace --no-deps` 无 warnings。
- 更新 `docs/remediation-plan-v2.1.md`、`docs/architecture-model.md`、`docs/algorithm-complexity-analysis.md`，把已完成项和剩余风险改为当前真实状态。

### 验证结果

- `cargo fmt --all -- --check`: 通过。
- `cargo clippy --workspace --all-targets -- -D warnings`: 通过。
- `cargo test --workspace`: 通过；真实 E01 tests 默认 ignored，需 `FORENSICS_E01_FIXTURE` 手动运行。
- `cargo build -p forensics-desktop`: 通过。
- `powershell -ExecutionPolicy Bypass -File scripts\check-release-guard.ps1`: 通过，`Release guard passed`。
- `cargo doc --workspace --no-deps`: 通过。
- `cargo deny check advisories bans licenses sources`: 通过；仍有 duplicate dependency warnings，属于后续依赖图治理。
- `cargo audit`: 通过，退出码 0；报告 19 个 allowed warning-class advisories。
- `pnpm --dir frontend typecheck`: 通过。
- `pnpm --dir frontend lint`: 通过，0 error，7 个既有 warning（MCP/DenseDataTable/SyntaxHighlighter/tauri-bridge）。
- `pnpm --dir frontend test`: 通过，13 个测试文件 / 51 个测试。ErrorBoundary 用例会打印预期的 `Test error` stack，退出码为 0。
- `pnpm --dir frontend build`: 通过；Vite 仍提示主 chunk 约 515KB。
- `pnpm --dir frontend audit --audit-level high`: 通过，无已知 high/critical 漏洞。
- `cargo test -p image-e01 --test e01_regression_test`: 通过，2 passed / 5 ignored；私有 E01 样本测试已改为 ignored opt-in。
- `cargo test -p image-e01 --test e01_dump`: 通过，0 passed / 2 ignored。
- `git diff --check`: 通过。
- 统一 grep 复核：页面/feature/API 中 Tauri `invoke` 只存在于 `frontend/src/lib/api/client.ts`；`crates apps frontend/src` 中不再存在 `E:/pangushi` / `liuyang_pc` 私有样本路径。

### 剩余风险

- Registry parser 尚未解析真实 key/value，不能输出 hostname、Windows version/build、timezone 等字段。
- EVTX boot/shutdown parser 尚未接入，当前只返回明确的 unavailable/notParsed provenance。
- 大媒体仍不是连续 streaming protocol，只是安全的 scoped range fallback。
- tiny E01 fixture 尚未入库；真实样本慢测仍依赖 `FORENSICS_E01_FIXTURE`。
- `cargo audit` 仍会报告若干 warning-class transitive advisories，短期由 `deny.toml` 例外追踪，后续应通过升级 Tauri/wry/Tantivy 等依赖消除。
