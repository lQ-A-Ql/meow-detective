# 2026-06-02 开发记录

## v2.1 Backlog 多 Agent 集成复核

**时间戳**: 2026-06-02 01:13:05 +08:00  
**署名**: Codex  
**基线**: `30d67bf [pi] Work in progress`

### 开发前文档复核

- 已读取并复核 `docs/remediation-plan-v2.1.md`、`docs/architecture-model.md`、`docs/algorithm-complexity-analysis.md`、`development-reports/sessions/2026-06-01.md` 和 `docs/开发记录.md`。
- 复核结论：四个 subagents 的实现已进入当前 HEAD；但 `docs/remediation-plan-v2.1.md` 仍把 Analysis provenance、Reports provenance、Job partial、Dependency gate、SBOM 和 tiny fixture 标成旧的 Partial/Remaining 状态。本轮已同步文档，保留真实 Registry/EVTX parser 与连续 media streaming 为剩余风险。

### 主线程统一审阅结论

| Stage | 审阅结果 | 说明 |
|---|---|---|
| Stage 1 Analysis / Artifact Parser | Accepted with known parser limits | `AnalysisProvenanceDto` 已覆盖 system info、boot records、classification 与 classified file；Registry 只做 hive discovery + `regf` header verification，不解析 key/value；EVTX 为正式 `notParsed/unavailable` adapter，不生成 boot/shutdown 假时间 |
| Stage 2 Frontend Analysis / Reports | Accepted | DataAnalysis 展示 status/warnings/provenance；前端类型、mock、hooks 与 API wrapper 同步；Reports HTML/CSV/JSON 输出 analysis summary/provenance，保留 HTML escaping 与 CSV formula sanitization |
| Stage 3 Viewer / Streaming | Accepted with fallback limit | 大媒体不暴露 host absolute path，使用 opaque handle + bounded `read_media_range`；当前仍是首 chunk/range fallback，不是完整 continuous streaming protocol |
| Stage 4 Jobs / Events | Accepted | `JobSnapshotDto`、SQLite migration、JobRepo/JobService、import/search/artifact stats 与 BottomDrawer UI 已同步 `warning/skipped/failed/partial` |
| Stage 5 CI / Fixture | Accepted with tiny E01 gap | CI 增加 `cargo deny`、`cargo audit`、`pnpm audit` 与 CycloneDX backend/frontend SBOM；仓库已有 tiny logical/RAW fixture 与 helper；真实 E01 改为 `FORENSICS_E01_FIXTURE` opt-in ignored slow tests，tiny E01 未入库 |
| Stage 6 Docs / Rustdoc | Accepted | `cargo doc --workspace --no-deps` 已通过；本轮修复 rustdoc bare URL / invalid link warnings，并更新 remediation/architecture/complexity 状态 |

### 本轮收尾改动

- 将 `frontend/package.json` 中 `vite` 升级到 `6.4.2`、`vitest` 升级到 `^4.1.0`，并同步 `pnpm.overrides.vite = 6.4.2` 和 lockfile，修复 `pnpm audit --audit-level high` 报告的 Vite/Vitest high/critical advisory。
- 清理本地 CycloneDX 验证生成的 `backend-sbom.json` 散落文件；这些文件是 CI artifact，不应提交到仓库源码树。
- 修复 rustdoc 文案中的裸 URL / 误识别 intra-doc link，确保 `cargo doc --workspace --no-deps` 无 warnings。
- 更新 `docs/remediation-plan-v2.1.md`、`docs/architecture-model.md`、`docs/algorithm-complexity-analysis.md`，把已完成项和剩余风险改为当前真实状态。

### 验证结果

- `cargo fmt --all -- --check`: 通过。
- `cargo clippy --workspace --all-targets -- -D warnings`: 通过。
- `cargo test --workspace`: 通过；真实 E01 tests 默认 ignored，需 `FORENSICS_E01_FIXTURE` 手动运行。
- `cargo build -p forensics-desktop`: 通过。
- `powershell -ExecutionPolicy Bypass -File scripts\check-release-guard.ps1`: 通过，`Release guard passed`。
- `cargo doc --workspace --no-deps`: 通过。
- `cargo deny check advisories bans licenses sources`: 通过；仍有 duplicate dependency warnings，属于后续依赖图治理。
- `cargo audit`: 通过，退出码 0；报告 19 个 allowed warning-class advisories。
- `pnpm --dir frontend typecheck`: 通过。
- `pnpm --dir frontend lint`: 通过，0 error，7 个既有 warning（MCP/DenseDataTable/SyntaxHighlighter/tauri-bridge）。
- `pnpm --dir frontend test`: 通过，13 个测试文件 / 51 个测试。ErrorBoundary 用例会打印预期的 `Test error` stack，退出码为 0。
- `pnpm --dir frontend build`: 通过；Vite 仍提示主 chunk 约 515KB。
- `pnpm --dir frontend audit --audit-level high`: 通过，无已知 high/critical 漏洞。
- `cargo test -p image-e01 --test e01_regression_test`: 通过，2 passed / 5 ignored；私有 E01 样本测试已改为 ignored opt-in。
- `cargo test -p image-e01 --test e01_dump`: 通过，0 passed / 2 ignored。
- `git diff --check`: 通过。
- 统一 grep 复核：页面/feature/API 中 Tauri `invoke` 只存在于 `frontend/src/lib/api/client.ts`；`crates apps frontend/src` 中不再存在 `E:/pangushi` / `liuyang_pc` 私有样本路径。

### 剩余风险

> 注：以下为 2026-06-02 01:13:05 统一复核时点的风险列表；同日 02:35:01 的“可信 Beta 收口执行”已将 Registry 定向 parser、EVTX candidate adapter 和 `evidence-media` protocol 推进到新的当前状态。

- Registry parser 尚未解析真实 key/value，不能输出 hostname、Windows version/build、timezone 等字段。
- EVTX boot/shutdown parser 尚未接入，当前只返回明确的 unavailable/notParsed provenance。
- 大媒体仍不是连续 streaming protocol，只是安全的 scoped range fallback。
- tiny E01 fixture 尚未入库；真实样本慢测仍依赖 `FORENSICS_E01_FIXTURE`。
- `cargo audit` 仍会报告若干 warning-class transitive advisories，短期由 `deny.toml` 例外追踪，后续应通过升级 Tauri/wry/Tantivy 等依赖消除。

---

## 可信 Beta 收口执行

**时间戳**: 2026-06-02 02:35:01 +08:00
**署名**: Codex
**基线**: `e7ffa35 fix: finalize v2.1 integration review` -> `codex/beta-forensics-backlog`

### 开发前文档复核

- 已读取 `docs/remediation-plan-v2.1.md`、`docs/architecture-model.md`、`docs/algorithm-complexity-analysis.md`、`docs/开发记录.md` 和本文件。
- 复核结论：上一轮文档准确记录了 media/Registry/EVTX 仍为主要剩余项。本轮将这些项从 fallback/stub 推进到可验证 beta：大媒体接入 `evidence-media` protocol，Registry 接入定向字段 parser，EVTX 接入 boot/shutdown candidate adapter。
- 未启动新的 subagents；因此没有闲置 agent 需要关闭。

### 本轮改动摘要

- 新增 `apps/desktop/src-tauri/src/media_protocol.rs`，注册 `evidence-media://handle/<encoded>` Tauri protocol；Range parser 支持 `bytes=start-end`、`bytes=start-`、`bytes=-suffix` 和 no Range bounded 206，每次最多读取 1MB，invalid range 返回 416。
- `get_media_url` 对大媒体返回 `MediaPreviewModeDto::Protocol`、opaque handle 和 protocol URL；小媒体继续返回 bounded inline data URL；`read_media_range` command 保留为 fallback。CSP 只在 `media-src` 允许 `evidence-media:`，不新增宽泛 asset scope。
- 新增 `artifacts-windows::registry::lookup` 定向 Registry reader，bounded 读取最多 64MB，解析 `regf` base block、NK/VK、`lf/lh/li/ri` 子键列表和 `REG_SZ` / `REG_EXPAND_SZ` / `REG_DWORD` / `REG_QWORD` / `REG_MULTI_SZ`；Analysis 从 SYSTEM/SOFTWARE 提取 hostname、timezone、ProductName、build、install date、owner/org/product id，并输出字段级 provenance。
- 新增 `artifacts-windows::evtx` adapter，使用 `evtx` crate bounded 解析 System.evtx 中的 6005、6006、6008、1074 候选事件；Analysis boot records 带 event id、record id、note 和 provenance，并明确这些是 EventLog/User32 candidates，不是绝对开关机事实。
- DataAnalysis、mock data、frontend models 和 reports 同步新增 field provenance、EVTX event fields 和 parser/source 展示；HTML escaping 与 CSV formula sanitization 回归测试保留。
- `deny.toml` 新增 `RUSTSEC-2021-0153` 临时例外，跟踪 `evtx -> encoding` unmaintained transitive advisory，owner/reason/expiry 均已记录。
- 更新 `docs/remediation-plan-v2.1.md`、`docs/architecture-model.md`、`docs/algorithm-complexity-analysis.md`，修正旧的 Partial/stub 状态。

### 验证结果

- `cargo fmt --all -- --check`: 通过。
- `cargo clippy --workspace --all-targets -- -D warnings`: 通过。
- `cargo test --workspace`: 通过；真实 E01 tests 默认 ignored，需 `FORENSICS_E01_FIXTURE` 手动运行。
- `cargo test -p app-services --lib analysis_service`: 通过，9 tests。
- `cargo test -p app-services --lib report_service`: 通过，4 tests。
- `cargo test -p artifacts-windows registry`: 通过，registry lookup/parser targeted tests。
- `cargo test -p artifacts-windows evtx`: 通过，5 targeted tests。
- `cargo test -p transport analysis`: 通过，3 tests。
- `pnpm --dir frontend typecheck`: 通过。
- `pnpm --dir frontend lint`: 通过，0 error，7 个既有 warning（MCP/DenseDataTable/SyntaxHighlighter/tauri-bridge）。
- `pnpm --dir frontend test -- analysis files`: 通过，4 files / 20 tests。
- `pnpm --dir frontend test`: 通过，13 files / 53 tests；ErrorBoundary 用例仍打印预期 jsdom stack，退出码 0。
- `pnpm --dir frontend build`: 通过；Vite chunk size warning 存在。
- `cargo build -p forensics-desktop`: 通过。
- `powershell -ExecutionPolicy Bypass -File scripts\check-release-guard.ps1`: 通过。
- `cargo doc --workspace --no-deps`: 通过。
- `cargo deny check advisories bans licenses sources`: 通过；仍有 duplicate dependency warnings；`RUSTSEC-2021-0153` 作为到期例外跟踪。
- `cargo audit`: 通过，退出码 0；报告 20 个 warning-class advisories，包括本轮 `encoding`。
- `pnpm --dir frontend audit --audit-level high`: 通过，No known vulnerabilities。
- Slow E01 manual gate 未运行：当前环境未设置 `FORENSICS_E01_FIXTURE`。运行条件为提供合法真实 E01 样本后执行 `cargo test -p app-services --test e01_full_pipeline_test -- --ignored --nocapture` 和 `cargo test -p app-services --test e01_mft_scan_test -- --ignored --nocapture`。

### 剩余风险

- EVTX adapter 尚缺仓库内合法 tiny real `.evtx` fixture；当前单测覆盖 JSON record extraction、malformed 和 oversized path，但真实 parser path 仍需 fixture 补强。
- Registry parser 是 Analysis 所需定向字段 parser，不是完整 hive browser；后续扩展 hive traversal 前需要重新评估 cell graph 覆盖和复杂度。
- `evidence-media` 已编译和单测通过，但尚未做 Windows 桌面 WebView2 播放器 seek smoke。
- `evtx -> encoding` unmaintained advisory 是临时例外，2026-09-01 前需要替换 parser path、升级依赖或重新评审。
- tiny E01 fixture 尚未入库；真实样本慢测仍依赖 `FORENSICS_E01_FIXTURE`。

---

## EVTX fixture/dependency 风险收束

**时间戳**: 2026-06-02 02:47:09 +08:00
**署名**: Codex
**基线**: `7d12299 feat: add beta media and analysis parsers`

### 开发前文档复核

- 已读取 `docs/remediation-plan-v2.1.md`、`docs/开发记录.md` 和本 session 记录。
- 复核结论：上一轮已把 media/Registry/EVTX 推进到 beta 状态，当前最小高价值闭环是把 EVTX fixture 缺口和 `encoding` advisory 依赖事实固定下来，避免后续把它误判为 feature 配置遗漏。
- 未启动新的 subagents；因此没有闲置 agent 需要关闭。

### 本轮改动摘要

- 新增 EVTX parser truncated magic 回归：输入仅包含 `ElfFile\0` 时必须返回 warning 且不 panic，补强 malformed path 覆盖。
- 本地复核 `evtx 0.11.2` crates.io package：`Cargo.toml.orig` 声明 `exclude = ["**/*.evtx", "**/*.dat"]`，发布包不含可直接复用的真实 `.evtx` fixture；license 为 `MIT/Apache-2.0`。
- 本地复核依赖链：`encoding v0.2.33 -> evtx v0.11.2 -> artifacts-windows`，且 `encoding = "0.2.33"` 是 `evtx` 直接非可选依赖，不能通过关闭 feature 消除 `RUSTSEC-2021-0153`。
- 更新 `docs/remediation-plan-v2.1.md` 和 `docs/开发记录.md`，明确可选后续路线：生成/引入可再分发 EVTX fixture、替换 parser、vendor/patch parser，或在 2026-09-01 前重新评审 `deny.toml` 例外。

### 验证结果

- `cargo test -p artifacts-windows evtx`: 通过，6 个 EVTX targeted tests。
- `cargo fmt --all -- --check`: 通过。
- `cargo clippy --workspace --all-targets -- -D warnings`: 通过。
- `git diff --check`: 通过。

### 剩余风险

- 真实 EVTX parser path 仍缺合法 tiny real `.evtx` fixture。
- `evtx -> encoding` unmaintained advisory 仍是到期例外，需在 2026-09-01 前处理。

---

## Dependency exception guard

**时间戳**: 2026-06-02 02:52:17 +08:00
**署名**: Codex
**基线**: `21f1546 docs: record evtx dependency limits`

### 本轮改动摘要

- 新增 `scripts/check-deny-exceptions.ps1`，扫描 `deny.toml` 的 `RUSTSEC-*` advisory ignore 项，要求每条例外含 `owner:`、`expires: YYYY-MM-DD` 和说明文本；过期例外直接失败。
- 将该脚本接入 `.github/workflows/ci-backend.yml`，在 release guard 后、`cargo deny` 前执行。
- 更新 `docs/remediation-plan-v2.1.md` 与 `docs/开发记录.md`，把 dependency exception guard 纳入 Phase 5.2 dependency gate 证据。
- 未启动新的 subagents；因此没有闲置 agent 需要关闭。

### 验证结果

- `powershell -ExecutionPolicy Bypass -File scripts\check-deny-exceptions.ps1`: 通过，18 条 advisory exceptions checked。
- `powershell -ExecutionPolicy Bypass -File scripts\check-release-guard.ps1`: 通过。
- `cargo fmt --all -- --check`: 通过。
- `cargo clippy --workspace --all-targets -- -D warnings`: 通过。
- `git diff --check`: 通过。

### 剩余风险

- Guard 只防止依赖例外元数据缺失或过期，不消除 advisory 本身。
- `evtx -> encoding` 和其他 upstream transitive advisories 仍需按 expiry 前升级、替换或重新评审。

---

## Settings config closure

**时间戳**: 2026-06-02 02:58:33 +08:00
**署名**: Codex
**基线**: `14fd45f ci: guard dependency exceptions`

### 本轮改动摘要

- 复核 Settings 实现，确认路径类设置已经通过后端 `AppSettingsDto` config 持久化和校验；theme/dev event trace 同步写入后端 DTO，并镜像 localStorage 用于 mock/dev 即时生效。
- 给 Settings 页面案件目录和镜像搜索路径输入补显式 label，改善可访问性。
- 新增 `frontend/src/app/pages/Settings.test.tsx`，覆盖远端配置加载、保存 API payload、localStorage mirror、主题应用、mock/local fallback 和非法路径拒绝。
- 更新 `docs/remediation-plan-v2.1.md` 和 `docs/开发记录.md`，将 Phase 3.8 从 Partial 收口为 Completed。
- 未启动新的 subagents；因此没有闲置 agent 需要关闭。

### 验证结果

- `pnpm --dir frontend test -- Settings`: 通过，3 files / 11 tests。
- `pnpm --dir frontend typecheck`: 通过。
- `pnpm --dir frontend lint`: 通过，0 error / 7 existing warnings。
- `git diff --check`: 通过。

### 剩余风险

- Settings 的 localStorage mirror 是前端 mock/dev 体验设计，不替代后端 config；路径类设置仍以后端校验和持久化为准。

---

## Event bridge closure

**时间戳**: 2026-06-02 03:05:16 +08:00
**署名**: Codex
**基线**: `4acfe79 test: cover settings config flow`

### 本轮改动摘要

- 新增 transport typed topic `data-source-imported` 与 `job-cancelled`，并同步 serde/as_str 测试。
- `event_bridge` 新增 `emit_data_source_imported()` 与 `emit_job_cancelled()`；data source imported payload 不包含宿主证据路径，只包含 `dataSourceId`、`name`、`kind`、`jobId`。
- Import 完成 finalizing 阶段发 `data-source-imported`；`cancel_import` 成功请求取消时发 `job-cancelled`。
- 前端 `EventTopic` union 和 Tauri bridge 监听列表同步新增 topic；新增 EventBus typed topic test。
- 更新 `docs/remediation-plan-v2.1.md` 与 `docs/开发记录.md`，将 Phase 4.2 收口为 Completed。
- 未启动新的 subagents；因此没有闲置 agent 需要关闭。

### 验证结果

- `cargo test -p transport events`: 通过。
- `cargo clippy -p forensics-desktop --all-targets -- -D warnings`: 通过。
- `pnpm --dir frontend test -- events`: 通过，1 file / 1 test。
- `pnpm --dir frontend typecheck`: 通过。
- `cargo fmt --all -- --check`: 通过。

### 剩余风险

- `job-cancelled` 当前是事件层语义；job snapshot 的最终 status 仍保持现有兼容模型，未新增 cancelled status/migration。

---

## Command SQL boundary guard

**时间戳**: 2026-06-02 03:10:13 +08:00
**署名**: Codex
**基线**: `668904c feat: complete event bridge topics`

### 本轮改动摘要

- 复核 `apps/desktop/src-tauri/src/commands`，当前无原始 SQL 语句或 `rusqlite::params!/prepare/execute` 低层调用。
- 新增 `scripts/check-command-sql-boundary.ps1`，拦截 Tauri command 层中的显式 SQL 和底层 rusqlite SQL API，防止业务 SQL 回流 command layer。
- 将该 guard 接入 `.github/workflows/ci-backend.yml`。
- 更新 `docs/remediation-plan-v2.1.md` 与 `docs/开发记录.md`，将 Phase 6.2 收口为 Completed / guarded。
- 未启动新的 subagents；因此没有闲置 agent 需要关闭。

### 验证结果

- `powershell -ExecutionPolicy Bypass -File scripts\check-command-sql-boundary.ps1`: 通过。
- `powershell -ExecutionPolicy Bypass -File scripts\check-release-guard.ps1`: 通过。
- `powershell -ExecutionPolicy Bypass -File scripts\check-deny-exceptions.ps1`: 通过。
- `cargo fmt --all -- --check`: 通过。
- `git diff --check`: 通过。

### 剩余风险

- Guard 约束 command layer 边界，不替代 repository/service 层 SQL 测试。

---

## Beta final gate and review closeout

**时间戳**: 2026-06-02 03:19:24 +08:00
**署名**: Codex
**基线**: `af2833c ci: guard command sql boundary`

### 本轮改动摘要

- 未启动新的 subagents，因此没有闲置 agent 需要关闭。
- 复核当前分支 `codex/beta-forensics-backlog`，工作树仅保留用户侧未跟踪 `.context/plans.md`。
- 重新读取 `docs/remediation-plan-v2.1.md`、`docs/architecture-model.md`、`docs/algorithm-complexity-analysis.md`、`docs/开发记录.md` 和本 session 记录。
- 补跑可信 beta final gate，完成统一 grep 审阅，并更新 `docs/remediation-plan-v2.1.md` / `docs/开发记录.md` 的最终测试计数与剩余风险。

### 验证结果

- `cargo fmt --all -- --check`: 通过。
- `cargo clippy --workspace --all-targets -- -D warnings`: 通过。
- `cargo test --workspace`: 通过；真实 E01 tests 默认 ignored。
- `cargo build -p forensics-desktop`: 通过。
- `cargo doc --workspace --no-deps`: 通过。
- `powershell -ExecutionPolicy Bypass -File scripts\check-release-guard.ps1`: 通过。
- `powershell -ExecutionPolicy Bypass -File scripts\check-command-sql-boundary.ps1`: 通过。
- `powershell -ExecutionPolicy Bypass -File scripts\check-deny-exceptions.ps1`: 通过，18 条 advisory exceptions checked。
- `cargo deny check advisories bans licenses sources`: 通过；仍有 duplicate dependency warnings。
- `cargo audit`: 通过，退出码 0；报告 20 个 allowed warning-class advisories。
- `pnpm --dir frontend typecheck`: 通过。
- `pnpm --dir frontend lint`: 通过，0 error / 7 existing warnings。
- `pnpm --dir frontend test`: 通过，15 files / 58 tests；ErrorBoundary 用例打印预期 jsdom stack。
- `pnpm --dir frontend build`: 通过；Vite 主 chunk 约 520KB，仍提示 chunk size warning。
- `pnpm --dir frontend audit --audit-level high`: 通过，无已知漏洞。
- `git diff --check`: 通过。

### 统一审阅

- 伪取证事实 grep 命中只存在于审计文档、负向测试或时间戳单测注释；业务代码和 UI 不输出 `FORENSICS-PC`、默认 `Windows 10`、默认 `19045` 或固定 boot timestamp。
- `@tauri-apps/api/core` / `invoke` 只存在于 `frontend/src/lib/api/client.ts`，页面和 feature hooks 没有绕过 `apiClient.request(...)`。
- 事件 bridge 的 data source imported payload 不含宿主证据路径；media preview/range 路径仍走 opaque handle 和 reader helper。
- Analysis/media/extract file 检查范围内未发现 `case_root.join` 或无界 `std::fs::read` 读取证据内容的回退；仅测试断言读取导出的 destination 文件。

### 剩余风险

- EVTX adapter 仍缺合法 tiny real `.evtx` fixture。
- Registry parser 是 Analysis 定向字段 parser，不是完整 hive browser。
- `evidence-media://` protocol 尚未做真实 Windows WebView2 桌面 seek smoke。
- `evtx -> encoding` advisory 例外需在 2026-09-01 前处理。
- tiny E01 fixture 尚未入库；slow E01 gate 未运行，因为当前环境未设置 `FORENSICS_E01_FIXTURE`。

---

## FS reader helper dedupe

**时间戳**: 2026-06-02 03:26:01 +08:00
**署名**: Codex
**基线**: `ad4ccd2 docs: record beta final gate`

### 本轮改动摘要

- 未启动新的 subagents，因此没有闲置 agent 需要关闭。
- 复核剩余 backlog 后选择本地可验证的 Phase 6.1 子项，避开需要外部样本或人工 smoke 的 EVTX/tiny E01/media protocol 项。
- 在 `evidence-core::filesystem` 新增公共 helper：root node、path join、常见 path/file not-found 和 directory/not-directory error。
- `fs-fat`、`fs-ntfs`、`fs-exfat` 改为复用公共 helper，删除重复 root node 构造和本地 path join/error 构造。FAT/NTFS 继续输出 slash-separated path；exFAT 继续输出 backslash-separated path。
- 更新 `docs/remediation-plan-v2.1.md` 和 `docs/开发记录.md`，把 Phase 6.1 标为 Partial，明确本轮不是完整 FS 枚举流程 refactor。

### 验证结果

- `cargo fmt --all -- --check`: 通过。
- `cargo test -p evidence-core filesystem`: 通过。
- `cargo test -p fs-fat`: 通过。
- `cargo test -p fs-exfat`: 通过。
- `cargo test -p fs-ntfs`: 通过。
- `cargo test -p app-services file_service`: 通过。
- `cargo test -p app-services --test file_service_real_test`: 通过。
- `cargo clippy --workspace --all-targets -- -D warnings`: 通过。

### 剩余风险

- FAT/NTFS/exFAT 深层枚举流程和 FS 特定错误转换仍可继续去重，但需要更大行为回归面。
- 本轮保持 public API、root semantics、排序和 path separator convention 不变。

---

## Frontend lint warning cleanup

**时间戳**: 2026-06-02 10:23:41 +08:00
**署名**: Codex
**基线**: `c32febf refactor: share filesystem path helpers`

### 本轮改动摘要

- 未启动新的 subagents，因此没有闲置 agent 需要关闭。
- 工作树仅保留用户侧未跟踪 `.context/plans.md`；本轮未 stage、未删除该文件。
- 清理前端既有 lint warning：未使用 import、`any`、未使用 highlighter prop、未使用 event bridge type import。
- `DenseDataTable` 的 memo row 保持泛型签名，避免用 `any` 弱化表格行类型。
- 更新 `docs/remediation-plan-v2.1.md`，把当前 frontend lint gate 标为 `0 error / 0 warnings`。

### 验证结果

- `pnpm --dir frontend lint`: 通过，0 error / 0 warnings。
- `pnpm --dir frontend typecheck`: 通过。
- `pnpm --dir frontend test`: 通过，15 files / 58 tests；ErrorBoundary 用例打印预期 jsdom stack。
- `pnpm --dir frontend build`: 通过；仍提示既有 Vite chunk size warning，主 chunk 约 520KB。

### 剩余风险

- Vite chunk size warning 尚未处理；建议作为后续独立小闭环，通过路由级 code splitting 或 `manualChunks` 验证。

---

## Frontend route-level code splitting

**时间戳**: 2026-06-02 10:27:03 +08:00
**署名**: Codex
**基线**: `890271c chore: clear frontend lint warnings`

### 本轮改动摘要

- 未启动新的 subagents，因此没有闲置 agent 需要关闭。
- 工作树仅保留用户侧未跟踪 `.context/plans.md`；本轮未 stage、未删除该文件。
- 将 `frontend/src/app/routes.tsx` 中页面级静态 imports 改为 React Router `lazy` route imports。
- 保留 `Layout` 静态导入，避免改变 app shell 结构；CaseHome、Analysis、Files、Search、Timeline、Artifacts、Reports、Settings 按路由拆包。
- 更新 `docs/remediation-plan-v2.1.md`，把当前 frontend build gate 标为无 chunk size warning，主 chunk 约 315KB。

### 验证结果

- `pnpm --dir frontend typecheck`: 通过。
- `pnpm --dir frontend lint`: 通过，0 error / 0 warnings。
- `pnpm --dir frontend test`: 通过，15 files / 58 tests；ErrorBoundary 用例打印预期 jsdom stack。
- `pnpm --dir frontend build`: 通过；无 Vite chunk size warning，主 chunk 约 315KB，页面 chunks 独立输出。

### 剩余风险

- 本轮没有做 vendor `manualChunks`；当前 build 已低于 Vite 默认 warning 阈值，后续依赖增长时再专项处理。

---

## Route splitting regression guard

**时间戳**: 2026-06-02 10:30:21 +08:00
**署名**: Codex
**基线**: `032902a perf: split frontend routes lazily`

### 本轮改动摘要

- 未启动新的 subagents，因此没有闲置 agent 需要关闭。
- 工作树仅保留用户侧未跟踪 `.context/plans.md`；本轮未 stage、未删除该文件。
- `frontend/src/app/routes.tsx` 导出 `appRoutes`，新增 `frontend/src/app/routes.test.ts`，断言 root layout 静态加载、页面 child routes 全部 lazy-loaded。
- `frontend/src/stores/ui-store.ts` 增加 safe localStorage read/write helper，避免 route config 测试导入 app shell 时因 storage API 不完整而崩溃；浏览器持久化语义保持不变。
- 更新 `docs/remediation-plan-v2.1.md`，把当前 frontend test gate 计数更新为 16 files / 59 tests。

### 验证结果

- `pnpm --dir frontend test -- routes`: 通过，1 file / 1 test；命令输出 Node `--localstorage-file` warning，但退出码为 0。
- `pnpm --dir frontend test`: 通过，16 files / 59 tests；ErrorBoundary 用例打印预期 jsdom stack。
- `pnpm --dir frontend typecheck`: 通过。
- `pnpm --dir frontend lint`: 通过，0 error / 0 warnings。
- `pnpm --dir frontend build`: 通过；无 Vite chunk size warning，主 chunk 约 315KB。

### 剩余风险

- route-level lazy split 已有防回退测试；更细的 vendor/common chunk 优化仍可作为后续性能专项。

---

## Tiny E01 reader fixture

**时间戳**: 2026-06-02 10:39:22 +08:00
**署名**: Codex
**基线**: `fd7c29f test: guard lazy route splitting`

### 本轮改动摘要

- 未启动新的 subagents，因此没有闲置 agent 需要关闭。
- 工作树仅保留用户侧未跟踪 `.context/plans.md`；本轮未 stage、未删除该文件。
- `scripts/generate-tiny-fixtures.ps1` 现在同时生成 RAW 和 deterministic synthetic single-segment E01 tiny fixtures。
- 新增 `testdata/fixtures/tiny/e01/tiny.E01`，大小 4405 bytes，用于默认 CI 覆盖 `image-e01` reader section/table/read/seek。
- `crates/testing::fixtures` 新增 `tiny_e01_image()`，`crates/image-e01` 默认 regression tests 使用该 fixture 读取 marker、signature、seek/end-of-file。
- 文档同步更新：tiny E01 是 reader fixture，不是完整 filesystem image；真实 E01 slow/manual gate 仍依赖 `FORENSICS_E01_FIXTURE`。

### 验证结果

- `powershell -ExecutionPolicy Bypass -File scripts\generate-tiny-fixtures.ps1`: 通过，生成 RAW 1024 bytes 和 E01 4405 bytes。
- `cargo test -p image-e01`: 通过；6 lib tests，4 default regression tests，7 ignored real-E01 dump/slow tests。
- `cargo test -p testing`: 通过，4 fixture helper tests。
- `cargo fmt --all -- --check`: 通过。
- `cargo clippy -p image-e01 --all-targets -- -D warnings`: 通过。

### 剩余风险

- `testdata/fixtures/tiny/e01/tiny.E01` 不能替代真实 E01 partition/filesystem/import 慢测；这些仍需 `FORENSICS_E01_FIXTURE`。
- EVTX adapter 仍缺合法 tiny real `.evtx` fixture。

---

## Coverage reporting gate

**时间戳**: 2026-06-02 10:56:14 +08:00
**署名**: Codex
**基线**: `e3692f2 test: add tiny e01 fixture`

### 本轮改动摘要

- 未启动新的 subagents，因此没有闲置 agent 需要关闭。
- 工作树仅保留用户侧未跟踪 `.context/plans.md`；本轮未 stage、未删除该文件。
- 为剩余 Phase 5.6 coverage 子项新增可执行入口：前端 `test:coverage`、`@vitest/coverage-v8`、统一 `scripts/run-coverage.ps1`。
- 后端 CI 新增 `cargo-llvm-cov` LCOV 生成和 `backend-coverage` artifact；前端 CI 新增 `frontend-coverage` artifact。
- `.gitignore` 忽略根目录 `coverage/` 和 `frontend/coverage/`；`scripts/README.md` 增加 coverage 用法。
- 文档更新为“coverage report 已落地，阈值待稳定 baseline 后再定”，避免把报告入口夸大为严格覆盖率门禁。

### 验证结果

- `pnpm --dir frontend test:coverage`: 通过；16 个测试文件 / 59 个测试，生成 `frontend/coverage/coverage-summary.json`、`lcov.info`，当前总行覆盖率约 47.97%。
- `powershell -ExecutionPolicy Bypass -File scripts\run-coverage.ps1 -Frontend`: 通过；统一 runner 的前端 coverage 路径可用。
- `pnpm --dir frontend typecheck`: 通过。
- `pnpm --dir frontend lint`: 通过，0 error / 0 warnings。
- `pnpm --dir frontend test`: 通过；16 个测试文件 / 59 个测试，ErrorBoundary 用例打印预期 jsdom stack，退出码为 0。
- `pnpm --dir frontend build`: 通过；无 Vite chunk size warning，主 chunk 约 315KB。
- `pnpm --dir frontend audit --audit-level high`: 通过，无已知漏洞。
- `pnpm --dir frontend install --frozen-lockfile`: 通过。
- `powershell -ExecutionPolicy Bypass -File scripts\run-coverage.ps1 -Rust`: 通过；本机未安装 `cargo-llvm-cov`，按设计 warning 后跳过 Rust coverage。
- `cargo fmt --all -- --check`: 通过。
- `git diff --check`: 通过。

### 剩余风险

- Coverage 当前只作为 CI artifact，不设置百分比阈值。
- Rust coverage 需要 `cargo-llvm-cov` 和 `llvm-tools-preview`；CI 会安装，本地脚本默认缺工具时 warning 跳过。
- EVTX 合法 tiny real `.evtx` fixture、Windows WebView2 media smoke、`evtx -> encoding` advisory cleanup 仍未完成。

---

## Frontend coverage baseline threshold

**时间戳**: 2026-06-02 11:06:05 +08:00
**署名**: Codex
**基线**: `7b42fac ci: add coverage report artifacts`

### 本轮改动摘要

- 未启动新的 subagents，因此没有闲置 agent 需要关闭。
- 工作树仅保留用户侧未跟踪 `.context/plans.md`；本轮未 stage、未删除该文件。
- 基于当前前端 coverage summary：lines 47.97%、statements 47.69%、functions 49.10%、branches 38.32%，新增初始全局 coverage thresholds：lines/statements/functions 45%，branches 35%。
- 前端 CI 已运行 `pnpm --dir frontend test:coverage`，因此 threshold 自动成为 frontend coverage gate。
- 后端 coverage 仍作为 `backend-coverage` LCOV artifact 输出，不设置百分比阈值。
- 更新 scripts 和 docs，避免继续把 coverage 状态描述为“无阈值”。

### 验证结果

- `pnpm --dir frontend test:coverage`: 通过；16 个测试文件 / 59 个测试，coverage 为 lines 47.97%、statements 47.69%、functions 49.10%、branches 38.32%，高于新阈值。
- `pnpm --dir frontend typecheck`: 通过。
- `pnpm --dir frontend lint`: 通过，0 error / 0 warnings。
- `pnpm --dir frontend test`: 通过；16 个测试文件 / 59 个测试，ErrorBoundary 用例打印预期 jsdom stack，退出码为 0。
- `pnpm --dir frontend build`: 通过；无 Vite chunk size warning，主 chunk约 315KB。
- `git diff --check`: 通过。

### 剩余风险

- 前端 coverage 阈值是初始防回退阈值，后续应随测试补强逐步提高。
- 后端 coverage 仍没有百分比阈值，只上传 LCOV artifact。
- EVTX 合法 tiny real `.evtx` fixture、Windows WebView2 media smoke、`evtx -> encoding` advisory cleanup 仍未完成。

---

## FS path component helper dedupe

**时间戳**: 2026-06-02 11:15:09 +08:00
**署名**: Codex
**基线**: `c7eaac5 test: enforce frontend coverage baseline`

### 本轮改动摘要

- 未启动新的 subagents，因此没有闲置 agent 需要关闭。
- 工作树仅保留用户侧未跟踪 `.context/plans.md`；本轮未 stage、未删除该文件。
- 从 Phase 6.1 剩余 FS enum 去重中选择低风险子项：FAT、exFAT、NTFS reader 重复的 slash/backslash path component splitting。
- 在 `evidence-core::filesystem` 新增 `path_components(path: &str) -> Vec<&str>`，并补单元测试。
- `fs-fat`、`fs-exfat`、`fs-ntfs` 改为复用该 helper；不改变 public API、排序、root semantics、path separator convention 或 reader behavior。
- 文档更新为当前 FS helper 去重仍是 Partial，深层枚举流程和 FS 特定错误转换仍留后续。

### 验证结果

- `cargo test -p evidence-core filesystem`: 通过。
- `cargo test -p fs-fat`: 通过。
- `cargo test -p fs-exfat`: 通过。
- `cargo test -p fs-ntfs`: 通过。
- `cargo test -p app-services file_service`: 通过。
- `cargo test -p app-services --test file_service_real_test`: 通过。
- `cargo clippy -p evidence-core -p fs-fat -p fs-exfat -p fs-ntfs --all-targets -- -D warnings`: 通过。
- `cargo fmt --all -- --check`: 通过。

### 剩余风险

- 本轮只抽出 path component normalization helper，不重构 FAT/NTFS/exFAT 深层枚举流程或错误类型体系。
- 进一步去重仍需保持 public API、排序、root semantics、path separator convention 和 reader behavior 不变。

---

## EVTX real fixture regression

**时间戳**: 2026-06-02 11:28:11 +08:00
**署名**: Codex
**基线**: `0e84cf8 refactor: share filesystem path component parsing`

### 本轮改动摘要

- 未启动新的 subagents，因此没有闲置 agent 需要关闭。
- 工作树仅保留用户侧未跟踪 `.context/plans.md`；本轮未 stage、未删除该文件。
- 从剩余 backlog 中选择 EVTX fixture gap。复核发现 `evtx 0.11.2` crates.io package 排除了 `*.evtx` fixture，但上游 GitHub repository 的 sample set 仍由 MIT/Apache-2.0 license 覆盖。
- 新增 `testdata/fixtures/tiny/evtx/system.evtx`，来源为 upstream `EVTX` commit `38a2d50b21629edb3dd77953a2c02a4b944badf1` 的 `samples/system.evtx`；新增 README 记录 license、source、SHA-256 `60e5fd8f10a3deecd58bd7bee2e03b1308d190fd13b52908be9d576c7f5d6623` 和大小 1,118,208 bytes。
- `crates/testing::fixtures` 新增 `tiny_system_evtx()`，并用单测验证 fixture 小于 2MB 且具有 `ElfFile\0` header。
- `artifacts-windows` EVTX parser 新增 real fixture regression，覆盖真实 parser path，并断言 fixture 可解析出 6005/6006/6008/1074 boot/shutdown candidate；仍明确这些是 EventLog/User32 candidate，不是绝对开关机事实。
- 更新 `docs/remediation-plan-v2.1.md`、`docs/architecture-model.md`、`docs/algorithm-complexity-analysis.md` 和 `docs/开发记录.md`，把 EVTX fixture gap 从 Remaining 移出；保留 `evtx -> encoding` advisory cleanup 与 WebView2 media smoke 为剩余项。

### 验证结果

- `cargo test -p artifacts-windows evtx`: 通过，7 个 EVTX targeted tests。
- `cargo test -p testing tiny_system_evtx`: 通过。
- `cargo test -p artifacts-windows`: 通过，24 个 lib tests、9 个 parser tests，4 个 real fixture tests ignored。
- `cargo test -p testing`: 通过，5 个 fixture helper tests。
- `cargo fmt --all -- --check`: 通过。
- `cargo clippy -p artifacts-windows -p testing --all-targets -- -D warnings`: 通过。
- `powershell -ExecutionPolicy Bypass -File scripts\check-deny-exceptions.ps1`: 通过，18 条 advisory exceptions checked。
- `cargo deny check advisories bans licenses sources`: 通过；仍有既有 duplicate dependency warnings。
- `cargo audit`: 通过，退出码 0；仍报告 20 个 warning-class advisories，包括 `evtx -> encoding`，继续由到期例外跟踪。

### 剩余风险

- `evtx -> encoding` advisory 仍是到期例外，需在 2026-09-01 前替换 parser、vendor/patch、升级或重新评审。
- Real `System.evtx` fixture 只覆盖当前 boot/shutdown candidate parser path，不代表完整 EVTX parser 平台。
- Windows WebView2 `evidence-media://` 播放/seek smoke 仍未执行。

---

## Media protocol CI guard

**时间戳**: 2026-06-02 11:38:10 +08:00
**署名**: Codex
**基线**: `1e1db68 test: add real evtx fixture coverage`

### 本轮改动摘要

- 未启动新的 subagents，因此没有闲置 agent 需要关闭。
- 工作树仅保留用户侧未跟踪 `.context/plans.md`；本轮未 stage、未删除该文件。
- 复核 `evidence-media` 剩余项后确认：真实 Windows WebView2 `<video>/<audio>` 播放/seek 需要桌面壳、案件和媒体样本，不适合替代为无头 CI。
- 新增 `scripts/check-media-protocol-guard.ps1`，固定 CI 可验证的 media 安全边界：CSP 必须允许 `evidence-media:`；desktop shell 必须注册 `media_protocol`；protocol handler 必须保留 bounded range parser、206/416 行为和 `MAX_MEDIA_PROTOCOL_READ_BYTES`；file command 必须保留 protocol mode、`media_protocol_url()` 和 `read_media_range` fallback；frontend API/hook 必须继续走 `get_media_url`、`read_media_range`、protocol mode 和 range fallback；desktop/frontend viewer 相关代码不得回退到 `asset://localhost` 或 `convertFileSrc`。
- 后端 CI 在 release guard 后新增 Media protocol guard step。
- 更新 `scripts/README.md`、`docs/remediation-plan-v2.1.md`、`docs/architecture-model.md`、`docs/algorithm-complexity-analysis.md` 和 `docs/开发记录.md`，明确该 guard 不替代真实 WebView2 seek smoke。

### 验证结果

- `powershell -ExecutionPolicy Bypass -File scripts\check-media-protocol-guard.ps1`: 通过。
- `cargo test -p forensics-desktop media_protocol`: 通过，10 个 protocol/range tests。
- `cargo test -p forensics-desktop commands::file_commands::tests::`: 通过，7 个 file command tests，覆盖 protocol media URL、range clamp、invalid handle、EOF、host path 不泄露和 extract file。
- `powershell -ExecutionPolicy Bypass -File scripts\check-release-guard.ps1`: 通过。
- `git diff --check`: 通过。

### 剩余风险

- 仍需人工或未来专用 harness 验证 Windows WebView2 中 `<video>/<audio>` 对 `evidence-media://` 的连续播放和 seek 行为。

---

## EVTX dependency decision guard

**时间戳**: 2026-06-02 11:44:15 +08:00
**署名**: Codex
**基线**: `3c20d76 test: guard media protocol wiring`

### 本轮改动摘要

- 未启动新的 subagents，因此没有闲置 agent 需要关闭。
- 工作树仅保留用户侧未跟踪 `.context/plans.md`；本轮未 stage、未删除该文件。
- 复核 `evtx -> encoding` advisory cleanup 可行性：crates.io 最新仍是 `evtx 0.11.2`；上游 `EVTX` commit `38a2d50b21629edb3dd77953a2c02a4b944badf1` 仍直接依赖 `encoding = "0.2.33"`；本地 `evtx` 源码中 `EncodingRef`、`WINDOWS_1252`、`DecoderTrap` 分布于 parser/binxml/render 路径，不能通过 feature trimming 移除。
- 替代 crate 复核结果：`evtx-msg 1.0.1` 为 GPL-3.0，`exhume_artefacts 0.2.6` 为 GPL-2.0-or-later，不适合当前 MIT workspace 直接引入；`rsigma 0.13.0` 是更大的 Sigma/runtime 工具链，不是窄 EVTX parser 替代，且 Rust 版本要求更高。
- 新增 `docs/evtx-dependency-decision.md`，记录当前保留 `evtx 0.11.2` 的风险边界、证据和 2026-09-01 前必须执行的替换/patch/复审选项。
- 新增 `scripts/check-evtx-dependency-decision.ps1`，检查 `deny.toml` 的 `RUSTSEC-2021-0153` 例外仍与决策文档同步，避免 dependency exception 和技术决策分叉。
- 后端 CI 在 dependency exception guard 后新增 EVTX dependency decision guard。
- 更新 `scripts/README.md`、`docs/remediation-plan-v2.1.md`、`docs/architecture-model.md`、`docs/algorithm-complexity-analysis.md` 和 `docs/开发记录.md`。

### 验证结果

- `powershell -ExecutionPolicy Bypass -File scripts\check-evtx-dependency-decision.ps1`: 通过。
- `powershell -ExecutionPolicy Bypass -File scripts\check-deny-exceptions.ps1`: 通过。
- `cargo deny check advisories bans licenses sources`: 通过；仍报告既有 duplicate dependency warnings。
- `cargo audit`: 通过，退出码 0；仍报告 20 个 warning-class transitive advisories，其中包含 `evtx -> encoding` 链路。
- `git diff --check`: 通过。

### 剩余风险

- `evtx -> encoding` advisory 本身仍未消除；本轮只是把不可安全替换的原因和到期处理要求变成可审计、可 CI 检查的决策记录。

---

## Beta backlog final gate refresh

**时间戳**: 2026-06-02 11:52:50 +08:00
**署名**: Codex
**基线**: `1d25ba2 test: guard evtx dependency decision`

### 本轮改动摘要

- 未启动新的 subagents，因此没有闲置 agent 需要关闭。
- 工作树仅保留用户侧未跟踪 `.context/plans.md`；本轮未 stage、未删除该文件。
- 在 EVTX dependency decision guard checkpoint 后补跑可信 beta 默认门禁，确认 media protocol guard、EVTX fixture coverage、EVTX dependency decision guard、coverage baseline 和 docs 状态没有引入回退。
- 最终 grep 审阅结论：伪取证事实命中只存在于负向测试、旧审计文档、时间戳注释或 Cargo checksum；业务代码/UI 未输出硬编码主机名、默认 Windows 版本、默认 build 或固定 boot timestamp。页面/feature 无直接 Tauri `invoke`；host path 字符串集中在 Settings 默认路径/测试、artifact parser 示例路径和防泄露断言，media/viewer 未回退到 `asset://localhost` 或 `convertFileSrc`。

### 验证结果

- `cargo fmt --all -- --check`: 通过。
- `cargo clippy --workspace --all-targets -- -D warnings`: 通过。
- `cargo test --workspace`: 通过；真实 E01 tests ignored，需要 `FORENSICS_E01_FIXTURE` opt-in。
- `cargo build -p forensics-desktop`: 通过。
- `cargo doc --workspace --no-deps`: 通过。
- `powershell -ExecutionPolicy Bypass -File scripts\check-release-guard.ps1`: 通过。
- `powershell -ExecutionPolicy Bypass -File scripts\check-media-protocol-guard.ps1`: 通过。
- `powershell -ExecutionPolicy Bypass -File scripts\check-evtx-dependency-decision.ps1`: 通过。
- `cargo deny check advisories bans licenses sources`: 通过；仍报告既有 duplicate dependency warnings。
- `cargo audit`: 通过，退出码 0；仍报告 20 个 warning-class transitive advisories，其中包含 `evtx -> encoding` 链路。
- `pnpm --dir frontend typecheck`: 通过。
- `pnpm --dir frontend lint`: 通过。
- `pnpm --dir frontend test`: 通过，16 个 test files / 59 个 tests；ErrorBoundary 用例打印预期 jsdom stack，退出码为 0。
- `pnpm --dir frontend build`: 通过，主 chunk 约 315KB，无 Vite chunk size warning。
- `pnpm --dir frontend test:coverage`: 通过，16 个 test files / 59 个 tests；coverage 为 statements 47.69%、branches 38.32%、functions 49.10%、lines 47.97%，高于当前阈值。
- `pnpm --dir frontend audit --audit-level high`: 通过，No known vulnerabilities found。

### 剩余风险

- Windows WebView2 `evidence-media://` 播放/seek smoke 仍未执行。
- `evtx -> encoding` advisory 仍是到期例外，需在 2026-09-01 前替换 parser、vendor/patch、升级或重新评审。
- Registry parser 仍是 Analysis 定向字段 parser，不是完整 hive browser；FS enum 更深层流程/错误转换去重仍留后续。

---

## Registry value-list cell hardening

**时间戳**: 2026-06-02 11:59:26 +08:00
**署名**: Codex
**基线**: `cd7d247 docs: record beta final gate refresh`

### 本轮改动摘要

- 未启动新的 subagents，因此没有闲置 agent 需要关闭。
- 工作树仅保留用户侧未跟踪 `.context/plans.md`；本轮未 stage、未删除该文件。
- 修复 `artifacts-windows::registry::lookup` 的真实 hive value-list 解析：NK 的 `values_list_offset` 现在按 registry cell 读取，先校验负 cell size 和 cell bounds，再从 cell payload 读取 value offsets；不再把 value-list 当作裸 u32 offset 数组。
- 强化 value data 边界：inline data 长度超过 4 bytes 时返回错误；`REG_DWORD` / `REG_QWORD` 外部数据长度不足时返回错误，不再静默补 0。
- 同步 app-services Analysis 测试 fixture builder，使 synthetic SYSTEM/SOFTWARE hive 使用带 cell header 的 value-list 布局，继续覆盖 Analysis registry provenance flow。
- 补充 `lh` / `li` / `ri` 子键列表导航回归测试，确保文档声称支持的 list 变体都能从 root 导航到目标 key 并读取 value。

### 验证结果

- `cargo test -p artifacts-windows registry::lookup`: 通过，16 个 targeted tests，新增 value-list cell header、truncated value-list、inline overlong、short DWORD、`lh/li/ri` subkey list 回归。
- `cargo test -p app-services --lib analysis_service`: 通过，9 个 Analysis targeted tests。
- `cargo fmt --all`: 已执行。

### 剩余风险

- Registry parser 仍只承诺 Analysis 所需字段；完整 hive traversal、真实 registry fixture 覆盖和更复杂 cell graph 仍需继续扩展。
- `evtx -> encoding` advisory 和 WebView2 media manual smoke 仍是独立剩余项。

---

## Registry typed value coverage

**时间戳**: 2026-06-02 15:38:12 +08:00
**署名**: Codex
**基线**: `45360eb test: cover registry subkey list variants`

### 本轮改动摘要

- 未启动新的 subagents，因此没有闲置 agent 需要关闭。
- 工作树仅保留用户侧未跟踪 `.context/plans.md`；本轮未 stage、未删除该文件。
- 继续深化 `artifacts-windows::registry::lookup` targeted parser 覆盖：新增非 compressed UTF-16 NK 名称、`REG_EXPAND_SZ`、`REG_MULTI_SZ`、`REG_QWORD` 和奇数长度 UTF-16 string data 回归。
- 修正 `REG_MULTI_SZ` 解码路径：multi-string 现在保留完整 UTF-16 字符串并按 NUL 分割，不再在第一个 NUL 截断；奇数长度 UTF-16 registry string data 现在返回 parse error，不再静默丢弃尾字节。

### 验证结果

- `cargo test -p artifacts-windows registry::lookup`: 通过，21 个 targeted tests。
- `cargo test -p app-services --lib analysis_service`: 通过，9 个 Analysis targeted tests。

### 剩余风险

- Registry parser 仍只承诺 Analysis 所需字段；完整 hive traversal、真实 registry fixture 覆盖和更复杂 cell graph 仍需继续扩展。
- `evtx -> encoding` advisory 和 WebView2 media manual smoke 仍是独立剩余项。

---

## FS node parent path helper dedupe

**时间戳**: 2026-06-02 15:52:16 +08:00
**署名**: Codex
**基线**: `339108f fix: preserve registry multi-string values`

### 本轮改动摘要

- 未启动新的 subagents，因此没有闲置 agent 需要关闭。
- 工作树仅保留用户侧未跟踪 `.context/plans.md`；本轮未 stage、未删除该文件。
- 在 `evidence-core::filesystem` 新增 `node_with_parent_path` 和 `node_with_parent_path_with_separator`，用于将已有 `FsNode` 按 parent path 补齐 child path，同时保留 metadata。
- FAT、NTFS 复用 slash-separated helper；exFAT 复用 backslash-preserving helper。未改变 public API、排序、root semantics 或 reader 行为。

### 验证结果

- `cargo test -p evidence-core filesystem`: 通过，7 个 targeted tests。
- `cargo test -p fs-fat`: 通过。
- `cargo test -p fs-exfat`: 通过。
- `cargo test -p fs-ntfs`: 通过。
- `cargo clippy -p evidence-core -p fs-fat -p fs-exfat -p fs-ntfs --all-targets -- -D warnings`: 通过。
- `cargo test -p app-services file_service`: 通过，10 个 filtered unit tests。
- `cargo test -p app-services --test file_service_real_test`: 通过，7 个 real file service tests。

### 剩余风险

- FS enum 去重仍是 Partial：更深层的目录枚举流程和 FS 特定错误转换还需要后续分轮处理。
- WebView2 media manual smoke 与 `evtx -> encoding` advisory cleanup 仍是独立剩余项。

---

## EVTX local patched dependency cleanup

**时间戳**: 2026-06-02 16:21:41 +08:00
**署名**: Codex
**基线**: `5acdf8d docs: order beta backlog session entries`

### 本轮改动摘要

- 未启动新的 subagents，因此没有闲置 agent 需要关闭。
- 工作树仍保留用户侧未跟踪 `.context/plans.md`；本轮未 stage、未删除该文件。
- 新增 workspace member `crates/evtx-patched`，以本地 `evtx 0.11.2` fork 替代 crates.io `evtx` dependency。
- local fork 保留库源码和 license/readme，移除 CLI/bench/sample 依赖路径；将 legacy `encoding = "0.2.33"` 替换为 `encoding_rs`，并保留 `EncodingRef` alias 以降低 runtime parser diff。
- 由于 crates.io package 排除了上游 sample EVTX 文件，local fork 禁用上游 sample-based internal tests 和 doctests；本仓 `artifacts-windows` EVTX tests 与 tiny real `System.evtx` fixture 继续覆盖运行时 parser path。
- 从 `deny.toml` 移除 `RUSTSEC-2021-0153` 例外，`scripts/check-evtx-dependency-decision.ps1` 改为 anti-regression guard：检查 workspace 使用 `crates/evtx-patched`、local fork 使用 `encoding_rs`、`Cargo.lock` 不含 `encoding`、决策文档记录 Resolved Decision。
- 更新 `docs/evtx-dependency-decision.md`、`docs/remediation-plan-v2.1.md`、`docs/architecture-model.md`、`docs/algorithm-complexity-analysis.md` 和 `scripts/README.md`，把 `evtx -> encoding` 从剩余 advisory cleanup 改为已通过 local patched fork 解决；仍保留 WebView2 media manual smoke、完整 Registry browser 和 FS 深层去重为剩余项。

### 验证结果

- `cargo check -p artifacts-windows`: 通过。
- `cargo test -p artifacts-windows evtx`: 通过，7 个 EVTX targeted tests。
- `cargo clippy -p evtx -p artifacts-windows --all-targets -- -D warnings`: 通过。
- `cargo test -p evtx`: 通过，10 个 local fork unit tests；doctests 因上游 sample 缺失在 manifest 中禁用。
- `cargo tree -p artifacts-windows -i encoding`: 未匹配 `encoding` package；仅提示相近 package `encoding_rs`。
- `cargo tree -p artifacts-windows -i evtx`: 指向 `evtx v0.11.2 (D:\forensics\crates\evtx-patched)`。
- `cargo tree -p artifacts-windows | Select-String -Pattern "encoding|evtx|encoding_rs"`: 显示 `evtx` local path 与 `encoding_rs v0.8.35`。
- `cargo fmt --all -- --check`: 通过。
- `cargo test --workspace`: 通过；真实 E01 slow tests ignored。
- `cargo clippy --workspace --all-targets -- -D warnings`: 通过。
- `git diff --check`: 通过。
- `powershell -ExecutionPolicy Bypass -File scripts\check-deny-exceptions.ps1`: 通过，17 条 advisory exceptions checked。
- `powershell -ExecutionPolicy Bypass -File scripts\check-evtx-dependency-decision.ps1`: 通过。
- `cargo audit`: 通过，退出码 0；仍报告 19 个 warning-class transitive advisories，`RUSTSEC-2021-0153` 已不在列表中。
- `cargo deny check advisories bans licenses sources`: 通过；仍报告 duplicate dependency warnings。

### 剩余风险

- local `evtx` fork 需要后续维护；若 upstream 发布 maintained permissive 版本，应优先替换回正常 dependency。
- EVTX adapter 仍只承诺 6005/6006/6008/1074 boot/shutdown candidate extraction，不是完整 EVTX parser 平台。
- WebView2 media manual smoke、完整 Registry browser 和 FS 深层枚举流程/错误转换去重仍是后续项。

---

## WebView2 media smoke harness

**时间戳**: 2026-06-02 16:48:28 +08:00
**署名**: Codex
**基线**: `197b0c3 fix: vendor evtx parser without encoding`

### 本轮改动摘要

- 未启动新的 subagents，因此没有闲置 agent 需要关闭。
- 新增 `scripts/run-webview2-media-smoke.ps1`，用于准备真实 Windows WebView2 播放/seek 手动 smoke：
  - 先运行 `scripts/check-media-protocol-guard.ps1`，确认 CSP/protocol/fallback/host-path guard 仍成立。
  - 在系统临时目录生成 logical evidence fixture：`small-inline.wav` 小于 inline threshold，`large-protocol.wav` 大于 20MB inline media threshold。
  - 输出 `WEBVIEW2_MEDIA_SMOKE.md` 清单，要求创建/打开 case、导入 generated logical evidence、验证小音频 inline preview、大音频 `evidence-media://handle/` protocol preview、WebView2 播放和 seek、以及不泄露宿主证据路径。
- 更新 `scripts/README.md`、`docs/remediation-plan-v2.1.md`、`docs/architecture-model.md` 和 `docs/algorithm-complexity-analysis.md`，把 media 剩余项从“manual smoke pending”细化为“smoke harness ready / manual result pending”。

### 验证结果

- `powershell -ExecutionPolicy Bypass -File scripts\run-webview2-media-smoke.ps1`: 通过。
  - `Media protocol guard passed`。
  - 生成 evidence directory: `C:\Users\QAQ\AppData\Local\Temp\forensics-webview2-media-smoke\logical-media-evidence`。
  - 生成 `small-inline.wav`，大小 88,244 bytes。
  - 生成 `large-protocol.wav`，大小 22,020,140 bytes，超过 20MB inline media threshold。
  - 生成 checklist: `C:\Users\QAQ\AppData\Local\Temp\forensics-webview2-media-smoke\WEBVIEW2_MEDIA_SMOKE.md`。

### 剩余风险

- 本轮只提供可复现 smoke harness 和 fixture，不宣称已经完成真实 WebView2 播放/seek 人工验证。
- 真实结果仍需在 Windows Tauri 桌面壳中按 checklist 操作后记录。

---

## Registry synthetic tiny hive fixture

**时间戳**: 2026-06-02 17:08:21 +08:00
**署名**: Codex
**基线**: `5e60435 test: add webview2 media smoke harness`

### 本轮改动摘要

- 未启动新的 subagents，因此没有闲置 agent 需要关闭。
- 从剩余 backlog 中选择 Registry fixture gap：当前 parser 已能解析 Analysis targeted fields，但仓库内缺少可复现 SYSTEM/SOFTWARE hive fixture。
- 新增 `crates/testing::builders::registry` 作为 deterministic synthetic registry hive 生成源，覆盖 `ComputerName`、timezone、`ProductName`、`CurrentBuild`、`DisplayVersion`、`RegisteredOwner`、`ProductId`、`InstallDate`。
- 新增 `testdata/fixtures/tiny/logical/Windows/System32/config/SYSTEM` 和 `SOFTWARE`，删除旧 `SYSTEM.placeholder`。
- `scripts/generate-tiny-fixtures.ps1` 现在可重建 RAW/E01/Registry fixtures；`crates/testing::fixtures` 新增 registry hive path helpers。
- `artifacts-windows` 新增 committed fixture parser-path 回归；`app-services` Analysis registry provenance 测试改为读取 committed fixture，减少重复私有 builder。
- 文档同步标注：这是 synthetic targeted fixture，不是完整 Windows registry corpus。

### 验证结果

- `powershell -ExecutionPolicy Bypass -File scripts\generate-tiny-fixtures.ps1`: 通过。
- `cargo test -p testing registry`: 通过，4 个 registry builder/fixture tests。
- `cargo test -p artifacts-windows registry::lookup`: 通过，23 个 targeted tests。
- `cargo test -p app-services --lib analysis_service`: 通过，9 个 analysis_service tests。

### 剩余风险

- Registry parser 仍不是完整 hive browser；真实 production hive corpus 覆盖仍需后续单独引入或生成。
- WebView2 media manual smoke 仍需人工执行并记录真实播放器 seek 结果。
- FS enum 深层目录枚举流程和 FS 特定错误转换去重仍是 Partial。

---

## FS child list helper expansion

**时间戳**: 2026-06-02 17:25:35 +08:00
**署名**: Codex
**基线**: `050ffa8 test: add tiny registry hive fixtures`

### 本轮改动摘要

- 未启动新的 subagents，因此没有闲置 agent 需要关闭。
- 继续 Phase 6.1 FS enum 去重，选择不改变行为的公共 child list finalization 子项。
- `evidence-core::filesystem` 新增批量 parent path assignment helpers 和 dot/dotdot special-entry predicate。
- `fs-fat`、`fs-exfat`、`fs-ntfs` 复用这些 helpers，减少重复 map/push/path assignment 逻辑，同时保留既有 slash/backslash convention。
- 本轮不触碰底层 directory entry parser、cluster traversal、NTFS parent verification 或 reader behavior。

### 验证结果

- `cargo test -p evidence-core filesystem`: 通过，9 个 filesystem/helper tests。
- `cargo test -p fs-fat -p fs-exfat -p fs-ntfs`: 通过。
- `cargo clippy -p evidence-core -p fs-fat -p fs-exfat -p fs-ntfs --all-targets -- -D warnings`: 通过。
- `cargo test -p app-services file_service`: 通过，10 个 filtered tests。

### 剩余风险

- FS enum 去重仍是 Partial：更深层目录枚举流程和 FS 特定错误转换仍需后续分轮处理。
- WebView2 media manual smoke 与真实 E01 slow/manual gate 仍依赖人工/外部样本。

### Post-check Gate

- `cargo test --workspace`: passed; real E01 slow/manual tests remained ignored / opt-in.
- `cargo clippy --workspace --all-targets -- -D warnings`: passed.

**Post-check time**: 2026-06-02 17:31:10 +08:00

---

## Dependency direct dirs alignment

**时间戳**: 2026-06-02 17:43:07 +08:00
**署名**: Codex
**基线**: `2cd945d docs: record registry fs post-check gates`

### 本轮改动摘要

- 未启动新的 subagents，因此没有闲置 agent 需要关闭。
- 继续处理依赖治理剩余项，先选择 workspace 直接依赖造成的 `dirs` v5/v6 分叉。
- `apps/desktop/src-tauri/Cargo.toml` 将 `dirs = "5"` 对齐到 `dirs = "6"`；`Cargo.lock` 同步删除旧 `dirs 5.0.1`、`dirs-sys 0.4.1`、`redox_users 0.4.6`、`windows-sys 0.48.0`、`windows-targets 0.48.5` 和对应 `windows_* 0.48.5` target packages。
- 复核唯一调用点 `dirs::config_dir()` 仍兼容 v6；本轮不扩大到 Tauri/Tantivy/GTK 传递依赖 patch。
- 更新 `docs/remediation-plan-v2.1.md` 和 `docs/开发记录.md`，将本轮 direct-dirs cleanup 作为 Phase 5.2 dependency gate 的补充证据，并保留剩余 duplicate warnings 风险。

### 验证记录

- `cargo check -p forensics-desktop`: 通过。
- `cargo test -p forensics-desktop`: 通过，25 个 desktop lib tests。
- `cargo clippy -p forensics-desktop --all-targets -- -D warnings`: 通过。
- `cargo deny check bans`: 通过；仍报告其他 Tauri/Tantivy/GTK 传递依赖 duplicate warnings，但 `dirs` / `dirs-sys` 和 `windows-targets 0.48` 组已消除。
- `cargo fmt --all -- --check`: 通过。
- `git diff --check`: 通过。

### 剩余风险

- 该 checkpoint 只消除 direct desktop `dirs` 分叉，不代表所有 duplicate dependency warnings 已清零。
- 其他 duplicate/advisory 治理仍需按上游 dependency graph 分批处理，避免用不安全 `[patch]` 覆盖传递依赖。
- WebView2 media manual smoke 与真实 E01 slow/manual gate 仍依赖人工/外部样本。

---

## Tantivy advisory cleanup

**时间戳**: 2026-06-02 17:59:08 +08:00
**署名**: Codex
**基线**: `b75aa52 chore: align desktop dirs dependency`

### 本轮改动摘要

- 未启动新的 subagents，因此没有闲置 agent 需要关闭。
- 继续处理依赖治理剩余项。复核 `cargo audit` 后确认 search stack 的 direct dependency `tantivy 0.22.1` 引入两个可自动消除的 advisory paths：
  - `measure_time -> instant 0.1.13` / `RUSTSEC-2024-0384`。
  - `lru 0.12.5` / `RUSTSEC-2026-0002`。
- 将 `crates/search` 的 Tantivy 约束升级为 `0.26`，lockfile 解析为 `tantivy 0.26.1`。
- 适配 Tantivy 0.26 score collector API，搜索排序仍按 score。
- 删除 `deny.toml` 中已不再命中的 `RUSTSEC-2024-0384` 例外，避免 advisory exception 沉积。
- 更新 `docs/remediation-plan-v2.1.md` 和 `docs/开发记录.md`，记录 audit warnings 从 19 降到 17。

### 验证记录

- `cargo test -p search`: 通过，14 个 tests。
- `cargo check -p app-services`: 通过。
- `cargo clippy -p search -p app-services --all-targets -- -D warnings`: 通过。
- `cargo test -p app-services --test search_service_test`: 通过，3 个 tests。
- `cargo test -p forensics-desktop`: 通过，25 个 tests。
- `cargo check -p forensics-desktop`: 通过。
- `cargo audit`: 通过，17 个 allowed warning-class advisories；`instant` 和 `lru` advisories 不再出现。
- `cargo deny check advisories bans licenses sources`: 通过；仍报告 duplicate dependency warnings，但没有 stale advisory ignore。
- `cargo fmt --all -- --check`: 通过。
- `git diff --check`: 通过。

### 剩余风险

- 剩余 17 个 `cargo audit` warnings 仍集中在 Tauri Linux GTK/WebKit/urlpattern 传递链和 glib advisory；这些需要上游 Tauri/wry/GTK stack 迁移或后续更大依赖升级评估。
- Tantivy 0.26 带来新的传递依赖集合，虽然 targeted search/app-services/desktop gates 已通过，后续仍应在默认 full gate 中复核。
- WebView2 media manual smoke 与真实 E01 slow/manual gate 仍依赖人工/外部样本。

---

## FS declared-size truncation helper

**时间戳**: 2026-06-02 18:12:41 +08:00
**署名**: Codex
**基线**: `dc29c41 chore: upgrade tantivy search dependency`

### 本轮改动摘要

- 未启动新的 subagents，因此没有闲置 agent 需要关闭。
- 工作树仍保留用户侧未跟踪 `.context/plans.md`；本轮未 stage、未删除该文件。
- 继续 Phase 6.1 FS enum / reader helper 去重，选择不改变路径解析、排序、root semantics 或 NTFS parent verification 的数据读取收口项。
- `evidence-core::filesystem` 新增 `truncate_data_to_declared_size()`，集中处理 FAT/exFAT/NTFS 读取完整 cluster/data-run 后按 filesystem declared logical size 去除 padding 的逻辑。
- `fs-fat`、`fs-exfat`、`fs-ntfs` 复用该 helper；exFAT 不再为截断场景额外 copy slice，FAT/NTFS 避免各自手写 `size as usize` 截断分支。

### 验证记录

- `cargo test -p evidence-core filesystem`: 通过，12 个 filesystem/helper tests。
- `cargo test -p fs-fat -p fs-exfat -p fs-ntfs`: 通过。
- `cargo test -p app-services file_service`: 通过，10 个 filtered file_service tests。
- `cargo test -p app-services --test file_service_real_test`: 通过，7 个 real file service tests。
- `cargo clippy -p evidence-core -p fs-fat -p fs-exfat -p fs-ntfs --all-targets -- -D warnings`: 通过。
- `cargo fmt --all -- --check`: 通过。
- `git diff --check`: 通过。

### 剩余风险

- 本轮只收口 filesystem declared-size data truncation，不触碰 FAT/exFAT directory traversal、NTFS path resolution 或 FS 特定错误转换；Phase 6.1 仍是 Partial。
- WebView2 media manual smoke 与真实 E01 slow/manual gate 仍依赖人工/外部样本。

### 后置 workspace gate

- `cargo test --workspace`: 通过；真实 E01 tests 保持 ignored / opt-in。
- `cargo clippy --workspace --all-targets -- -D warnings`: 通过。
- `cargo fmt --all -- --check`: 通过。
- `powershell -ExecutionPolicy Bypass -File scripts\run-webview2-media-smoke.ps1`: 通过；重新生成 `C:\Users\QAQ\AppData\Local\Temp\forensics-webview2-media-smoke\WEBVIEW2_MEDIA_SMOKE.md`，仍需真实 Windows Tauri WebView2 手动播放/seek 验证。
- `FORENSICS_E01_FIXTURE`: 当前未设置，因此真实 E01 slow/manual gate 未执行。
- `cargo audit`: 通过，仍报告 17 个 allowed warning-class advisories，来源集中在 Tauri Linux GTK/WebKit/urlpattern/glib 传递链。

**后置验证时间**: 2026-06-02 18:19:06 +08:00

---

## Tauri patch dependency update

**时间戳**: 2026-06-02 18:25:36 +08:00
**署名**: Codex
**基线**: `01332f2 docs: record fs post-check gates`

### 本轮改动摘要

- 未启动新的 subagents，因此没有闲置 agent 需要关闭。
- 工作树仍保留用户侧未跟踪 `.context/plans.md`；本轮未 stage、未删除该文件。
- 用 `cargo search` / `cargo info` 复核 Tauri patch 版本后，执行 `cargo update -p tauri -p tauri-utils`。
- `Cargo.lock` 更新：
  - `tauri`、`tauri-runtime`、`tauri-runtime-wry`: `2.11.1` -> `2.11.2`
  - `tauri-build`、`tauri-codegen`、`tauri-macros`: `2.6.1` -> `2.6.2`
  - `tauri-utils`: `2.9.1` -> `2.9.2`
- 本轮没有修改 Tauri capabilities/CSP/commands，也没有引入宽泛 asset scope。

### 验证记录

- `cargo check -p forensics-desktop`: 通过。
- `cargo test -p forensics-desktop`: 通过，25 个 desktop tests。
- `cargo clippy -p forensics-desktop --all-targets -- -D warnings`: 通过。
- `cargo build -p forensics-desktop`: 通过。
- `powershell -ExecutionPolicy Bypass -File scripts\check-release-guard.ps1`: 通过。
- `cargo audit`: 通过，仍报告 17 个 allowed warning-class advisories。
- `cargo deny check advisories bans licenses sources`: 通过；仍报告 duplicate dependency warnings。
- `cargo fmt --all -- --check`: 通过。
- `git diff --check`: 通过。

### 剩余风险

- Tauri patch 更新没有减少 `cargo audit` warning count；剩余风险仍集中在 Tauri Linux GTK/WebKit/urlpattern/glib 传递链，需要等待上游 stack 迁移或做更大版本评估。
- WebView2 media manual smoke 与真实 E01 slow/manual gate 仍依赖人工/外部样本。

### 前端当前 HEAD 门禁

- `pnpm --dir frontend typecheck`: 通过。
- `pnpm --dir frontend lint`: 通过。
- `pnpm --dir frontend test`: 通过，16 个测试文件 / 59 个测试；ErrorBoundary 用例打印预期 jsdom stack，退出码为 0。
- `pnpm --dir frontend build`: 通过；未留下 tracked / untracked dist 变更。

**前端验证时间**: 2026-06-02 18:30:22 +08:00

---

## FS typed error helper consolidation

**时间戳**: 2026-06-02 18:44:16 +08:00
**署名**: Codex
**基线**: `d105e84 docs: record frontend current head gate`

### 本轮改动摘要

- 未启动新的 subagents，因此没有闲置 agent 需要关闭。
- 工作树仍保留用户侧未跟踪 `.context/plans.md`；本轮未 stage、未删除该文件。
- 继续处理 Phase 6.1 的 FS enum / helper 剩余架构债，选择不改变 reader 语义的错误转换收口。
- `evidence-core::filesystem` 新增 filesystem typed error constructors：`invalid_fs_data()`、`unsupported_fs()`、`unexpected_fs_eof()`、`fs_out_of_memory()`。
- FAT/exFAT/NTFS 中同等 `ErrorKind` 和 message 的重复 `io::Error::new` 调用改为复用这些 helper。
- `fs-exfat::dir` 为 VolumeLabel 异常长度增加 bounds check；坏 label 长度现在返回 `InvalidData`，避免 malformed directory entry 触发 slice 越界 panic。

### 验证记录

- `cargo test -p evidence-core filesystem`: 通过，13 个 filesystem/helper tests。
- `cargo test -p fs-fat -p fs-exfat -p fs-ntfs`: 通过；fs-exfat 31 个 tests、fs-fat 10 个 tests、fs-ntfs 22 个 tests/doc-tests。
- `cargo test -p app-services file_service`: 通过，10 个 filtered tests。
- `cargo test -p app-services --test file_service_real_test`: 通过，7 个 tests。
- `cargo clippy -p evidence-core -p fs-fat -p fs-exfat -p fs-ntfs --all-targets -- -D warnings`: 通过。
- `cargo fmt --all -- --check`: 通过。

### 剩余风险

- 该 checkpoint 不触碰 FAT/exFAT/NTFS directory traversal、排序、root semantics、NTFS `$FILE_NAME.par_ref` verification 或 evidence reader behavior。
- Phase 6.1 仍保留更深层枚举流程去重空间。
- WebView2 media manual smoke 与真实 E01 slow/manual gate 仍未因本轮完成。
