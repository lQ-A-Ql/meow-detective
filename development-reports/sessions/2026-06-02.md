# 2026-06-02 开发记录

## v2.1 Backlog 多 Agent 集成复核

**时间戳**: 2026-06-02 01:13:05 +08:00  
**署名**: Codex  
**基线**: `30d67bf [pi] Work in progress`

### 开发前文档复核

- 已读取并复核 `docs/remediation-plan-v2.1.md`、`docs/architecture-model.md`、`docs/algorithm-complexity-analysis.md`、`development-reports/sessions/2026-06-01.md` 和 `docs/开发记录.md`。
- 复核结论：四个 subagents 的实现已进入当前 HEAD；但 `docs/remediation-plan-v2.1.md` 仍把 Analysis provenance、Reports provenance、Job partial、Dependency gate、SBOM 和 tiny fixture 标成旧的 Partial/Remaining 状态。本轮已同步文档，保留真实 Registry/EVTX parser 与连续 media streaming 为剩余风险。

### 主线程统一审阅结论

| Stage | 审阅结果 | 说明 |
|---|---|---|
| Stage 1 Analysis / Artifact Parser | Accepted with known parser limits | `AnalysisProvenanceDto` 已覆盖 system info、boot records、classification 与 classified file；Registry 只做 hive discovery + `regf` header verification，不解析 key/value；EVTX 为正式 `notParsed/unavailable` adapter，不生成 boot/shutdown 假时间 |
| Stage 2 Frontend Analysis / Reports | Accepted | DataAnalysis 展示 status/warnings/provenance；前端类型、mock、hooks 与 API wrapper 同步；Reports HTML/CSV/JSON 输出 analysis summary/provenance，保留 HTML escaping 与 CSV formula sanitization |
| Stage 3 Viewer / Streaming | Accepted with fallback limit | 大媒体不暴露 host absolute path，使用 opaque handle + bounded `read_media_range`；当前仍是首 chunk/range fallback，不是完整 continuous streaming protocol |
| Stage 4 Jobs / Events | Accepted | `JobSnapshotDto`、SQLite migration、JobRepo/JobService、import/search/artifact stats 与 BottomDrawer UI 已同步 `warning/skipped/failed/partial` |
| Stage 5 CI / Fixture | Accepted with tiny E01 gap | CI 增加 `cargo deny`、`cargo audit`、`pnpm audit` 与 CycloneDX backend/frontend SBOM；仓库已有 tiny logical/RAW fixture 与 helper；真实 E01 改为 `FORENSICS_E01_FIXTURE` opt-in ignored slow tests，tiny E01 未入库 |
| Stage 6 Docs / Rustdoc | Accepted | `cargo doc --workspace --no-deps` 已通过；本轮修复 rustdoc bare URL / invalid link warnings，并更新 remediation/architecture/complexity 状态 |

### 本轮收尾改动

- 将 `frontend/package.json` 中 `vite` 升级到 `6.4.2`、`vitest` 升级到 `^4.1.0`，并同步 `pnpm.overrides.vite = 6.4.2` 和 lockfile，修复 `pnpm audit --audit-level high` 报告的 Vite/Vitest high/critical advisory。
- 清理本地 CycloneDX 验证生成的 `backend-sbom.json` 散落文件；这些文件是 CI artifact，不应提交到仓库源码树。
- 修复 rustdoc 文案中的裸 URL / 误识别 intra-doc link，确保 `cargo doc --workspace --no-deps` 无 warnings。
- 更新 `docs/remediation-plan-v2.1.md`、`docs/architecture-model.md`、`docs/algorithm-complexity-analysis.md`，把已完成项和剩余风险改为当前真实状态。

### 验证结果

- `cargo fmt --all -- --check`: 通过。
- `cargo clippy --workspace --all-targets -- -D warnings`: 通过。
- `cargo test --workspace`: 通过；真实 E01 tests 默认 ignored，需 `FORENSICS_E01_FIXTURE` 手动运行。
- `cargo build -p forensics-desktop`: 通过。
- `powershell -ExecutionPolicy Bypass -File scripts\check-release-guard.ps1`: 通过，`Release guard passed`。
- `cargo doc --workspace --no-deps`: 通过。
- `cargo deny check advisories bans licenses sources`: 通过；仍有 duplicate dependency warnings，属于后续依赖图治理。
- `cargo audit`: 通过，退出码 0；报告 19 个 allowed warning-class advisories。
- `pnpm --dir frontend typecheck`: 通过。
- `pnpm --dir frontend lint`: 通过，0 error，7 个既有 warning（MCP/DenseDataTable/SyntaxHighlighter/tauri-bridge）。
- `pnpm --dir frontend test`: 通过，13 个测试文件 / 51 个测试。ErrorBoundary 用例会打印预期的 `Test error` stack，退出码为 0。
- `pnpm --dir frontend build`: 通过；Vite 仍提示主 chunk 约 515KB。
- `pnpm --dir frontend audit --audit-level high`: 通过，无已知 high/critical 漏洞。
- `cargo test -p image-e01 --test e01_regression_test`: 通过，2 passed / 5 ignored；私有 E01 样本测试已改为 ignored opt-in。
- `cargo test -p image-e01 --test e01_dump`: 通过，0 passed / 2 ignored。
- `git diff --check`: 通过。
- 统一 grep 复核：页面/feature/API 中 Tauri `invoke` 只存在于 `frontend/src/lib/api/client.ts`；`crates apps frontend/src` 中不再存在 `E:/pangushi` / `liuyang_pc` 私有样本路径。

### 剩余风险

> 注：以下为 2026-06-02 01:13:05 统一复核时点的风险列表；同日 02:35:01 的“可信 Beta 收口执行”已将 Registry 定向 parser、EVTX candidate adapter 和 `evidence-media` protocol 推进到新的当前状态。

- Registry parser 尚未解析真实 key/value，不能输出 hostname、Windows version/build、timezone 等字段。
- EVTX boot/shutdown parser 尚未接入，当前只返回明确的 unavailable/notParsed provenance。
- 大媒体仍不是连续 streaming protocol，只是安全的 scoped range fallback。
- tiny E01 fixture 尚未入库；真实样本慢测仍依赖 `FORENSICS_E01_FIXTURE`。
- `cargo audit` 仍会报告若干 warning-class transitive advisories，短期由 `deny.toml` 例外追踪，后续应通过升级 Tauri/wry/Tantivy 等依赖消除。

---

## 可信 Beta 收口执行

**时间戳**: 2026-06-02 02:35:01 +08:00
**署名**: Codex
**基线**: `e7ffa35 fix: finalize v2.1 integration review` -> `codex/beta-forensics-backlog`

### 开发前文档复核

- 已读取 `docs/remediation-plan-v2.1.md`、`docs/architecture-model.md`、`docs/algorithm-complexity-analysis.md`、`docs/开发记录.md` 和本文件。
- 复核结论：上一轮文档准确记录了 media/Registry/EVTX 仍为主要剩余项。本轮将这些项从 fallback/stub 推进到可验证 beta：大媒体接入 `evidence-media` protocol，Registry 接入定向字段 parser，EVTX 接入 boot/shutdown candidate adapter。
- 未启动新的 subagents；因此没有闲置 agent 需要关闭。

### 本轮改动摘要

- 新增 `apps/desktop/src-tauri/src/media_protocol.rs`，注册 `evidence-media://handle/<encoded>` Tauri protocol；Range parser 支持 `bytes=start-end`、`bytes=start-`、`bytes=-suffix` 和 no Range bounded 206，每次最多读取 1MB，invalid range 返回 416。
- `get_media_url` 对大媒体返回 `MediaPreviewModeDto::Protocol`、opaque handle 和 protocol URL；小媒体继续返回 bounded inline data URL；`read_media_range` command 保留为 fallback。CSP 只在 `media-src` 允许 `evidence-media:`，不新增宽泛 asset scope。
- 新增 `artifacts-windows::registry::lookup` 定向 Registry reader，bounded 读取最多 64MB，解析 `regf` base block、NK/VK、`lf/lh/li/ri` 子键列表和 `REG_SZ` / `REG_EXPAND_SZ` / `REG_DWORD` / `REG_QWORD` / `REG_MULTI_SZ`；Analysis 从 SYSTEM/SOFTWARE 提取 hostname、timezone、ProductName、build、install date、owner/org/product id，并输出字段级 provenance。
- 新增 `artifacts-windows::evtx` adapter，使用 `evtx` crate bounded 解析 System.evtx 中的 6005、6006、6008、1074 候选事件；Analysis boot records 带 event id、record id、note 和 provenance，并明确这些是 EventLog/User32 candidates，不是绝对开关机事实。
- DataAnalysis、mock data、frontend models 和 reports 同步新增 field provenance、EVTX event fields 和 parser/source 展示；HTML escaping 与 CSV formula sanitization 回归测试保留。
- `deny.toml` 新增 `RUSTSEC-2021-0153` 临时例外，跟踪 `evtx -> encoding` unmaintained transitive advisory，owner/reason/expiry 均已记录。
- 更新 `docs/remediation-plan-v2.1.md`、`docs/architecture-model.md`、`docs/algorithm-complexity-analysis.md`，修正旧的 Partial/stub 状态。

### 验证结果

- `cargo fmt --all -- --check`: 通过。
- `cargo clippy --workspace --all-targets -- -D warnings`: 通过。
- `cargo test --workspace`: 通过；真实 E01 tests 默认 ignored，需 `FORENSICS_E01_FIXTURE` 手动运行。
- `cargo test -p app-services --lib analysis_service`: 通过，9 tests。
- `cargo test -p app-services --lib report_service`: 通过，4 tests。
- `cargo test -p artifacts-windows registry`: 通过，registry lookup/parser targeted tests。
- `cargo test -p artifacts-windows evtx`: 通过，5 targeted tests。
- `cargo test -p transport analysis`: 通过，3 tests。
- `pnpm --dir frontend typecheck`: 通过。
- `pnpm --dir frontend lint`: 通过，0 error，7 个既有 warning（MCP/DenseDataTable/SyntaxHighlighter/tauri-bridge）。
- `pnpm --dir frontend test -- analysis files`: 通过，4 files / 20 tests。
- `pnpm --dir frontend test`: 通过，13 files / 53 tests；ErrorBoundary 用例仍打印预期 jsdom stack，退出码 0。
- `pnpm --dir frontend build`: 通过；Vite chunk size warning 存在。
- `cargo build -p forensics-desktop`: 通过。
- `powershell -ExecutionPolicy Bypass -File scripts\check-release-guard.ps1`: 通过。
- `cargo doc --workspace --no-deps`: 通过。
- `cargo deny check advisories bans licenses sources`: 通过；仍有 duplicate dependency warnings；`RUSTSEC-2021-0153` 作为到期例外跟踪。
- `cargo audit`: 通过，退出码 0；报告 20 个 warning-class advisories，包括本轮 `encoding`。
- `pnpm --dir frontend audit --audit-level high`: 通过，No known vulnerabilities。
- Slow E01 manual gate 未运行：当前环境未设置 `FORENSICS_E01_FIXTURE`。运行条件为提供合法真实 E01 样本后执行 `cargo test -p app-services --test e01_full_pipeline_test -- --ignored --nocapture` 和 `cargo test -p app-services --test e01_mft_scan_test -- --ignored --nocapture`。

### 剩余风险

- EVTX adapter 尚缺仓库内合法 tiny real `.evtx` fixture；当前单测覆盖 JSON record extraction、malformed 和 oversized path，但真实 parser path 仍需 fixture 补强。
- Registry parser 是 Analysis 所需定向字段 parser，不是完整 hive browser；后续扩展 hive traversal 前需要重新评估 cell graph 覆盖和复杂度。
- `evidence-media` 已编译和单测通过，但尚未做 Windows 桌面 WebView2 播放器 seek smoke。
- `evtx -> encoding` unmaintained advisory 是临时例外，2026-09-01 前需要替换 parser path、升级依赖或重新评审。
- tiny E01 fixture 尚未入库；真实样本慢测仍依赖 `FORENSICS_E01_FIXTURE`。

---

## EVTX fixture/dependency 风险收束

**时间戳**: 2026-06-02 02:47:09 +08:00
**署名**: Codex
**基线**: `7d12299 feat: add beta media and analysis parsers`

### 开发前文档复核

- 已读取 `docs/remediation-plan-v2.1.md`、`docs/开发记录.md` 和本 session 记录。
- 复核结论：上一轮已把 media/Registry/EVTX 推进到 beta 状态，当前最小高价值闭环是把 EVTX fixture 缺口和 `encoding` advisory 依赖事实固定下来，避免后续把它误判为 feature 配置遗漏。
- 未启动新的 subagents；因此没有闲置 agent 需要关闭。

### 本轮改动摘要

- 新增 EVTX parser truncated magic 回归：输入仅包含 `ElfFile\0` 时必须返回 warning 且不 panic，补强 malformed path 覆盖。
- 本地复核 `evtx 0.11.2` crates.io package：`Cargo.toml.orig` 声明 `exclude = ["**/*.evtx", "**/*.dat"]`，发布包不含可直接复用的真实 `.evtx` fixture；license 为 `MIT/Apache-2.0`。
- 本地复核依赖链：`encoding v0.2.33 -> evtx v0.11.2 -> artifacts-windows`，且 `encoding = "0.2.33"` 是 `evtx` 直接非可选依赖，不能通过关闭 feature 消除 `RUSTSEC-2021-0153`。
- 更新 `docs/remediation-plan-v2.1.md` 和 `docs/开发记录.md`，明确可选后续路线：生成/引入可再分发 EVTX fixture、替换 parser、vendor/patch parser，或在 2026-09-01 前重新评审 `deny.toml` 例外。

### 验证结果

- `cargo test -p artifacts-windows evtx`: 通过，6 个 EVTX targeted tests。
- `cargo fmt --all -- --check`: 通过。
- `cargo clippy --workspace --all-targets -- -D warnings`: 通过。
- `git diff --check`: 通过。

### 剩余风险

- 真实 EVTX parser path 仍缺合法 tiny real `.evtx` fixture。
- `evtx -> encoding` unmaintained advisory 仍是到期例外，需在 2026-09-01 前处理。

---

## Dependency exception guard

**时间戳**: 2026-06-02 02:52:17 +08:00
**署名**: Codex
**基线**: `21f1546 docs: record evtx dependency limits`

### 本轮改动摘要

- 新增 `scripts/check-deny-exceptions.ps1`，扫描 `deny.toml` 的 `RUSTSEC-*` advisory ignore 项，要求每条例外含 `owner:`、`expires: YYYY-MM-DD` 和说明文本；过期例外直接失败。
- 将该脚本接入 `.github/workflows/ci-backend.yml`，在 release guard 后、`cargo deny` 前执行。
- 更新 `docs/remediation-plan-v2.1.md` 与 `docs/开发记录.md`，把 dependency exception guard 纳入 Phase 5.2 dependency gate 证据。
- 未启动新的 subagents；因此没有闲置 agent 需要关闭。

### 验证结果

- `powershell -ExecutionPolicy Bypass -File scripts\check-deny-exceptions.ps1`: 通过，18 条 advisory exceptions checked。
- `powershell -ExecutionPolicy Bypass -File scripts\check-release-guard.ps1`: 通过。
- `cargo fmt --all -- --check`: 通过。
- `cargo clippy --workspace --all-targets -- -D warnings`: 通过。
- `git diff --check`: 通过。

### 剩余风险

- Guard 只防止依赖例外元数据缺失或过期，不消除 advisory 本身。
- `evtx -> encoding` 和其他 upstream transitive advisories 仍需按 expiry 前升级、替换或重新评审。

---

## Settings config closure

**时间戳**: 2026-06-02 02:58:33 +08:00
**署名**: Codex
**基线**: `14fd45f ci: guard dependency exceptions`

### 本轮改动摘要

- 复核 Settings 实现，确认路径类设置已经通过后端 `AppSettingsDto` config 持久化和校验；theme/dev event trace 同步写入后端 DTO，并镜像 localStorage 用于 mock/dev 即时生效。
- 给 Settings 页面案件目录和镜像搜索路径输入补显式 label，改善可访问性。
- 新增 `frontend/src/app/pages/Settings.test.tsx`，覆盖远端配置加载、保存 API payload、localStorage mirror、主题应用、mock/local fallback 和非法路径拒绝。
- 更新 `docs/remediation-plan-v2.1.md` 和 `docs/开发记录.md`，将 Phase 3.8 从 Partial 收口为 Completed。
- 未启动新的 subagents；因此没有闲置 agent 需要关闭。

### 验证结果

- `pnpm --dir frontend test -- Settings`: 通过，3 files / 11 tests。
- `pnpm --dir frontend typecheck`: 通过。
- `pnpm --dir frontend lint`: 通过，0 error / 7 existing warnings。
- `git diff --check`: 通过。

### 剩余风险

- Settings 的 localStorage mirror 是前端 mock/dev 体验设计，不替代后端 config；路径类设置仍以后端校验和持久化为准。

---

## Event bridge closure

**时间戳**: 2026-06-02 03:05:16 +08:00
**署名**: Codex
**基线**: `4acfe79 test: cover settings config flow`

### 本轮改动摘要

- 新增 transport typed topic `data-source-imported` 与 `job-cancelled`，并同步 serde/as_str 测试。
- `event_bridge` 新增 `emit_data_source_imported()` 与 `emit_job_cancelled()`；data source imported payload 不包含宿主证据路径，只包含 `dataSourceId`、`name`、`kind`、`jobId`。
- Import 完成 finalizing 阶段发 `data-source-imported`；`cancel_import` 成功请求取消时发 `job-cancelled`。
- 前端 `EventTopic` union 和 Tauri bridge 监听列表同步新增 topic；新增 EventBus typed topic test。
- 更新 `docs/remediation-plan-v2.1.md` 与 `docs/开发记录.md`，将 Phase 4.2 收口为 Completed。
- 未启动新的 subagents；因此没有闲置 agent 需要关闭。

### 验证结果

- `cargo test -p transport events`: 通过。
- `cargo clippy -p forensics-desktop --all-targets -- -D warnings`: 通过。
- `pnpm --dir frontend test -- events`: 通过，1 file / 1 test。
- `pnpm --dir frontend typecheck`: 通过。
- `cargo fmt --all -- --check`: 通过。

### 剩余风险

- `job-cancelled` 当前是事件层语义；job snapshot 的最终 status 仍保持现有兼容模型，未新增 cancelled status/migration。

---

## Command SQL boundary guard

**时间戳**: 2026-06-02 03:10:13 +08:00
**署名**: Codex
**基线**: `668904c feat: complete event bridge topics`

### 本轮改动摘要

- 复核 `apps/desktop/src-tauri/src/commands`，当前无原始 SQL 语句或 `rusqlite::params!/prepare/execute` 低层调用。
- 新增 `scripts/check-command-sql-boundary.ps1`，拦截 Tauri command 层中的显式 SQL 和底层 rusqlite SQL API，防止业务 SQL 回流 command layer。
- 将该 guard 接入 `.github/workflows/ci-backend.yml`。
- 更新 `docs/remediation-plan-v2.1.md` 与 `docs/开发记录.md`，将 Phase 6.2 收口为 Completed / guarded。
- 未启动新的 subagents；因此没有闲置 agent 需要关闭。

### 验证结果

- `powershell -ExecutionPolicy Bypass -File scripts\check-command-sql-boundary.ps1`: 通过。
- `powershell -ExecutionPolicy Bypass -File scripts\check-release-guard.ps1`: 通过。
- `powershell -ExecutionPolicy Bypass -File scripts\check-deny-exceptions.ps1`: 通过。
- `cargo fmt --all -- --check`: 通过。
- `git diff --check`: 通过。

### 剩余风险

- Guard 约束 command layer 边界，不替代 repository/service 层 SQL 测试。

---

## Beta final gate and review closeout

**时间戳**: 2026-06-02 03:19:24 +08:00
**署名**: Codex
**基线**: `af2833c ci: guard command sql boundary`

### 本轮改动摘要

- 未启动新的 subagents，因此没有闲置 agent 需要关闭。
- 复核当前分支 `codex/beta-forensics-backlog`，工作树仅保留用户侧未跟踪 `.context/plans.md`。
- 重新读取 `docs/remediation-plan-v2.1.md`、`docs/architecture-model.md`、`docs/algorithm-complexity-analysis.md`、`docs/开发记录.md` 和本 session 记录。
- 补跑可信 beta final gate，完成统一 grep 审阅，并更新 `docs/remediation-plan-v2.1.md` / `docs/开发记录.md` 的最终测试计数与剩余风险。

### 验证结果

- `cargo fmt --all -- --check`: 通过。
- `cargo clippy --workspace --all-targets -- -D warnings`: 通过。
- `cargo test --workspace`: 通过；真实 E01 tests 默认 ignored。
- `cargo build -p forensics-desktop`: 通过。
- `cargo doc --workspace --no-deps`: 通过。
- `powershell -ExecutionPolicy Bypass -File scripts\check-release-guard.ps1`: 通过。
- `powershell -ExecutionPolicy Bypass -File scripts\check-command-sql-boundary.ps1`: 通过。
- `powershell -ExecutionPolicy Bypass -File scripts\check-deny-exceptions.ps1`: 通过，18 条 advisory exceptions checked。
- `cargo deny check advisories bans licenses sources`: 通过；仍有 duplicate dependency warnings。
- `cargo audit`: 通过，退出码 0；报告 20 个 allowed warning-class advisories。
- `pnpm --dir frontend typecheck`: 通过。
- `pnpm --dir frontend lint`: 通过，0 error / 7 existing warnings。
- `pnpm --dir frontend test`: 通过，15 files / 58 tests；ErrorBoundary 用例打印预期 jsdom stack。
- `pnpm --dir frontend build`: 通过；Vite 主 chunk 约 520KB，仍提示 chunk size warning。
- `pnpm --dir frontend audit --audit-level high`: 通过，无已知漏洞。
- `git diff --check`: 通过。

### 统一审阅

- 伪取证事实 grep 命中只存在于审计文档、负向测试或时间戳单测注释；业务代码和 UI 不输出 `FORENSICS-PC`、默认 `Windows 10`、默认 `19045` 或固定 boot timestamp。
- `@tauri-apps/api/core` / `invoke` 只存在于 `frontend/src/lib/api/client.ts`，页面和 feature hooks 没有绕过 `apiClient.request(...)`。
- 事件 bridge 的 data source imported payload 不含宿主证据路径；media preview/range 路径仍走 opaque handle 和 reader helper。
- Analysis/media/extract file 检查范围内未发现 `case_root.join` 或无界 `std::fs::read` 读取证据内容的回退；仅测试断言读取导出的 destination 文件。

### 剩余风险

- EVTX adapter 仍缺合法 tiny real `.evtx` fixture。
- Registry parser 是 Analysis 定向字段 parser，不是完整 hive browser。
- `evidence-media://` protocol 尚未做真实 Windows WebView2 桌面 seek smoke。
- `evtx -> encoding` advisory 例外需在 2026-09-01 前处理。
- tiny E01 fixture 尚未入库；slow E01 gate 未运行，因为当前环境未设置 `FORENSICS_E01_FIXTURE`。

---

## FS reader helper dedupe

**时间戳**: 2026-06-02 03:26:01 +08:00
**署名**: Codex
**基线**: `ad4ccd2 docs: record beta final gate`

### 本轮改动摘要

- 未启动新的 subagents，因此没有闲置 agent 需要关闭。
- 复核剩余 backlog 后选择本地可验证的 Phase 6.1 子项，避开需要外部样本或人工 smoke 的 EVTX/tiny E01/media protocol 项。
- 在 `evidence-core::filesystem` 新增公共 helper：root node、path join、常见 path/file not-found 和 directory/not-directory error。
- `fs-fat`、`fs-ntfs`、`fs-exfat` 改为复用公共 helper，删除重复 root node 构造和本地 path join/error 构造。FAT/NTFS 继续输出 slash-separated path；exFAT 继续输出 backslash-separated path。
- 更新 `docs/remediation-plan-v2.1.md` 和 `docs/开发记录.md`，把 Phase 6.1 标为 Partial，明确本轮不是完整 FS 枚举流程 refactor。

### 验证结果

- `cargo fmt --all -- --check`: 通过。
- `cargo test -p evidence-core filesystem`: 通过。
- `cargo test -p fs-fat`: 通过。
- `cargo test -p fs-exfat`: 通过。
- `cargo test -p fs-ntfs`: 通过。
- `cargo test -p app-services file_service`: 通过。
- `cargo test -p app-services --test file_service_real_test`: 通过。
- `cargo clippy --workspace --all-targets -- -D warnings`: 通过。

### 剩余风险

- FAT/NTFS/exFAT 深层枚举流程和 FS 特定错误转换仍可继续去重，但需要更大行为回归面。
- 本轮保持 public API、root semantics、排序和 path separator convention 不变。

---

## Frontend lint warning cleanup

**时间戳**: 2026-06-02 10:23:41 +08:00
**署名**: Codex
**基线**: `c32febf refactor: share filesystem path helpers`

### 本轮改动摘要

- 未启动新的 subagents，因此没有闲置 agent 需要关闭。
- 工作树仅保留用户侧未跟踪 `.context/plans.md`；本轮未 stage、未删除该文件。
- 清理前端既有 lint warning：未使用 import、`any`、未使用 highlighter prop、未使用 event bridge type import。
- `DenseDataTable` 的 memo row 保持泛型签名，避免用 `any` 弱化表格行类型。
- 更新 `docs/remediation-plan-v2.1.md`，把当前 frontend lint gate 标为 `0 error / 0 warnings`。

### 验证结果

- `pnpm --dir frontend lint`: 通过，0 error / 0 warnings。
- `pnpm --dir frontend typecheck`: 通过。
- `pnpm --dir frontend test`: 通过，15 files / 58 tests；ErrorBoundary 用例打印预期 jsdom stack。
- `pnpm --dir frontend build`: 通过；仍提示既有 Vite chunk size warning，主 chunk 约 520KB。

### 剩余风险

- Vite chunk size warning 尚未处理；建议作为后续独立小闭环，通过路由级 code splitting 或 `manualChunks` 验证。

---

## Frontend route-level code splitting

**时间戳**: 2026-06-02 10:27:03 +08:00
**署名**: Codex
**基线**: `890271c chore: clear frontend lint warnings`

### 本轮改动摘要

- 未启动新的 subagents，因此没有闲置 agent 需要关闭。
- 工作树仅保留用户侧未跟踪 `.context/plans.md`；本轮未 stage、未删除该文件。
- 将 `frontend/src/app/routes.tsx` 中页面级静态 imports 改为 React Router `lazy` route imports。
- 保留 `Layout` 静态导入，避免改变 app shell 结构；CaseHome、Analysis、Files、Search、Timeline、Artifacts、Reports、Settings 按路由拆包。
- 更新 `docs/remediation-plan-v2.1.md`，把当前 frontend build gate 标为无 chunk size warning，主 chunk 约 315KB。

### 验证结果

- `pnpm --dir frontend typecheck`: 通过。
- `pnpm --dir frontend lint`: 通过，0 error / 0 warnings。
- `pnpm --dir frontend test`: 通过，15 files / 58 tests；ErrorBoundary 用例打印预期 jsdom stack。
- `pnpm --dir frontend build`: 通过；无 Vite chunk size warning，主 chunk 约 315KB，页面 chunks 独立输出。

### 剩余风险

- 本轮没有做 vendor `manualChunks`；当前 build 已低于 Vite 默认 warning 阈值，后续依赖增长时再专项处理。

---

## Route splitting regression guard

**时间戳**: 2026-06-02 10:30:21 +08:00
**署名**: Codex
**基线**: `032902a perf: split frontend routes lazily`

### 本轮改动摘要

- 未启动新的 subagents，因此没有闲置 agent 需要关闭。
- 工作树仅保留用户侧未跟踪 `.context/plans.md`；本轮未 stage、未删除该文件。
- `frontend/src/app/routes.tsx` 导出 `appRoutes`，新增 `frontend/src/app/routes.test.ts`，断言 root layout 静态加载、页面 child routes 全部 lazy-loaded。
- `frontend/src/stores/ui-store.ts` 增加 safe localStorage read/write helper，避免 route config 测试导入 app shell 时因 storage API 不完整而崩溃；浏览器持久化语义保持不变。
- 更新 `docs/remediation-plan-v2.1.md`，把当前 frontend test gate 计数更新为 16 files / 59 tests。

### 验证结果

- `pnpm --dir frontend test -- routes`: 通过，1 file / 1 test；命令输出 Node `--localstorage-file` warning，但退出码为 0。
- `pnpm --dir frontend test`: 通过，16 files / 59 tests；ErrorBoundary 用例打印预期 jsdom stack。
- `pnpm --dir frontend typecheck`: 通过。
- `pnpm --dir frontend lint`: 通过，0 error / 0 warnings。
- `pnpm --dir frontend build`: 通过；无 Vite chunk size warning，主 chunk 约 315KB。

### 剩余风险

- route-level lazy split 已有防回退测试；更细的 vendor/common chunk 优化仍可作为后续性能专项。

---

## Tiny E01 reader fixture

**时间戳**: 2026-06-02 10:39:22 +08:00
**署名**: Codex
**基线**: `fd7c29f test: guard lazy route splitting`

### 本轮改动摘要

- 未启动新的 subagents，因此没有闲置 agent 需要关闭。
- 工作树仅保留用户侧未跟踪 `.context/plans.md`；本轮未 stage、未删除该文件。
- `scripts/generate-tiny-fixtures.ps1` 现在同时生成 RAW 和 deterministic synthetic single-segment E01 tiny fixtures。
- 新增 `testdata/fixtures/tiny/e01/tiny.E01`，大小 4405 bytes，用于默认 CI 覆盖 `image-e01` reader section/table/read/seek。
- `crates/testing::fixtures` 新增 `tiny_e01_image()`，`crates/image-e01` 默认 regression tests 使用该 fixture 读取 marker、signature、seek/end-of-file。
- 文档同步更新：tiny E01 是 reader fixture，不是完整 filesystem image；真实 E01 slow/manual gate 仍依赖 `FORENSICS_E01_FIXTURE`。

### 验证结果

- `powershell -ExecutionPolicy Bypass -File scripts\generate-tiny-fixtures.ps1`: 通过，生成 RAW 1024 bytes 和 E01 4405 bytes。
- `cargo test -p image-e01`: 通过；6 lib tests，4 default regression tests，7 ignored real-E01 dump/slow tests。
- `cargo test -p testing`: 通过，4 fixture helper tests。
- `cargo fmt --all -- --check`: 通过。
- `cargo clippy -p image-e01 --all-targets -- -D warnings`: 通过。

### 剩余风险

- `testdata/fixtures/tiny/e01/tiny.E01` 不能替代真实 E01 partition/filesystem/import 慢测；这些仍需 `FORENSICS_E01_FIXTURE`。
- EVTX adapter 仍缺合法 tiny real `.evtx` fixture。

---

## Coverage reporting gate

**时间戳**: 2026-06-02 10:56:14 +08:00
**署名**: Codex
**基线**: `e3692f2 test: add tiny e01 fixture`

### 本轮改动摘要

- 未启动新的 subagents，因此没有闲置 agent 需要关闭。
- 工作树仅保留用户侧未跟踪 `.context/plans.md`；本轮未 stage、未删除该文件。
- 为剩余 Phase 5.6 coverage 子项新增可执行入口：前端 `test:coverage`、`@vitest/coverage-v8`、统一 `scripts/run-coverage.ps1`。
- 后端 CI 新增 `cargo-llvm-cov` LCOV 生成和 `backend-coverage` artifact；前端 CI 新增 `frontend-coverage` artifact。
- `.gitignore` 忽略根目录 `coverage/` 和 `frontend/coverage/`；`scripts/README.md` 增加 coverage 用法。
- 文档更新为“coverage report 已落地，阈值待稳定 baseline 后再定”，避免把报告入口夸大为严格覆盖率门禁。

### 验证结果

- `pnpm --dir frontend test:coverage`: 通过；16 个测试文件 / 59 个测试，生成 `frontend/coverage/coverage-summary.json`、`lcov.info`，当前总行覆盖率约 47.97%。
- `powershell -ExecutionPolicy Bypass -File scripts\run-coverage.ps1 -Frontend`: 通过；统一 runner 的前端 coverage 路径可用。
- `pnpm --dir frontend typecheck`: 通过。
- `pnpm --dir frontend lint`: 通过，0 error / 0 warnings。
- `pnpm --dir frontend test`: 通过；16 个测试文件 / 59 个测试，ErrorBoundary 用例打印预期 jsdom stack，退出码为 0。
- `pnpm --dir frontend build`: 通过；无 Vite chunk size warning，主 chunk 约 315KB。
- `pnpm --dir frontend audit --audit-level high`: 通过，无已知漏洞。
- `pnpm --dir frontend install --frozen-lockfile`: 通过。
- `powershell -ExecutionPolicy Bypass -File scripts\run-coverage.ps1 -Rust`: 通过；本机未安装 `cargo-llvm-cov`，按设计 warning 后跳过 Rust coverage。
- `cargo fmt --all -- --check`: 通过。
- `git diff --check`: 通过。

### 剩余风险

- Coverage 当前只作为 CI artifact，不设置百分比阈值。
- Rust coverage 需要 `cargo-llvm-cov` 和 `llvm-tools-preview`；CI 会安装，本地脚本默认缺工具时 warning 跳过。
- EVTX 合法 tiny real `.evtx` fixture、Windows WebView2 media smoke、`evtx -> encoding` advisory cleanup 仍未完成。
