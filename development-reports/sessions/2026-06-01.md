# 2026-06-01 开发记录

## 全量审计与项目评估

**时间戳**: 2026-06-01 20:07:28 +08:00  
**署名**: Codex  
**报告**: `docs/full-project-audit-2026-06-01.md`

### 本轮范围

- 按项目约定先检查 `docs/`，阅读并评审 2026-06-01 附近新增的 `docs/algorithm-complexity-analysis.md` 与 `docs/architecture-model.md`。
- 审计当前 dirty tree 中新增的 `analysis_service`、Tauri analysis commands、`DataAnalysis.tsx`、MFT 路径重建优化和新增测试。
- 复核 Rust 后端、Tauri command、SQLite migration、frontend API/mock/type 契约、文件预览与导入后处理链路。
- 运行可行质量门禁并记录结果。

### 当前改动情况

本轮未修改业务代码，只新增审计文档和本开发记录。工作区在审计开始前已有以下未提交改动：新增 `analysis_commands.rs`、`analysis_service.rs`、`DataAnalysis.tsx`，修改 command 注册、app-services 导出、MFT 路径重建和若干测试/文档。

### 最新文档评审

- `docs/architecture-model.md`: 架构描述方向基本正确，但测试数量、性能指标、安全能力等表述与当前验证结果不一致。建议改成“设计目标/当前实现/验证状态”。
- `docs/algorithm-complexity-analysis.md`: 复杂度框架有价值，但路径重建复杂度与当前代码修改不一致；Magic 检测文档忽略当前实现读取整文件的 I/O 成本；MFT 扫描的 `O(n/p)` 表述需要区分 CPU 解析、I/O 和 SQLite 写入瓶颈。

### 验证结果

- `cargo fmt --all -- --check`: 失败，多文件需要格式化。
- `cargo clippy --workspace --all-targets -- -D warnings`: 失败，domain/evidence-core 旧问题和新增 analysis unused warnings。
- `cargo test --workspace`: 失败，`case_service_test` 迁移版本断言仍指向 `0010_job_partition_progress`。
- `cargo test --workspace --no-fail-fast`: 失败；确认 `case_service_test`、`connection_test`、`domain doctest` 失败；`e01_full_pipeline_test` 中慢测长时间无输出后被本轮手动停止。
- `pnpm typecheck`: 通过。
- `pnpm test`: 通过，5 个测试文件 / 22 个测试。
- `pnpm build`: 通过。
- `pnpm lint`: 失败，`mcp-store.ts` 有 `preserve-caught-error` error，新增 `DataAnalysis.tsx` 有 `any` warning。

### 主要结论

- 新增 `analysis_service` 当前会返回硬编码系统信息和开机记录，不具备取证可信度，必须在进入 UI/报告前禁用或改为真实解析。
- `analysis_commands` 用 `case_root.join(entry.path)` 读取文件，既读错 backing store，也绕过既有路径安全与镜像读取逻辑。
- 导入后的工件抽取/全文索引 reader function 把 `FileEntryId` 当路径使用，存在读取不到真实内容的断链风险。
- SQLite migration runner 会在迁移失败后标记 applied，这是数据库可靠性高风险问题。
- transport/frontend 契约被新增 analysis 功能绕过，需要补 DTO、API client、mock provider 和 hook。

### 后续建议

优先恢复门禁，再处理伪证据输出和统一文件读取路径。建议下一轮按 `docs/full-project-audit-2026-06-01.md` 的 Phase 0-2 执行：格式化与 clippy/test/lint 修复、禁用硬编码分析输出、把 analysis DTO 迁入 transport 并接入 `apiClient`。

---

## v2 修复方案实施

**时间戳**: 2026-06-01 21:20:29 +08:00
**署名**: Codex
**依据**: 用户提供的 `Forensics Workbench 修复方案 v2` 与前端复核结果

### 本轮完成项

| Phase | Task | 状态 | 说明 |
|------|------|------|------|
| Phase 0 | Rust fmt/clippy/test 与 domain doctest | 完成 | 修复 timestamp range/identity op、IO error 构造、unused mut、多个 clippy `-D warnings` 问题；默认 Rust 门禁通过 |
| Phase 0 | 前端 lint/type/test/build | 完成 | 修复 `mcp-store.ts` preserve-caught-error 与 DataAnalysis `as any`；前端专项和全量测试通过 |
| Phase 0 | 慢测分层 | 完成 | `e01_ntfs_enumerate_limited` 标记 ignored，从默认 `cargo test --workspace` 分离 |
| Phase 1 | 禁止伪取证输出 | 完成 | analysis 系统信息不再返回 `FORENSICS-PC`、`Windows 10`、`19045`、`User` 或固定 boot timestamp；未接 parser 时返回 `notParsed` 和 warning |
| Phase 1 | Summary 修正 | 完成 | `generate_analysis_summary` 基于 DTO 状态输出“未解析”，不输出硬编码取证事实 |
| Phase 2 | Analysis transport DTO/request | 完成 | 新增 `crates/transport/src/dto/analysis.rs` 与 `ClassifyFilesRequest { sampleSize }`；serde 字段为 camelCase |
| Phase 2 | sampleSize 边界 | 完成 | 默认 1000，最大 5000，0/超限返回 invalid input |
| Phase 3 | 前端 API/hook/mock/UI/nav | 完成 | 新增 `frontend/src/lib/api/analysis.ts`、`frontend/src/features/analysis/hooks.ts`、mock provider/data；DataAnalysis 移除 direct invoke，接入 active case gate、Tabs ARIA、导航和 PageKey |
| Phase 4 | 统一文件读取链路 | 完成 | 新增 `open_file_content_by_id` / `read_file_header_by_id`；analysis、text/image preview、post-import pipeline 改走 `FileEntryId + DataSourceKind` helper；magic header 限制 8KB，exact-length magic 修复；补 traversal path 回归 |
| Phase 5 | 导入后处理 reader | 完成 | post-import artifact/search reader 不再把 `FileEntryId` 当路径；新增 logical import 回归，验证唯一 marker 可搜索命中且 Prefetch fixture 产出 artifact |
| Phase 6 | SQLite migration 可靠性 | 完成 | migration runner 失败 rollback 且不写 applied；`0016_add_cascade_delete` 改显式列清单并保留 job partition 字段；补旧 schema 升级 fixture、data source 删除 cascade/orphan/audit 回归 |
| Phase 7 | 文件系统与预览安全 | 完成 | logical directory 枚举不下钻 symlink/reparse point；image preview 加 5MB 上限；media 不返回 absolute host path，当前小文件用 20MB data URL 并有回归测试；大媒体 streaming/protocol 作为后续增强 |
| Phase 8 | 文档 | 完成 | 更新 `docs/architecture-model.md`、`docs/algorithm-complexity-analysis.md`，补 Analysis API contract、当前 parser 状态和验证状态 |

### 关键实现说明

- Analysis API 的后端响应类型进入 `transport::dto::analysis`，命令层返回 transport DTO，前端只消费 `types/models.ts`、API wrapper 和 hooks。
- `DataAnalysis.tsx` 不再 import `@tauri-apps/api/core`；mock 模式下 `/analysis` 可通过 `apiClient.request(...)` 正常渲染。
- 系统信息 parser 未接入前，UI、API、summary 均以 `notParsed`/“未解析”表达，不制造默认主机事实。
- 文件分类不读整文件，只读取 bounded header；读取入口统一到 file service helper，避免绕过 logical/E01/RAW reader。
- `0016_add_cascade_delete.sql` 不再使用脆弱的 `INSERT INTO jobs_new SELECT * FROM jobs`，避免列数/顺序漂移导致迁移失败。

### 验证结果

- `cargo fmt --all -- --check`: 通过。
- `cargo clippy --workspace --all-targets -- -D warnings`: 通过。
- `cargo test -p domain --doc`: 通过，3 个 doctest。
- `cargo test -p app-services --test case_service_test`: 通过，9 个测试。
- `cargo test -p persistence-sqlite --test connection_test`: 通过，8 个测试。
- `cargo test -p forensics-desktop commands::import::pipeline::tests::logical_import_post_pipeline_indexes_marker_and_extracts_artifact -- --nocapture`: 通过，覆盖 post-import 搜索索引和 Prefetch artifact 抽取。
- `cargo test -p persistence-sqlite --test connection_test upgrades_legacy_schema_to_latest_with_partition_job_columns`: 通过，覆盖旧 schema 升级到 latest 并保留 job partition 字段。
- `cargo test -p app-services --test case_service_test delete_data_source_cascades_rows_and_writes_audit_log`: 通过，覆盖 data source 删除后无 orphan rows 且 audit log 存在。
- `cargo test -p forensics-desktop commands::file_commands::tests::media_preview_returns_data_url_without_host_path -- --nocapture`: 通过，覆盖 media preview 不泄露 host path / `asset://` / `file:` handle。
- `cargo test --workspace`: 通过；`e01_ntfs_enumerate_limited` 作为 slow test ignored。
- `pnpm --dir frontend typecheck`: 通过。
- `pnpm --dir frontend lint`: 通过，0 error，7 个既有 warning。
- `pnpm --dir frontend test -- analysis`: 通过，3 个文件 / 11 个测试。
- `pnpm --dir frontend test`: 通过，8 个文件 / 33 个测试。
- `pnpm --dir frontend build`: 通过。
- `cargo test -p app-services --test e01_mft_scan_test -- --nocapture`: 通过，2 个测试；真实样本输出显示 `$MFT` 约 196,352 records，多线程枚举 140,025 files / 25,918 directories，测试总耗时约 9.67s。
- `cargo test -p app-services --test e01_full_pipeline_test -- --ignored --nocapture`: 未在本轮完成；该 ignored 测试走 full BFS 枚举本地多 GB E01 样本，超过 8 分钟无进一步输出后手动结束。默认门禁不包含该测试，建议后续单独长时窗口运行或将其改为 bounded fixture。

### 剩余风险 / 后续增强

- 大媒体预览当前为了避免路径泄露，采用 20MB 内 data URL；更完整的实现应增加 scoped streaming command 或受控 Tauri protocol。
- Registry/EVTX 系统信息 parser 尚未接入，因此 Analysis 系统信息仍会明确显示 `notParsed`。
- `e01_ntfs_enumerate_limited` full BFS 慢测仍需要单独长时窗口或改造成 bounded fixture。

---

## v2 慢测收口与 MFT 树修复

**时间戳**: 2026-06-01 22:20:57 +08:00
**署名**: Codex
**范围**: 继续用户要求的 v2 方案实施，补齐上一节未完成的 E01 ignored 慢测验收。

### 本轮改动

- 将 `e01_full_pipeline_test` 中原先会对多 GB E01 样本执行 full BFS 的 `e01_ntfs_enumerate_limited`，改为 `e01_ntfs_mft_enumeration_builds_navigable_tree`。
- 新测试仍使用真实样本 `E:/pangushi/刘洋/liuyang_pc.E01`，但通过 MFT 快速枚举路径完成验证；验收内容包括 DB 落库、文件/目录计数、`get_file_tree_real` 根节点可见，以及 `get_file_children_lazy` 可展开根目录。
- 修复 MFT 枚举落库的根节点和父节点语义：`mft:5` 被保留为唯一树根，根节点 `parent_id = NULL`；批量插入时先以空父节点落库，所有记录存在后再回填 `parent_id`，避免 SQLite 自引用外键受 MFT 乱序记录影响。
- 对 MFT 父记录缺失的目录/文件统一锚定到 `mft:5`，避免文件树首层出现大量 orphan roots。
- 增加 `file_service::tests::mft_root_record_becomes_tree_root` 与 `mft_orphan_records_are_anchored_to_root`，锁定 `mft:5` 作为导航根节点和 orphan 锚定行为。

### 文档复核

- 本轮开始前复核 `docs/architecture-model.md` 与 `docs/algorithm-complexity-analysis.md` 的 Analysis contract、预览读取、Magic 分类和慢测命令描述。当前 docs 中慢测命令仍匹配最新验收口径：`e01_full_pipeline_test --ignored --nocapture` 与 `e01_mft_scan_test --nocapture`。
- `docs/full-project-audit-2026-06-01.md` 保留的是 20:07 审计时点的失败记录，不作为当前实现状态；最新实现状态以本开发记录为准。

### 验证结果

- `cargo fmt --all -- --check`: 通过。
- `cargo clippy --workspace --all-targets -- -D warnings`: 通过。
- `cargo test -p app-services file_service::tests::mft_root_record_becomes_tree_root`: 通过。
- `cargo test -p app-services file_service::tests::mft_orphan_records_are_anchored_to_root`: 通过。
- `cargo test -p app-services --test e01_full_pipeline_test e01_ntfs_mft_enumeration_builds_navigable_tree -- --ignored --nocapture`: 通过；真实样本输出 140,025 files / 25,919 dirs / 156,208,584,777 bytes，验证 `Tree: 1 roots, 19769 children`，耗时约 54.50s。
- `cargo test -p app-services --test e01_full_pipeline_test -- --ignored --nocapture`: 通过；当前 ignored 慢测 1 个，输出 `Tree: 1 roots, 19769 children`，耗时约 73.79s。
- `cargo test -p app-services --test e01_mft_scan_test -- --nocapture`: 通过；2 个测试，真实样本输出 196,352 MFT records、140,025 files、25,919 dirs，耗时约 58.35s。
- `cargo test --workspace`: 通过；默认门禁中 `e01_ntfs_mft_enumeration_builds_navigable_tree` 仍 ignored，其他 workspace 测试和 doctest 全通过。
- `pnpm --dir frontend typecheck`: 通过。
- `pnpm --dir frontend lint`: 通过，0 error，7 个既有 warning。
- `pnpm --dir frontend test`: 通过，8 个文件 / 33 个测试。
- `pnpm --dir frontend build`: 通过。

### 当前验收状态

- v2 默认门禁已通过。
- v2 慢测验收已补齐；不再存在“ignored E01 slow test 未完成”的遗留项。
- 剩余产品增强仍是 Registry/EVTX parser 接入和大媒体 scoped streaming/protocol。

---

## v2.1 剩余 Backlog 修补

**时间戳**: 2026-06-01 23:23:29 +08:00  
**署名**: Codex  
**范围**: 继续用户提供的 `Forensics Workbench 剩余 v2.1 Backlog 修补方案`，在 v2 默认门禁已通过的基础上补齐低风险剩余项，并更新状态文档。

### 开发前文档复核

- 已读取 `docs/remediation-plan-v2.1.md`、`development-reports/sessions/2026-06-01.md`，并抽查 `docs/architecture-model.md`、`docs/algorithm-complexity-analysis.md` 中 Analysis、媒体预览、CI 与事件契约相关段落。
- 复核结论：v2.1 文档仍停留在旧版计划状态，未反映上一轮 v2/v2.1 已完成项；架构文档中的媒体预览描述仍停留在“小文件 data URL，大文件待 streaming/protocol”，需要补充本轮 scoped range command 状态。

### 本轮完成项

| Phase | Task | 状态 | 说明 |
|------|------|------|------|
| Phase 1 | Viewer request 校验 | 完成 | 新增 `MediaRangeRequestDto` / `MediaRangeResponseDto`，`handleId`、`length` 校验并将 range 长度 clamp 到 1MB |
| Phase 3 | 大媒体 scoped streaming | 部分完成 | `get_media_url` 对大媒体返回 opaque `handleId`、MIME、size、`canReadRanges`；新增 `read_media_range` command 通过 evidence reader helper 读取 bounded byte range；前端用首个 chunk 构造 blob URL，不暴露宿主绝对路径。完整连续 streaming/protocol 仍为后续增强 |
| Phase 3 | Settings editable config | 部分完成 | 新增 `get_app_settings` / `save_app_settings` Tauri commands，路径类设置经后端 validate 后写入 `app-settings.json`；前端保存时同时写 localStorage 以保持 mock/dev 即时主题和调试开关体验 |
| Phase 4 | Event bridge 扩展 | 部分完成 | `artifact-added` emit helper 接入 post-import artifact store 后循环 emit；case/job/timeline/search/partition emit 保持 typed topic + targeted emit |
| Phase 5 | Workflow 去重 | 完成 | 修正 `ci-frontend.yml` 在仓库根执行脚本的问题，所有 pnpm 命令改为 `pnpm --dir frontend ...`，SBOM 输出路径同步到 `frontend/frontend-sbom.json` |
| Phase 6 | 文档更新 | 完成 | 重写 `docs/remediation-plan-v2.1.md` 为当前状态表；更新 architecture/complexity 中 Settings、typed events、媒体 range 与 CI 状态 |

### 关键实现说明

- 大媒体预览现在的安全边界是“后端受控 opaque handle + bounded Tauri command range read”，不是裸 `file:` / `asset://` / absolute host path。由于浏览器 `<video>`/`<audio>` 不能把 Tauri command 直接当 HTTP range stream 使用，当前前端只读取首个 bounded chunk 构造 blob 预览；真正 seek-aware continuous streaming 仍需后续 protocol/stream abstraction。
- Settings 后端配置文件路径复用 Tauri app config dir 下的 `app-settings.json`；保存前会验证 case root 和 image search paths 是可访问目录，并拒绝 NUL/device/extended-length/reserved device path。
- `artifact-added` payload 只包含 artifact id/type，不携带 evidence host path。

### 本轮 targeted 验证结果

- `pnpm --dir frontend test -- settings files`: 通过，3 个测试文件 / 15 个测试。
- `cargo test -p transport`: 通过，26 个测试。
- `cargo test -p forensics-desktop commands::settings_commands`: 通过，2 个测试。
- `cargo test -p forensics-desktop commands::file_commands::tests::oversized_media_preview_returns_scoped_handle_and_range_reads`: 通过。
- `cargo check -p forensics-desktop`: 通过。
- `pnpm --dir frontend typecheck`: 通过。

### 剩余风险 / 后续增强

- Registry/EVTX parser 尚未实现，Analysis system info 仍应保持 `notParsed`，不能生成演示事实。
- 大媒体预览仍未达到完整连续 streaming/protocol，只完成 scoped handle + bounded range command。
- Dependency gate 仍缺 `cargo deny`；SBOM 当前是 lightweight metadata/list JSON，不是正式 CycloneDX/SPDX。
- tiny RAW/E01 fixture 策略仍未完成，真实 E01 仍作为 ignored slow test 手动验收。
- job partial/warning count、Reports provenance、FS enum 去重、command layer SQL 下沉仍是后续 backlog。

### 最终门禁结果（本轮收尾）

- `cargo fmt --all -- --check`: 通过。
- `cargo clippy --workspace --all-targets -- -D warnings`: 通过。
- `cargo test --workspace`: 通过；`e01_ntfs_mft_enumeration_builds_navigable_tree` 仍 ignored，`e01_mft_scan_test` 默认执行并通过，本轮总输出包含真实样本 MFT 多线程测试约 66.42s。
- `pnpm --dir frontend typecheck`: 通过。
- `pnpm --dir frontend lint`: 通过，0 error，7 个既有 warning（MCP/DenseDataTable/SyntaxHighlighter/tauri-bridge）。
- `pnpm --dir frontend test`: 通过，11 个测试文件 / 46 个测试。
- `pnpm --dir frontend build`: 通过；Vite 提示主 chunk 约 508KB，属于 build warning。
- `cargo build -p forensics-desktop`: 通过。
- `powershell -ExecutionPolicy Bypass -File scripts\check-release-guard.ps1`: 通过，输出 `Release guard passed`。

### 当前未完成项确认

- Registry/EVTX parser 与 Analysis 字段级 provenance 未实现。
- 大媒体已具备 scoped handle + bounded `read_media_range`，但不是完整连续 streaming/protocol。
- `cargo deny`、正式 CycloneDX/SPDX SBOM、tiny RAW/E01 fixture、统一 job partial/warning count、Reports provenance、FS enum 去重与 SQL 下沉仍在 backlog。
